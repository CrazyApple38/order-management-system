# Q-G Scorecard (SC) v1

採点日: 2026-04-20
対象: Phase Q-G

## スコア

| 区分 | 配点 | 獲得 | 所見 |
|------|-----:|-----:|------|
| A. 死変数除去 | 20 | **18** | `--night-text` を削除（QA 本文参照ゼロ） |
| B. 未定義変数検出 | 30 | **30** | 32 種の `var()` 参照すべて co-tokens.css / QA :root で解決 |
| C. hard-coded 色値 | 20 | **16** | 装飾・警告ステート用のみに限定、カテゴリ色分化は QA 対象外のため許容 |
| D. JS 互換性 | 20 | **20** | 136 箇所の qa-* 参照すべて CSS で有効 |
| G. 完了度サマリ | 10 | **9** | 下記サマリ参照 |

**総合: 93/100 PASS**

## QA 新DS移行完了度サマリ

| 指標 | 値 |
|------|---:|
| `:root` 変数削減率 | **93%**（29 → 2） |
| トークン化率（主要装飾属性） | **約 85%**（color/radius/font-size/font-weight/transition/spacing） |
| alias 化率（共通トークン） | **100%**（全削除変数が co-tokens.css で解決） |
| a11y（`:focus-visible` 網羅率） | **約 90%**（主要ボタン 23 セレクタ + タブ） |
| print 対応 | **完全**（`@media print` 整備） |
| タッチターゲット 44px | **主要 5 ボタン + modal-btn** |
| reduced-motion | **完全**（全要素 0.01ms + パルス停止） |

## 重大Claim

- なし

## 総合評価

QA の新DS移行は高い完了度で達成。モバイル特有のUI（カード・カレンダー・アコーディオン編集パネル）に DS トークンを適用しつつ、JS 互換性を完全保持。値不一致の `--warning-text` / `--warning-bg` のみが意図的に残留する健全な状態。
