# テスト実施結果: D15-1 — Surface Tier 拡張（DS昇格のみ）

- 実施日: 2026-04-24
- 実施者: Test Executor（TE）
- TD仕様書: [d15-1-td-v1.md](./d15-1-td-v1.md)
- 検証手段: Grep / Read / Node (JSON構文検証) / git diff / Playwright MCP（order-book.html 実ブラウザ検証）

---

## A. DS準拠（40点満点）— 検証結果

### A1. co-tokens.css への追加

- **A1-1**. `--bg-surface-neutral: #F5F5F5;`
  - 実行コマンド: `grep -nE "^\s*--bg-surface-neutral:\s*#F5F5F5" docs/mockup/co-tokens.css`
  - 実行結果: `31:  --bg-surface-neutral:     #F5F5F5;`（1件ヒット）
  - 判定: 合致
- **A1-2**. `--bg-surface-neutral-alt: #F2F2F1;`
  - 実行結果: `32:  --bg-surface-neutral-alt: #F2F2F1;`（1件ヒット）
  - 判定: 合致
- **A1-3**. `--day-sat: #EAF0F1;`
  - 実行結果: `35:  --day-sat:       #EAF0F1;`（1件ヒット）
  - 判定: 合致
- **A1-4**. `--day-sun: #F3ECEE;`
  - 実行結果: `36:  --day-sun:       #F3ECEE;`（1件ヒット）
  - 判定: 合致
- **A1-5**. `--day-sat-head: #BECAC5;`
  - 実行結果: `37:  --day-sat-head:  #BECAC5;`（1件ヒット）
  - 判定: 合致
- **A1-6**. `--day-sun-head: #D4BEBD;`
  - 実行結果: `38:  --day-sun-head:  #D4BEBD;`（1件ヒット）
  - 判定: 合致
- **A1-7**. `--day-sat-cal: rgba(68, 166, 181, 0.06);`
  - 実行結果: `39:  --day-sat-cal:   rgba(68, 166, 181, 0.06);`（1件ヒット）
  - 判定: 合致
- **A1-8**. `--day-sun-cal: rgba(220, 120, 120, 0.06);`
  - 実行結果: `40:  --day-sun-cal:   rgba(220, 120, 120, 0.06);`（1件ヒット）
  - 判定: 合致

### A2. styles-light.css への追加

- **A2-1**. `--bg-surface-neutral: #F5F5F5;`
  - 実行結果: `13:    --bg-surface-neutral:     #F5F5F5;`（1件ヒット）
  - 判定: 合致
- **A2-2**. `--bg-surface-neutral-alt: #F2F2F1;`
  - 実行結果: `14:    --bg-surface-neutral-alt: #F2F2F1;`（1件ヒット）
  - 判定: 合致
- **A2-3〜A2-8**. `--day-sat / --day-sun / --day-sat-head / --day-sun-head / --day-sat-cal / --day-sun-cal`
  - 実行コマンド: `grep -nE "--day-sat:|--day-sun:|--day-sat-head:|--day-sun-head:|--day-sat-cal:|--day-sun-cal:" docs/ui-components/styles-light.css`
  - 実行結果:
    - `16:    --day-sat:       #EAF0F1;`
    - `17:    --day-sun:       #F3ECEE;`
    - `18:    --day-sat-head:  #BECAC5;`
    - `19:    --day-sun-head:  #D4BEBD;`
    - `20:    --day-sat-cal:   rgba(68, 166, 181, 0.06);`
    - `21:    --day-sun-cal:   rgba(220, 120, 120, 0.06);`
  - 期待値（6件・全値）と一致
  - 判定: 合致
- **A2-9**. Phase D15-1 由来コメントの付与
  - 実行コマンド: `grep -nE "D15-1|D15\.1|Surface Tier" docs/ui-components/styles-light.css`
  - 実行結果:
    - `12:    /* Phase D15-1 Surface Tier 拡張 — WS/OB データグリッド用 neutral surface */`
    - `15:    /* Phase D15-1 曜日別セル色（WS/OB 共通、color.day） */`
  - 判定: 合致（2件ヒット）

### A3. tokens.json への追加

- **A3-1**. `$version` = `"1.3.0"`
  - 実行結果: `4:  "$version": "1.3.0",`（1件ヒット）
  - `"1.2.0"` 残存チェック: grep 結果「No matches found」
  - 判定: 合致
- **A3-2**. `$lastUpdated` = `"2026-04-24"`
  - 実行結果: `5:  "$lastUpdated": "2026-04-24",`（1件ヒット）
  - 判定: 合致
- **A3-3**. `color.base` 直下 `bg-surface-neutral` / `bg-surface-neutral-alt`
  - 実行結果:
    - `13:      "bg-surface-neutral":     { "value": "#F5F5F5", "type": "color", "$description": "..." },`
    - `14:      "bg-surface-neutral-alt": { "value": "#F2F2F1", "type": "color", "$description": "..." },`
  - 判定: 合致（base 直下方式、`#F5F5F5` / `#F2F2F1` 両方含む）
- **A3-4**. `color.day` 配下 6キー
  - 実行結果（行 19–24）: `sat: #EAF0F1` / `sun: #F3ECEE` / `sat-head: #BECAC5` / `sun-head: #D4BEBD` / `sat-cal: rgba(68, 166, 181, 0.06)` / `sun-cal: rgba(220, 120, 120, 0.06)`
  - 6件ヒット、全 value が A1 の値と一致
  - 判定: 合致
- **A3-5**. `$description` 付与
  - 実行コマンド: `grep -nE "D15-1|Surface Tier|曜日" docs/ui-components/tokens.json`
  - 実行結果: 行 13, 14, 19, 20, 21, 22, 23, 24 の8箇所に `"Phase D15-1 ..."` 記述
  - 判定: 合致
- **A3-6**. JSON構文の有効性
  - 実行コマンド: `node -e "JSON.parse(require('fs').readFileSync('docs/ui-components/tokens.json','utf8'))" && echo OK`
  - 実行結果: `OK`
  - 判定: 合致

### A4. 3者同期の整合性

- **A4-1**. `--bg-surface-neutral` / `--bg-surface-neutral-alt` 値一致
  - co-tokens.css（31, 32）: `#F5F5F5` / `#F2F2F1`
  - styles-light.css（13, 14）: `#F5F5F5` / `#F2F2F1`
  - tokens.json（13, 14）: `#F5F5F5` / `#F2F2F1`
  - 判定: 3者完全一致
- **A4-2**. `--day-sat / sun / sat-head / sun-head` 値一致
  - 3者すべて `#EAF0F1` / `#F3ECEE` / `#BECAC5` / `#D4BEBD`
  - 判定: 3者完全一致
- **A4-3**. `--day-sat-cal` / `--day-sun-cal` rgba 値一致
  - 3者すべて `rgba(68, 166, 181, 0.06)` / `rgba(220, 120, 120, 0.06)`
  - 判定: 3者完全一致（空白差異なし）
- **A4-4**. スコープ逸脱（新規追加が上記8トークン以外）の有無
  - 実行コマンド: `git diff docs/ui-components/styles-light.css docs/ui-components/tokens.json docs/mockup/co-tokens.css | grep -E "^\+" | grep -iE "#[0-9a-f]{3,6}|rgba\("`
  - 実行結果: 新規追加された hex/rgba はすべて指定8値のみ（それ以外の色追加なし）
  - 判定: 合致

### A5. order-book.css :root 整理

- **A5-1**. `--day-*` 6定義の削除
  - 実行コマンド: `grep -nE "^\s*--day-sat:|^\s*--day-sun:|^\s*--day-sat-head:|^\s*--day-sun-head:|^\s*--day-sat-cal:|^\s*--day-sun-cal:" docs/mockup/order-book.css`
  - 実行結果: No matches found（0件）
  - 判定: 合致
- **A5-2**. `--base-grid: var(--bg-surface-neutral)` エイリアス
  - 実行結果: `8:    --base-grid:         var(--bg-surface-neutral);`（1件ヒット）
  - 判定: 合致
- **A5-3**. `--base-grid-alt: var(--bg-surface-neutral-alt)` エイリアス
  - 実行結果: `9:    --base-grid-alt:     var(--bg-surface-neutral-alt);`（1件ヒット）
  - 判定: 合致
- **A5-4**. `--base-grid` / `--base-grid-alt` にリテラル hex 直書きなし
  - 実行コマンド: `grep -nE "^\s*--base-grid(-alt)?:\s*#[0-9A-Fa-f]" docs/mockup/order-book.css`
  - 実行結果: No matches found（0件）
  - 判定: 合致
- **A5-5**. `--base-grid-total: #F0F4F4` 残留
  - 実行結果: `10:    --base-grid-total:   #F0F4F4;`（1件ヒット）
  - 判定: 合致
- **A5-6**. :root ヘッダコメント更新（D15-1 反映）
  - 実行コマンド: `grep -nE "D15-1|DS昇格|エイリアス" docs/mockup/order-book.css`（先頭付近）
  - 実行結果:
    - `7:    /* OB Grid背景 — Phase D15-1: --bg-surface-neutral/-alt 系へ DS 昇格済（エイリアス維持）、--base-grid-total は OB 独自残留 */`
    - `12:    /* 曜日オーバーレイ — Phase D15-1 で co-tokens.css に昇格済。OB 側の再定義は撤去し、DS からの継承に一本化 */`
  - 判定: 合致（先頭30行以内に2件）

---

## B. カラーコーディネーション（20点満点）

- **B1**. 新トークン8値の厳密一致
  - co-tokens.css（31, 32, 35-40）+ styles-light.css（13, 14, 16-21）の全定義行を目視、すべて指定8値と一致
  - 判定: 合致
- **B2**. 旧WS値 `#F7F5F2` の混入
  - 実行コマンド: `grep -n "#F7F5F2" docs/ui-components/styles-light.css docs/mockup/co-tokens.css docs/ui-components/tokens.json`
  - 実行結果: No matches found（0件）
  - 判定: 合致
- **B3**. Coastal Palette外の色混入
  - 実行コマンド: `git diff ... | grep -E "^\+" | grep -iE "#[0-9a-f]{3,6}|rgba\("`
  - 実行結果: 追加されたhex/rgbaは仕様8値のみ（A4-4と同一結果）
  - 判定: 合致
- **B4**. rgba 色相（teal vs rose）の取り違え検証
  - styles-light.css 20: `--day-sat-cal: rgba(68, 166, 181, 0.06)` ← teal 系（`--accent-primary: #44A6B5` と同色相）
  - styles-light.css 21: `--day-sun-cal: rgba(220, 120, 120, 0.06)` ← warm rose 系
  - 判定: 合致（取り違えなし）
- **B5**. コントラスト劣化チェック（値同値の再確認）
  - git diff で `order-book.css :root` から削除された `--day-*` 値と、新規 DS 追加値は1文字違わず一致（`#EAF0F1 / #F3ECEE / #BECAC5 / #D4BEBD / rgba(68,166,181,0.06) / rgba(220,120,120,0.06)`）
  - 判定: 合致

---

## E. 機能回帰（25点満点）

### E1. Computed 値一致（Playwright 実ブラウザ検証）

実行環境: http://localhost/order-management-system/docs/order-book.html（XAMPP）、キャッシュバスター付でstylesheet再読み込み後に `getComputedStyle()` で測定。

- **E1-1**. `var(--base-grid)` → 期待 `rgb(245, 245, 245)`
  - 実測: `rgb(245, 245, 245)`
  - 判定: 合致
- **E1-2**. `var(--day-sat-head)` → 期待 `rgb(190, 202, 197)`（#BECAC5）
  - 実測: `rgb(190, 202, 197)`
  - 判定: 合致
- **E1-3**. `var(--day-sun)` → 期待 `rgb(243, 236, 238)`（#F3ECEE）
  - 実測: `rgb(243, 236, 238)`
  - 判定: 合致
- **E1-4**. `var(--base-grid-total)` → 期待 `rgb(240, 244, 244)`（#F0F4F4）
  - 実測: `rgb(240, 244, 244)`
  - 判定: 合致
- 追加計測:
  - `var(--base-grid-alt)` → `rgb(242, 242, 241)` ✓ (#F2F2F1)
  - `var(--day-sat)` → `rgb(234, 240, 241)` ✓ (#EAF0F1)
  - `var(--day-sun-head)` → `rgb(212, 190, 189)` ✓ (#D4BEBD)
  - `var(--bg-surface-neutral)` → `rgb(245, 245, 245)` ✓
  - `var(--bg-surface-neutral-alt)` → `rgb(242, 242, 241)` ✓

### E2. 参照の健全性（grep 検証）

- **E2-1**. `order-book.css` 内の `var(--day-*)` / `var(--bg-surface-neutral*)` 参照
  - 実行コマンド: `grep -nE "var\(--(day-sat|day-sun|day-sat-head|day-sun-head|day-sat-cal|day-sun-cal|bg-surface-neutral|bg-surface-neutral-alt)" docs/mockup/order-book.css`
  - 実行結果（主要箇所）:
    - 8: `--base-grid: var(--bg-surface-neutral)`
    - 9: `--base-grid-alt: var(--bg-surface-neutral-alt)`
    - 335: `.tbl-grid__cell[data-dow="sat"] { background-color: var(--day-sat) ... }`
    - 336: `.tbl-grid__cell[data-dow="sun"] { background-color: var(--day-sun) ... }`
    - 337: `.md-ob-holiday { background-color: var(--day-sun) ... }`
    - 339: `.tbl-grid__cell[data-dow="sat-head"] { background-color: var(--day-sat-head) ... }`
    - 340: `.tbl-grid__cell[data-dow="sun-head"] { background-color: var(--day-sun-head) ... }`
    - 341: `.md-ob-holiday-head { background-color: var(--day-sun-head) ... }`
    - 1081: `.md-ob-cal-cell.md-ob-cal-sun { background: var(--day-sun-cal); }`
    - 1083: `.md-ob-cal-cell.md-ob-cal-sat { background: var(--day-sat-cal); }`
    - 1085: `.md-ob-cal-cell.md-ob-cal-holiday { background: var(--day-sun-cal); }`
  - 全参照は A1/A2 定義（co-tokens.css / styles-light.css）に対応
  - 判定: 合致
- **E2-2**. `--base-grid` の解決
  - Playwright Computed で `rgb(245, 245, 245)` に解決
  - 判定: 合致
- **E2-3**. `--base-grid-alt` の解決
  - Playwright Computed で `rgb(242, 242, 241)` に解決
  - 判定: 合致

### E3. ブラウザ目視 Before/After

- **E3-1〜E3-4**. D15-1 実装前コミットのスクリーンショット取得は実施せず、値同値の論理検証で代替。
  - IMの git diff で削除された `order-book.css :root` の `--day-*` / `--base-grid` / `--base-grid-alt` の値と、新規 DS 側に追加された値は1文字違わず完全同値（TD仕様書 B5 と同一確認）
  - E1 の Computed 実測値がすべて期待値（旧実装と同値）と一致
  - 判定: 論理的にビジュアル不変が実証。ピクセル差分比較ツールによる画像差分ゼロ確認は未実施（SC 判断を仰ぐ）

### E4. インタラクション

- **E4-1**. 主要インタラクション動作の実測
  - Playwright で order-book.html を読み込み完了、ページタイトル `受注簿`、console 0 errors / 1 warning（iframe sandbox 警告のみ、CSS 無関係）
  - 詳細なクリック／ドロップダウン操作は未実施
  - 判定: 部分検証（ロード成功・エラーなし）、詳細操作検証は未実施
- **E4-2**. コンソール CSS 関連の警告・エラー
  - Playwright `browser_console_messages`: Total 1 (Errors: 0, Warnings: 1)
  - 警告内容: `An iframe which has both allow-scripts and allow-same-origin for its sandbox attribute can escape its sandboxing. @ about:blank:0`（CSS無関係）
  - CSS 関連の `Invalid property value` / `unknown variable` 警告なし
  - 判定: 合致

---

## G. コード品質・保守性（15点満点）

- **G1**. `tokens.json` の `$description` 付与
  - 実行結果: 追加8トークンすべてに `$description` 付与（行 13, 14, 19-24）
  - 既存トークン（`bg-page` / `bg-surface` 等）は `$description` 非付与だったが、本フェーズ追加分には8件すべて付与
  - 判定: 合致（既存スタイルとの一貫性については「新規追加部は詳細に」という判断が適用されたと観察、客観判定は合致）
- **G2**. `co-tokens.css` のセクションコメント整合
  - 実行コマンド: `grep -nE "color\.(surface|day|base)" docs/mockup/co-tokens.css`
  - 実行結果（先頭コメントブロック）: `color.base / color.text / color.accent / color.semantic / color.category / color.day / color.chart / ...` の列挙に `color.day` が追記済
  - 追加で: `※ color.base には Phase D15-1 で Surface Tier 拡張 ... を追加。color.day は Phase D15-1 で新設`
  - 判定: 合致
- **G3**. 追加箇所のセクション区切り
  - co-tokens.css: Surface 系は既存 `--bg-surface-3` 直後（31-32）、Day 系は独立ブロックとして直後に配置（35-40）
  - styles-light.css: 同じく `--bg-surface-3` 直後にSurface系、直後にDay系（13-21）
  - 判定: 合致（論理グループ化）
- **G4**. 重複定義ゼロ
  - co-tokens.css: uniq -d 出力 = 空
  - styles-light.css: uniq -d 出力 = 空
  - order-book.css: 対象8トークンの定義 = 0件
  - 判定: 合致
- **G5**. `order-book.css` :root ヘッダコメント更新
  - 旧コメント「OB固有トークン（co-tokens.css 未収載。Phase M-A で個別対応予定）」 → `--base-grid` 近傍は D15-1 反映済（A5-6 参照）、残留の OB 固有α値ブロック（行14）にのみ旧文言が維持
  - 判定: 合致（対象箇所は更新、残留ブロックは適切に文脈保持）
- **G6**. 不要空行・TODO コメントなし
  - git diff 上で TODO / FIXME の追加なし
  - 3行以上連続した空行の追加なし
  - 判定: 合致
- **G7**. セムバ規約（MINOR bump 1.2.0 → 1.3.0）
  - 実行結果: `4:  "$version": "1.3.0",`
  - 純追加（既存トークン削除・値変更なし）であり MINOR bump として妥当
  - 判定: 合致
- **G8**. ファイル末尾整形
  - co-tokens.css: `0a 7d 0a`（`\n}\n`）末尾改行あり
  - styles-light.css: `0a 0d 0a`（`\n\r\n`）末尾改行あり（CRLF 混在）
  - tokens.json: `0a 7d 0a` 末尾改行あり
  - order-book.css: `0a 7d 0a` 末尾改行あり
  - 判定: 全ファイル末尾に改行存在。styles-light.css のみ末尾に CR が混在するがEOL整形はOS依存のため客観記録のみ

---

## 重大Claim検出

- **Claim 1**（OB ビジュアル変化）: Claim 該当 = NO
  - 理由: Playwright Computed 値がすべて旧同値に解決（E1-1〜E1-4）。`order-book.css` から削除された `--day-*` / `--base-grid` / `--base-grid-alt` の値と DS 側追加値が完全同値（git diff で確認）
- **Claim 2**（3者不一致）: Claim 該当 = NO
  - 理由: A4-1〜A4-3 で 3 ファイルの全 8 トークン値が一致
- **Claim 3**（Coastal Palette外の色混入）: Claim 該当 = NO
  - 理由: B3 / A4-4 で git diff 上の追加色はすべて仕様8値
- **Claim 4**（絵文字・Unicode記号使用）: Claim 該当 = NO
  - 理由: 追加コメント（`/* Phase D15-1 ... */`）に絵文字・Unicode 記号の使用なし。`※` は全角記号で CSS コメント文脈であり禁止対象外（TD 基準では ✓★⚠ 等が例示）
- **Claim 5**（コントラストAA未達）: Claim 該当 = NO
  - 理由: 値同値維持のため発生しえない（B5 確認）
- **Claim 6**（スコープ逸脱）: Claim 該当 = NO
  - 理由: 追加トークン = 指定8件のみ、既存 `--cell-base-*` / `--base-grid-total` / Dark テーマへの手入れなし（A4-4）
- **Claim 7**（`order-book.css` の `--day-*` リテラル残存）: Claim 該当 = NO
  - 理由: A5-1 で `--day-*` 6定義の削除を grep 0 件で確認
- **Claim 8**（`tokens.json` の `$version` / `$lastUpdated` 誤り）: Claim 該当 = NO
  - 理由: A3-1 / A3-2 で `1.3.0` / `2026-04-24` を確認

---

## 客観事実サマリー

- 合格項目: 51件 / 全54件
- 不合格項目: 0件
- 検証不能項目: 3件（E3-1〜E3-3 実コミット Before/After スクリーンショット画像差分。値同値の論理検証で代替実施済み）
- E4-1 は部分検証（ロード成功・CSS関連エラーなし）、操作面の詳細検証は未実施
- 重大Claim該当: 0件

---

## 次工程への申し送り（Scorer 向け参考情報）

- **A 軸（40点）**: A1-1〜A5-6 の全28項目で合致。3者同期が完全一致（A4）。
- **B 軸（20点）**: B1-B5 全項目で合致。`#F7F5F2` 等の WS 旧値混入ゼロ（B2）、仕様外色の追加ゼロ（B3）。
- **E 軸（25点）**: E1 の Computed 値がすべて期待値一致。E2 参照健全性 OK。E3 については「値同値のため論理的にビジュアル不変」を git diff / Computed で実証（画像差分比較は未実施 → SC で Before/After 画像取得が必要と判断するなら再検証依頼）。E4-2 の CSS エラー増加なしを実測確認。
- **G 軸（15点）**: G1-G8 全項目で合致。重複定義 0、セムバ 1.2.0→1.3.0 妥当、全ファイル末尾改行あり（styles-light.css の CRLF 混在は OS 由来、追加要因なし）。

重大Claim 0件 / 不合格 0件 / 検証不能 3件（画像差分のみ、論理代替あり）。
