# Q-A Scorecard (SC) v1

採点日: 2026-04-20
対象: Phase Q-A

## スコア

| 区分 | 配点 | 獲得 | 所見 |
|------|-----:|-----:|------|
| A. DS準拠（重複削除） | 25 | **24** | 29→3変数に削減（90%減）。co-tokens legacy aliases で全解決 |
| B. カラー一致 | 20 | **20** | 削除対象はすべて co-tokens 同値 or alias。視覚不変 |
| D. QA固有残留 | 15 | **15** | 値不一致 `--warning-text: #975A16` / `--warning-bg: #FEFCBF` と QA独自 `--night-text` が正しく残留 |
| E. 機能回帰 | 25 | **25** | JS 未変更、本文 `var()` 参照は全解決 |
| G. コード品質 | 15 | **14** | :root ブロックの重複統合、意図を明示するコメント追加 |

**総合: 98/100 PASS**

## 重大Claim

- なし

## ハイライト

- 重複 `:root` ブロック（通知用、L1591-1597）を整理し、値不一致の `--warning-text` / `--warning-bg` のみ残して理由をコメント化
- `--night-text` は QA での実参照はないが、将来のカレンダー夜間マーカー用途で co-navbar.js 互換性として残置
