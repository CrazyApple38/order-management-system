# マスタ管理・アカウント画面 モックアップ計画（F / G）

**作成**: 2026-07-11（ユーザー決定に基づく）
**ステータス**: F-0 完了（2026-07-11。各 Phase 着手時にユーザー確認）
**対象**: F=マスタ管理画面（統合1画面） / G=アカウント画面 + QA整合確認
**実装者**: Sonnet 5 / Codex 等の複数エージェント（本計画書がズレ防止の単一情報源）

---

## 1. 目的と確定事項

Phase 2 モックアップとして F/G の2画面を新DS準拠で新規作成する。要件の正本は
`01_要件定義.md` **§3.21（マスタ管理・統合1画面）/ §3.22（アカウント画面）**、DB の正本は
`03_データベース設計.md` **§3.31 user_profiles / §3.32 user_preferences**（2026-07-11 追補）。

| 決定（2026-07-11 ユーザー確定） | 内容 |
| --- | --- |
| マスタ管理の構成 | **統合1画面＋左レール種別切替**。マスタごとの個別画面・旧ハブ（リンクカード集）は作らない |
| アカウント画面の構成 | **個人設定タブ＋ユーザー管理タブ（権限者のみ）の1画面** |
| 実装の意味 | モックアップ実装（Phase 2 継続）。本実装（Next.js+Supabase）は Phase 4 以降で別計画 |
| QA の扱い | モック・要件とも既存のため**整合確認のみ**（§6） |

## 2. AI実装ガイドライン（全エージェント共通・厳守）

どのエージェントが実装しても同一の結果になるための規範。**着手前に必ず全読すること。**

### 2.1 必読ドキュメント（順番に）

1. `docs/SHARED-MEMORY.md` 全読（特に「触らないでほしいもの」「命名規約」）
2. `docs/HANDOFF.md` 全読
3. 本計画書の該当 Phase 節
4. `docs/design-system/README.md` + `04_ai-implementation-guide.md`（DS実装手順の正本）
5. **`docs/design-system/03_screen-application.md` §4「F マスタ管理」「G アカウント画面」節（画面別設計の正本・2026-07-11 追記）**
6. `docs/design-system/02_components.md` の使用コンポーネント節（A-01/A-05/B系/C系）

### 2.2 実装規範（新画面共通）

- **CSS読込チェーン（この順序で固定)**: `ds-tokens.css` → `ds-legacy-aliases.css` → `ds-components.css` → 画面別CSS（F=`ma-ds.css` / G=`ac-ds.css`・新設）。**`co-tokens.css` は読み込まない**（同名別値トークンのため併載禁止）
- **骨格**: 既存4画面と同一の `app > toolbar / workspace(rail｜main-card｜prop｜panel-rail)`。SL/OB/WS/LA の HTML を骨格の見本とする
- **フォーム**: DS A-05 体系（`.field`/`.field-label`/`.input`）のみ。**旧 `.md-fi-*` / `md-ob-*` / co-buttons 系クラスの新規使用は禁止**（廃止済み）
- **ボタン**: DS A-01（`.btn` / `.btn-primary` / §H 拡張バリアント）のみ
- **通知**: 統合ベル1個（`data-bell="all"`）+ 共通 `.cn-card`。発火は `coNotifyPanel.addItem('all', {...})`、マスタ変更は `domain: 'master'`・**targetDate なし**（「対象日なし」グループに入るのが正）。旧ベルID・旧 `.cn-panel` は使用禁止
- **script 順序**: `co-mock-store.js` → `co-navbar.js` の順を維持。ダミーデータは固定文字列を書かず `co-mock-store.js` / 実DOM の実値から動的取得
- **トークン値**: `ds-tokens.css` / `ds-components.css` / `ds-legacy-aliases.css` の**値の変更・追加はユーザー承認必須**。新色・新寸法を発明しない
- **キャッシュバスター**: JS/CSS を編集したら `?v=N` を必ず更新
- **スクリーンショット**: プロジェクトルート `screenshots/` へ（`f1-xxx.png` 等 Phase 接頭辞）

### 2.3 完了条件（全 Phase 共通・省略不可）

1. `node --check` 構文検査 OK（対象JS）
2. `node scripts/design-audit/ds-audit.js` **NG=0**（WARN 増加なし）
3. localhost HTTP 200 + コンソールエラー0（Playwright）
4. 既存6ページ（OB/SL/WS/LA/QA/admin-notify）の回帰なし（読込チェーン・共通JSに触れた場合は必須）
5. スクリーンショット保存 + **1 Phase = 1 コミット** + `docs/HANDOFF.md` 上書き更新（本体作業と同一コミット）

### 2.4 判断に迷ったら

- 計画書・要件・DS 正本に無い判断（配色・配置・命名・データ構造）は**実装せずユーザーへ確認**（AskUserQuestion）
- 本計画外の「ついで実装」は凍結（プロジェクト共通ルール）

## 3. F: マスタ管理画面（統合1画面）

**要件正本**: `01_要件定義.md` §3.21。仮ファイル名 `docs/master-management.html`（**F-1 着手時に命名をユーザー確認**）。

| Phase | 内容 | 完了条件（§2.3 に加えて） |
| --- | --- | --- |
| F-0 データ準備 | マスタ種別ごとのモックデータ棚卸し。既存 `demo-data.js` / `co-mock-store.js` / `mock-assignments-data.js` から参照できるもの（社員/契約先/現場/車両/ETC/協力業者/GC）はそれを正とし、無いもの（資格種別/祝日/ペナルティ/定型文/組織階層）のみ新規シード作成 | 種別×データソース対応表を本計画書へ追記 |
| F-1 骨格+パターン確立 | 骨格+左レール種別ナビ（§3.21.3 の8カテゴリ）+ **契約先マスタ1種別**で共通CRUDパターン（中央一覧+右プロパティ編集+論理削除+検索+有効/無効seg）を確立 | 契約先の一覧/追加/編集/無効化が右プロパティで完結・パターンを本計画書 §3.1 として記録 |
| F-2 単純マスタ横展開 | F-1 パターンを GC/祝日/ペナルティ/定型文2種/資格種別/協力業者/車両/ETC へ複製展開（列定義のみ種別ごと差替え） | 全種別で一覧+CRUD動作・種別切替で状態が混線しない |
| F-3 階層マスタ | 組織階層種別+組織ノード / 現場（親子） / 区分・バッジを中央ツリー表示で実装（骨格は共通のまま） | ツリー展開/折りたたみ・親子追加・階層移動 |
| F-4 社員マスタ | 社員一覧+右プロパティ編集（所属GC/組織ノード/資格サブリスト/配置制約） | 資格・制約の追加削除が右プロパティ内で完結 |
| F-5 通知+検証 | マスタ変更→`domain: 'master'` 通知発火（対象日なし）。他画面のベルに反映。全体回帰+スクショ | 通知が「対象日なし」グループに入る・センターの種別軸に表示される |

#### F-0 データソース対応表（2026-07-11 棚卸し済み）

| カテゴリ | マスタ | モックでの正本 / 初期値 | Fでの扱い |
| --- | --- | --- | --- |
| 組織 | グループ会社 | `demo-data.js` の `groupCompaniesData` | 既存配列を参照。会社コードは SHARED-MEMORY のマッピングを維持 |
| 組織 | 組織階層種別 | `demo-data.js` の `orgLevelTypesData` | 既存オブジェクトを参照（当初の「新規シード」想定を棚卸しで訂正） |
| 組織 | 組織ノード | `demo-data.js` の `orgUnitsData` | 既存ツリーを参照。`departmentsData` は後方互換の派生配列なので正本にしない |
| 人事 | 社員 | `mock-employees-data.js` の `OmsMockEmployeesData.createEmployees()`（`demo-data.js` の `employeesData` 経由） | 既存25名を参照。社員コード・資格・配置制約は未保持のため F-4 で補完 |
| 人事 | 資格検定 | モック正本なし。DB §3.4 `license_types` の初期データ | F-2 用の新規シードが必要 |
| 現場・取引 | 契約先 | `demo-data.js` の `companiesData`（初期値=`defaultCompaniesData`） | F-1 の共通CRUDパターン確立に使用 |
| 現場・取引 | 現場 | `demo-data.js` の `sitesData`（初期値=`defaultSitesData`） | F-3 のツリーに使用。`mock-assignments-data.js#createSites()` はWS/LA配置用の6現場であり、マスタ正本にしない |
| 現場・取引 | 区分・バッジ | `order-book.js` の `badgeDefinitions` | 既存定義を初期値に使用。F-3 で共有ソース化する場合はOBへの横断影響があるため着手前に確認 |
| 協力業者 | 協力業者 | `mock-assignments-data.js` の `OmsMockAssignmentsData.createSupportPartners()` | `preset-unified` はUI専用なのでCRUD対象外。残る5社を参照 |
| 書類テンプレ | 料金特記定型文 | モック正本なし。DB §3.26 `price_note_templates` の初期データ | F-2 用の新規シードが必要 |
| 書類テンプレ | その他特記定型文 | モック正本なし。DB §3.27 `special_note_templates` の初期データ | F-2 用の新規シードが必要 |
| 書類テンプレ | 注文書テンプレート | モック・DBとも専用マスタなし | 将来項目。今回のCRUD対象外 |
| 車両・資機材 | 車両 | `mock-vehicles-data.js` の `OmsMockVehiclesData.createVehicles()`（`demo-data.js` の `vehiclesData` 経由） | 既存15台を参照 |
| 車両・資機材 | ETCカード | `mock-vehicles-data.js` の `OmsMockVehiclesData.createEtcCards()`（`demo-data.js` の `etcCardsData` 経由） | 既存9枚を参照 |
| 運用・制度 | 祝日 | 単一のモック正本なし。LA=`holidays-jp API`+キャッシュ+2025〜2027フォールバック、OB/WS=画面別固定値。DB §3.18 に2026年初期データ | F-2 用の新規シードが必要。画面別祝日の一本化は横断変更なので本Phaseでは行わない |
| 運用・制度 | ペナルティコード | モック正本なし。DB §3.12 `penalty_codes` の初期データ | F-2 用の新規シードが必要 |
| システム | ユーザー・権限 | F側のデータ正本なし。G=`user_profiles` 相当 | FはG画面への導線のみ。CRUDはG-2 |
| システム | カラープリセット | `screen-layout.js` の `colorPresets_v2_light`（SLローカル）/ DB §3.19 `user_color_presets` | Fで重複管理せず、G-1の個人設定から既存管理への導線を提供 |
| システム | ユーザー契約先紐付 | モック正本なし。DB §3.20 `user_company_assignments` | Fで重複管理せず、G-2でモック正本を作成してQAと共有 |

`co-mock-store.js` はOB/SL/WS/LAの保存済み画面状態を保持するストアであり、マスタ初期値の正本ではない。FのCRUD状態を同ストアへ追加すると既存構造へ影響するため、F-1以降の保存方式は着手時に別途確認する。

## 4. G: アカウント画面

**要件正本**: `01_要件定義.md` §3.22。仮ファイル名 `docs/account-settings.html`（**G-1 着手時に命名をユーザー確認**）。
モックでの設定保存は `localStorage`（`mock.oms.state.v1` と別キー・`user_preferences` 相当の key-value 形。キーは DB §3.32 の既知キー表に合わせる）。

| Phase | 内容 | 完了条件（§2.3 に加えて） |
| --- | --- | --- |
| G-1 骨格+個人設定タブ | 共通ナビ `.menu-user` メニューに「アカウント設定」を追加し遷移。個人設定タブ（プロフィール表示/GCフィルタ初期値/通知既定スコープ/画面表示非表示/密度既定/QA・カラープリセットへの導線） | 設定変更が localStorage に保存され再読込で復元される |
| G-2 ユーザー管理タブ | 権限者のみ表示。ユーザー一覧+ロール割当+社員紐付け+担当契約先紐付け（`user_company_assignments` 相当のモック） | ロールを切替えるとタブ表示権限が変わるデモが成立 |
| G-3 各画面への反映 | ログイン時復元のシミュレーション: GCフィルタ初期値→OB/SL/WS/LA 初期選択、画面表示非表示→共通ナビのメニュー増減、通知既定スコープ→通知パネルの既定絞り込み | 4画面+ナビが設定に追従・設定リセットで既定へ戻る |

※ G-3 は共通 `co-navbar.js` / 各画面 JS に触れるため、**既存6ページの回帰確認が必須**（§2.3-4）。

## 5. 実施順序と着手条件

- 推奨順: **F-0 → F-1 → G-1**（F-1 と G-1 でパターンが固まってから F-2〜/G-2〜を並行可）
- 各 Phase 着手時にユーザー確認（既存運用踏襲）。F と G の間に依存はないが、`.menu-user` メニュー改修（G-1）は共有ナビのため単独 Phase として扱う
- Phase Gate: 本計画はモックアップ作成であり、Phase 3（仕様書）進行には「モックアップ完了」宣言が必要

## 6. QA（クイックアクセス）整合確認

QA はモック・要件（§3.12）とも既存のため新規作業はなし。G-2 完了時に以下を確認する:

- [ ] QA の担当契約先カード一覧が、G-2 のユーザー契約先紐付けモックと同じデータソースを参照する（固定文字列の重複定義をしない）
- [ ] QA非表示契約先/現場（`qa_user_hidden_*` 相当）への導線が G-1 個人設定タブから通じる

## 7. 進捗

| Phase | 状態 | 開始 | 完了 | メモ |
| --- | --- | --- | --- | --- |
| F-0 | 完了 | 2026-07-11 | 2026-07-11 | §3 F-0 データソース対応表を追記 |
| F-1 | 未着手 | — | — | 着手時: ファイル名確認 |
| F-2 | 未着手 | — | — | |
| F-3 | 未着手 | — | — | |
| F-4 | 未着手 | — | — | |
| F-5 | 未着手 | — | — | |
| G-1 | 未着手 | — | — | 着手時: ファイル名確認・.menu-user 改修は共有ナビ |
| G-2 | 未着手 | — | — | |
| G-3 | 未着手 | — | — | 共通JS改修=全画面回帰必須 |
| QA整合 | 未着手 | — | — | G-2 完了時 |
