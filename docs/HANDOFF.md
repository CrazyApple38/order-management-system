# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Codex (GPT-5)
- **日付**: 2026-07-11
- **コミット**: 直前HEAD `775f63b`

## 直前にやったこと
- **G-3（各画面への個人設定反映）完了**。共有設定ローダーを追加し、GC初期値・ナビ表示・通知既定スコープ・densityを反映。
- 画面別GC実行時選択を専用キーに保持し、設定保存/リセット時に初期値へ戻る契約を確立。
- 通知カードへ「担当・所属 / 全社」を追加。モックの関与判定は `affects[]` / `primaryPage` を使用。
- 4画面設定追従、設定リセット、既存6ページ、HTTP 200、`ds-audit NG=0 WARN=0` を確認。

## 次にやるべきこと
1. 次の実装Phaseは **F-2（単純マスタ横展開）**。着手前にユーザー確認する。
2. QA整合の残りはG-1からQA非表示契約先/現場管理への直接導線。画面間整合変更なので別途承認後に実施する。
3. F-2は計画書 §3 の対象9種をF-1 CRUDパターンへ展開し、種別間の状態混線を検証する。
4. Phase 3へは「モックアップ完了」宣言があるまで進まない。

## 今だけの申し送り
- 合格スクリーンショットは `screenshots/g3-account-preferences-applied.png`。Chrome拡張由来の既知エラーを除き、対象JSのwarning/errorは0。
