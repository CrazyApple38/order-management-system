# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告・会社マッピング・命名規約は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Codex (GPT-5)
- **日付**: 2026-07-11
- **コミット**: 作業開始時 HEAD `2afc10e`（R-5は未コミット）

## 直前にやったこと（最新のみ）
- **R-5 DB設計追補完了**: `docs/03_データベース設計.md` §3.15 に `change_notifications` / `route_participants` を定義。
- domain導出カテゴリ、`target_date`+範囲終端、画面別target、差分、復元スナップショット、主要索引を設計。
- `notification_logs` をLINE/メール外部送信ログへ責務限定し、ER概要・RLS・更新履歴を同期。
- スレッド/確認状態は初期DBに追加せず、将来の別テーブル拡張メモに限定。

## 次にやるべきこと
1. `mockup-refactor-plan.md` の最終工程 **R-6 結合検証**へ進む。
2. 新カテゴリ/対象日/配置サブタグの通知発火マトリクスを再検証する。
3. OB/SL/WS/LA/QAのクロスジャンプと通知センター導線をlocalhostで一括確認する。

## 今だけの申し送り
- ローカルに `psql` が無いためDDL実行検証は未実施。静的構造・現行Mockup語彙・Markdown差分は検証済み。
- アプリ内ブラウザは前回まで iab 接続先なし。R-6開始時に再確認する。
- Phase 2継続中。「モックアップ完了」宣言なしのため Phase 3へ進めない。
