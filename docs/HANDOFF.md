# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Codex (GPT-5)
- **日付**: 2026-07-11
- **コミット**: 直前HEAD `7ee1357`

## 直前にやったこと
- **G-2（ユーザー管理タブ）完了**。管理者のみ表示される一覧+右プロパティで、新規作成・編集・無効化・検索・状態絞り込みを実装。
- ロール、社員/所属GC、担当契約先を `mock.oms.account.users.v1` に保存・復元。閲覧権限変更で管理タブの表示が切り替わる。
- `mock-account-data.js` にQA契約先シードとユーザ契約先紐付けを集約。QAは田中の担当契約先だけを表示する。
- 保存・再読込復元・権限表示・新規無効ユーザー・QA連携・HTTP 200・`ds-audit NG=0 WARN=0` を確認。

## 次にやるべきこと
1. 次は **G-3（各画面への個人設定反映）** または **F-2（単純マスタ横展開）**。着手Phaseをユーザーへ確認する。
2. G-3は `co-navbar.js` とOB/SL/WS/LAに触れるため、既存6画面の回帰確認が必須。
3. QA整合の残りはG-1からQA非表示契約先/現場管理への導線確認。
4. Phase 3へは「モックアップ完了」宣言があるまで進まない。

## 今だけの申し送り
- 合格スクリーンショットは `screenshots/g2-user-management.png`。Chrome拡張由来エラーを除き、G-2対象JSのwarning/errorは0。
