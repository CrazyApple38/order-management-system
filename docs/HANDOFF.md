# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Codex (GPT-5)
- **日付**: 2026-07-12
- **コミット**: 直前HEAD `d813699`

## 直前にやったこと
- **F-2（単純マスタ横展開）完了**。F-1 CRUDを定義駆動化し、対象9種の一覧・追加・編集・論理削除を実装。
- `mock.oms.master.v1` をversion 2へ移行し、`datasets[masterId]` で種別状態を分離。F-1契約先データの移行にも対応。
- 全9種のCRUD実操作、種別切替後の件数・列分離、HTTP 200、`ds-audit NG=0 WARN=0` を確認。

## 次にやるべきこと
1. 次の実装Phase候補は **F-3（階層マスタ）**。着手前にユーザー確認する。
2. F-3は組織階層種別+組織ノード / 現場（親子） / 区分・バッジを中央ツリー表示へ展開する。
3. 区分・バッジを共有ソース化する場合はOBへの横断影響があるため、実装前に別途確認する。
4. QA非表示契約先/現場管理への直接導線も、画面間整合変更として別途承認が必要。
5. Phase 3へは「モックアップ完了」宣言があるまで進まない。

## 今だけの申し送り
- 合格スクリーンショットは `screenshots/f2-simple-masters.png`。Chrome拡張由来の既知エラーを除き、対象JSのwarning/errorは0。
