# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告・会社マッピング・命名規約は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Codex (GPT-5)
- **日付**: 2026-07-11
- **コミット**: 作業開始時 HEAD `72a62b2`（R-4は未コミット）

## 直前にやったこと（最新のみ）
- **R-4 通知センター改修完了**: `change-notification-center-mockup.html` からスレッド/確認依頼/解消記録 UI を撤去。
- スレッド依存の要対応軸・状態フィルタを撤去し、詳細を変更内容+情報経路+現場画面導線へ簡素化。
- 日別軸を `targetDate` 基準へ変更し、配置区分4種のフィルタを追加。
- JS構文、旧スレッド参照0、4区分網羅、HTTP 200、`ds-audit NG=0 WARN=0` を確認。

## 次にやるべきこと
1. アプリ内ブラウザが利用可能なら R-4 の日別対象日表示と配置区分フィルタを localhost で操作確認する。
2. `mockup-refactor-plan.md` の次工程 **R-5 DB設計追補**へ進む。
3. R-5では `change_notifications` / `route_participants` を `docs/03_データベース設計.md` へ追補し、スレッド系は将来拡張メモに限定する。

## 今だけの申し送り
- アプリ内ブラウザは Chrome拡張のみで iab 接続先なし。手順規約に従い別バックエンドへ切り替えていない。
- Phase 2継続中。「モックアップ完了」宣言なしのため Phase 3へ進めない。
