# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Codex (GPT-5)
- **日付**: 2026-07-11
- **コミット**: 作業開始時 HEAD `8acf1bd`（R-6静的検証成果は未コミット）

## 直前にやったこと
- R-6通知発火マトリクスを新カテゴリ/対象日/配置4サブタグで静的再検証し、60項目合格。
- DB `change_notifications.target_date` を「対象日なし」通知に限りNULL許容へ補正。
- 通知センターの「現場画面で開く」をOB/SL/WS/LAへの同タブ遷移へ接続。
- JS構文、localhost 6画面HTTP 200、`ds-audit NG=0 WARN=0`、diff検査に合格。

## 次にやるべきこと
1. アプリ内ブラウザ接続後、OB/SL/WS/LAのクロスジャンプを一括実操作する。
2. 各画面の通知カードからセンターへ遷移し、センターから実画面へ戻る導線を確認する。
3. runtime合格後、スクリーンショットを`screenshots/`へ保存してR-6を完了扱いにする。

## 今だけの申し送り
- アプリ内ブラウザは今回も`iab`接続先なし。別ブラウザ制御には切り替えていない。
- `docs/doc-export-smoke-test.{md,docx,pdf}`は先行作業の未追跡成果物。今回変更・コミットしない。
- Phase 2継続中。「モックアップ完了」宣言なしのためPhase 3へ進めない。
