# 引き継ぎノート（10〜30行・毎回上書き）

永続事実・制約・構造的変更の警告は `docs/SHARED-MEMORY.md` を参照。

## 最終更新
- **更新者**: Claude Code (Fable 5)
- **日付**: 2026-07-11
- **コミット**: 作業開始時 HEAD `24d9457`

## 直前にやったこと
- Codex 実装の R-4/R-5/R-6 を計画書・確定事項と静的照合レビュー。ズレ・実装抜けなし（domain語彙=DB CHECK一致・スレッド残骸0・センター相対パス正・verify 60項目/ds-audit NG=0 再現）。
- R-6 runtime 検証を Playwright で完遂: センター描画/サブタグフィルタ/センター⇄実画面導線/SL→OB→WS→LA クロスジャンプ一巡/cnJump除去/スポットライト発火/コンソールエラー0。スクショ `screenshots/r6-*.png` 7枚。
- mockup-refactor-plan を R-0〜R-6 全完了へ更新。**リファクタ統合計画はこれで完了**。

## 次にやるべきこと
1. ユーザーへ R 系完了を報告済み。次テーマはユーザー指示待ち（候補: 残存モックアップ D 経理 / E 休日申請の新DS準拠作成、Phase 2.5 検証、admin-notify DS化）。
2. Phase 3（仕様書）へは「モックアップ完了」宣言があるまで進まない。

## 今だけの申し送り
- `docs/doc-export-smoke-test.{md,docx,pdf}` は先行作業の未追跡成果物。変更・コミットしない。
- OB の cn:jump 着地で右プロパティ詳細ペインが「未選択」なのは仕様（詳細ドックは編集時のみ。着地行は連携・所在/変更履歴モードへ反映される）。
