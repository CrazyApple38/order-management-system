# データベース設計書

## 1. 概要

### 1.1 データベース

- **DBMS**: PostgreSQL（Supabase）
- **文字コード**: UTF-8

### 1.2 設計方針

- 正規化を基本としつつ、パフォーマンスを考慮
- 拡張性を確保（将来の機能追加に対応）
- 履歴管理のためのタイムスタンプを全テーブルに設置
- フロントエンド非依存: React Webアプリ（先行開発）およびExcel VBA UI（後続開発）の両方からSupabase REST APIで共通利用

---

## 2. ER図（概要）

```
┌─────────────────┐
│ group_companies │ グループ会社マスタ
└────────┬────────┘
         │
         │ 1:N
         ▼
┌─────────────────┐
│    companies    │ 契約先マスタ（元請け先）
└────────┬────────┘
         │
         │ 1:N
         ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│     sites       │◄─────│ site_categories │      │ group_companies │
│   (現場)        │ N:1  └────────┬────────┘      └─────────────────┘
└────────┬────────┘              │▲ N:1                     │
         │                  1:N  ││                          │
         │               ┌──────▼────────┐                  │
         │               │category_badges│ 区分バッジ        │
         │               │ (子バッジ)    │                  │
         │               └───────────────┘                  │
         └───────────────────────┼──────────────────────────┘
                                 │          sites.group_company_id (FK)
         │
         ├────────────────────────────────────┐
         │ 1:N                                │ 1:N
         ▼                                    ▼
┌──────────────────┐                  ┌─────────────────┐
│daily_site_orders │                  │ site_sub_items  │ 現場子項目
│  (日毎受注)      │                  │ (工事名/作業等) │
│  ※営業が入力    │                  └─────────────────┘
└────────┬─────────┘
         │
         ├────────────────────────────────────┐
         │ 1:1                                │ 1:N
         ▼                                    ▼
┌──────────────────┐    ┌──────────────────────┐    ┌──────────────────────────┐
│daily_assignments │    │order_change_history  │    │daily_order_sub_items     │
│  (日毎配置)      │    │  ※ロールバック対応   │    │  (日毎受注業務詳細)      │
│  ※配置担当管理  │    └──────────────────────┘    │  ※動的ラベル+値ペア     │
└────────┬─────────┘                                └──────────────────────────┘
         │
         │              ┌─────────────────┐     ┌──────────────────┐
         ├─────────────►│   employees     │◄───►│employee_conflicts│
         │              │   (社員)        │     │  (配置制約)      │
         │              └────────┬────────┘     └──────────────────┘
         │                       │▲
         │                       ││ N:1
         │                       │├──────────────┐
         │                       │               │
         │                  1:N  │  ┌───────────────────────┐
         │                       ▼  │ organization_units    │ 組織ノード
         │              ┌─────────────────┐│  (自己参照ツリー)     │
         │              │employee_licenses│ └───────────┬───────────┘
         │              └─────────────────┘             │▲ parent_id
         │                                              ││
         │                                    ┌─────────────────────┐
         │                                    │  org_level_types    │ 階層種別定義
         │                                    └─────────────────────┘
         │
         ▼
┌─────────────────┐      ┌─────────────────┐
│assignment_details│     │transport_relations│ 送迎関係
│ (配置詳細)      │      └─────────────────┘
└─────────────────┘

※ グループ会社（東央警備、Nikkeiホールディングス、全日本エンタープライズ等）は group_companies で管理
※ 契約先（元請け先）は companies で管理、sites.group_company_id で現場単位にグループ会社と紐付け
※ 同じ元請け先が複数のグループ会社と契約可能
※ 連続勤務は配置データから自動検知（テーブルなし）
※ 社員（employees）はgroup_company_idでグループ会社に所属、org_unit_idで組織ノードに所属。休日申請管理のフィルタ等に使用
※ 組織階層はorg_level_types（階層種別定義）+ organization_units（自己参照ツリー）で管理。グループ会社ごとに階層名を自由定義（例: 東央警備は「課」のみ、全日本エンタープライズは「拠点→課」）。最大5階層
※ 社員はツリーの任意の階層に所属可能（営業所直属の社員も、課所属の社員も可）
※ 日毎受注（営業入力）と日毎配置（配置担当管理）を分離
※ 受注変更履歴で人数変更・キャンセルの追跡とロールバックが可能
※ 現場地図はdaily_site_orders.maps（JSONB配列）で複数エントリ対応
※ 車両（vehicles）はgroup_company_idでグループ会社に所属。サイドバーのGCフィルタ・GC別グループ表示に使用
※ 車両配置はdaily_assignments.vehicle_idsで管理
※ ETCカード配置はdaily_assignments.etc_card_idsで管理
※ 車両・ETCは同一シフト帯（昼/夜）に1現場のみ配置可能
※ バッジ: 親バッジ=区分（site_categories）、子バッジ・孫バッジ=category_badges（自己参照FK）で定義。日毎受注のbadge_child_idsで選択済み子バッジID、badge_gc_mapで選択済み孫バッジIDを保持

┌─────────────────┐
│    vehicles     │ 車両マスタ
│   (車両)        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│maintenance_schedules│ メンテナンス予定
└─────────────────┘

┌─────────────────┐
│   etc_cards     │ ETCカードマスタ
│  (ETCカード)    │
└─────────────────┘

┌─────────────────┐
│ penalty_records │ ペナルティ記録
└─────────────────┘

┌─────────────────┐
│   holidays      │ 祝日マスタ
└─────────────────┘

┌─────────────────┐
│ change_history  │ 変更履歴
└─────────────────┘

┌──────────────────────────────┐
│ employee_day_off_requests   │ 休日申請
└──────────────────────────────┘
```

---

## 3. テーブル定義

### 3.0 group_companies（グループ会社マスタ）

自社グループ会社（東央警備、Nikkeiホールディングス等）を管理するマスタ。
契約先（元請け先）とは異なり、自社側の所属会社を表す。
各現場（sites）はいずれかのグループ会社に紐付く。
**注文書の「甲（委託者）」情報ソースも兼ねる**（住所・代表者役職・代表者名・電話）。

| カラム名             | 型           | NULL | 説明                                           |
| -------------------- | ------------ | ---- | ---------------------------------------------- |
| id                   | UUID         | NO   | 主キー                                         |
| code                 | VARCHAR(20)  | NO   | グループ会社コード（ユニーク）                 |
| name                 | VARCHAR(100) | NO   | グループ会社名                                 |
| short_name           | VARCHAR(20)  | YES  | 表示用略称（例: 東央, Nikkei）                 |
| badge_color          | VARCHAR(7)   | YES  | ボーダー色（例: #93c5fd）                      |
| badge_text_color     | VARCHAR(7)   | YES  | バッジ文字色（例: #ffffff）                    |
| postal_code          | VARCHAR(8)   | YES  | 郵便番号（例: 780-0945）※注文書甲情報          |
| address              | TEXT         | YES  | 住所（都道府県から記載）※注文書甲情報          |
| representative_title | VARCHAR(50)  | YES  | 代表者役職（例: 代表取締役）※注文書甲情報      |
| representative_name  | VARCHAR(50)  | YES  | 代表者名（例: 藤田剛志）※注文書甲情報          |
| phone                | VARCHAR(20)  | YES  | 代表電話番号（例: 088-856-6666）※注文書甲情報  |
| sort_order           | INTEGER      | YES  | 表示順                                         |
| is_active            | BOOLEAN      | NO   | 有効フラグ（デフォルト: true）                 |
| created_at           | TIMESTAMPTZ  | NO   | 作成日時                                       |
| updated_at           | TIMESTAMPTZ  | NO   | 更新日時                                       |

```sql
CREATE TABLE group_companies (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    short_name VARCHAR(20),
    badge_color VARCHAR(7) DEFAULT '#718096',
    badge_text_color VARCHAR(7) DEFAULT '#ffffff',
    postal_code VARCHAR(8),
    address TEXT,
    representative_title VARCHAR(50),
    representative_name VARCHAR(50),
    phone VARCHAR(20),
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 初期データ
INSERT INTO group_companies (code, name, short_name, badge_color, badge_text_color, sort_order) VALUES
    ('TOUO', '東央警備', '東央', '#93c5fd', '#ffffff', 1),
    ('NIKKEI', 'Nikkeiホールディングス', 'Nikkei', '#fca5a5', '#ffffff', 2),
    ('ZENNIHON', '全日本エンタープライズ', 'AJE', '#86efac', '#ffffff', 3);
```

---

### 3.1 companies（契約先マスタ）

契約先（元請け先）マスタ。`name` はUI表示用の**略称**（例: `(株)○○`）、`formal_name` は**正式名称**（例: `株式会社○○`）で、注文書の「丙（発注者）」記載時に使用。受注簿・週間予定表等では `name` を表示し、注文書発行時にのみ `formal_name` を参照する。

| カラム名     | 型           | NULL | 説明                                                       |
| ------------ | ------------ | ---- | ---------------------------------------------------------- |
| id           | UUID         | NO   | 主キー                                                     |
| code         | VARCHAR(20)  | NO   | 契約先コード（ユニーク）                                   |
| name         | VARCHAR(100) | NO   | 契約先名（UI表示用略称）                                   |
| formal_name  | VARCHAR(200) | NO   | 正式名称（注文書用、括弧株等を展開）                       |
| notes        | TEXT         | YES  | 備考                                                       |
| sort_order   | INTEGER      | YES  | 表示順                                                     |
| is_active    | BOOLEAN      | NO   | 有効フラグ（デフォルト: true）                             |
| created_at   | TIMESTAMPTZ  | NO   | 作成日時                                                   |
| updated_at   | TIMESTAMPTZ  | NO   | 更新日時                                                   |

```sql
CREATE TABLE companies (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    formal_name VARCHAR(200) NOT NULL,
    notes TEXT,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 曖昧検索用（Quick Access新規契約先登録時の重複防止バリデーション）
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE INDEX idx_companies_name_trgm ON companies USING GIN (name gin_trgm_ops);
```

---

### 3.2 site_categories（区分マスタ）

現場をカテゴリ分けするための区分マスタ。

| カラム名   | 型          | NULL | 説明                   |
| ---------- | ----------- | ---- | ---------------------- |
| id         | UUID        | NO   | 主キー                 |
| code       | VARCHAR(20) | NO   | 区分コード（ユニーク） |
| name       | VARCHAR(50) | NO   | 区分名                 |
| sort_order | INTEGER     | YES  | 表示順                 |
| is_active  | BOOLEAN     | NO   | 有効フラグ             |
| created_at | TIMESTAMPTZ | NO   | 作成日時               |
| updated_at | TIMESTAMPTZ | NO   | 更新日時               |

```sql
CREATE TABLE site_categories (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) NOT NULL UNIQUE,
    name VARCHAR(50) NOT NULL,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 初期データ
INSERT INTO site_categories (code, name, sort_order) VALUES
    ('FACILITY', '施設', 1),
    ('EVENT', 'イベント', 2),
    ('TRAFFIC', '交通', 3),
    ('HIGHWAY', '高速', 4),
    ('SUPPORT_EVENT', '応援イベント', 5),
    ('SUPPORT_TRAFFIC', '応援交通', 6),
    ('SUPPORT_HIGHWAY', '応援高速', 7),
    ('TRAINING', '研修', 8),
    ('COMPANY', '会社', 9);
```

---

### 3.2.1 category_badges（区分バッジマスタ）

区分（site_categories）に紐づく子バッジの定義。
セル編集モーダルで区分に応じた子バッジをチップ選択できる。
親バッジ＝区分名（読み取り専用）、子バッジ＝本テーブルで定義された選択肢。

| カラム名        | 型           | NULL | 説明                                              |
| --------------- | ------------ | ---- | ------------------------------------------------- |
| id              | UUID         | NO   | 主キー                                            |
| category_id     | UUID         | NO   | 区分ID（FK）                                      |
| parent_badge_id | UUID         | YES  | 親バッジID（自己参照FK、NULLなら子バッジ）        |
| level           | INTEGER      | NO   | 階層レベル（1=子バッジ, 2=孫バッジ）              |
| code            | VARCHAR(20)  | NO   | バッジコード                                      |
| name            | VARCHAR(50)  | NO   | バッジ名（表示用）                                |
| color           | VARCHAR(7)   | YES  | バッジ背景色                                      |
| text_color      | VARCHAR(7)   | YES  | バッジ文字色                                      |
| sort_order      | INTEGER      | YES  | 表示順                                            |
| is_active       | BOOLEAN      | NO   | 有効フラグ                                        |
| created_at      | TIMESTAMPTZ  | NO   | 作成日時                                          |
| updated_at      | TIMESTAMPTZ  | NO   | 更新日時                                          |

※ 親バッジは `site_categories.name` をそのまま使用（読み取り専用表示）
※ 子バッジ: `parent_badge_id = NULL`, `level = 1`（区分直下）
※ 孫バッジ: `parent_badge_id = 子バッジID`, `level = 2`（子バッジの詳細項目）
※ 子バッジが0件の区分もある（施設、イベント等）。その場合、UIでは「バッジなし」と表示し、追加ボタンから新規作成可能
※ ユーザーがモーダルから新規バッジを追加した場合、本テーブルにINSERTされる

```sql
CREATE TABLE category_badges (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    category_id UUID NOT NULL REFERENCES site_categories(id) ON DELETE CASCADE,
    parent_badge_id UUID REFERENCES category_badges(id) ON DELETE CASCADE,
    level INTEGER NOT NULL DEFAULT 1 CHECK (level IN (1, 2)),
    code VARCHAR(20) NOT NULL,
    name VARCHAR(50) NOT NULL,
    color VARCHAR(7) DEFAULT '#718096',
    text_color VARCHAR(7) DEFAULT '#ffffff',
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(category_id, code)
);

CREATE INDEX idx_category_badges_category ON category_badges(category_id);
CREATE INDEX idx_category_badges_parent ON category_badges(parent_badge_id);

-- 初期データ（高速系）
INSERT INTO category_badges (category_id, code, name, sort_order)
SELECT sc.id, b.code, b.name, b.sort_order
FROM site_categories sc
CROSS JOIN (VALUES
    ('LANE_REGULATION', '車線規制', 1),
    ('SHOULDER_REGULATION', '路肩規制', 2),
    ('BOOTH_REGULATION', 'ブース規制', 3),
    ('SECURITY_GUARD', '保安員', 4)
) AS b(code, name, sort_order)
WHERE sc.code = 'HIGHWAY';

-- 初期データ（交通系）
INSERT INTO category_badges (category_id, code, name, sort_order)
SELECT sc.id, b.code, b.name, b.sort_order
FROM site_categories sc
CROSS JOIN (VALUES
    ('ALTERNATE_TRAFFIC', '片側交互', 1),
    ('ROAD_CLOSURE', '通行止め', 2)
) AS b(code, name, sort_order)
WHERE sc.code = 'TRAFFIC';
```

---

### 3.3 sites（現場マスタ）

| カラム名              | 型           | NULL | 説明                                       |
| --------------------- | ------------ | ---- | ------------------------------------------ |
| id                    | UUID         | NO   | 主キー                                     |
| group_company_id      | UUID         | NO   | グループ会社ID（FK）                       |
| company_id            | UUID         | NO   | 契約先ID（FK）                             |
| category_id           | UUID         | YES  | 区分ID（FK）                               |
| code                  | VARCHAR(20)  | NO   | 現場コード                                 |
| name                  | VARCHAR(100) | YES  | 現場名（個別業務の場合はNULL）             |
| short_name            | VARCHAR(20)  | YES  | 略称（出勤簿表示用）                       |
| hierarchy_depth       | INTEGER      | NO   | 階層の深さ（1=親のみ、2=子まで、3=孫まで） |
| default_shift_type    | VARCHAR(10)  | YES  | デフォルトシフト区分（day/night）          |
| default_start_time    | TIME         | YES  | デフォルト開始時間                         |
| default_end_time      | TIME         | YES  | デフォルト終了時間                         |
| default_contact       | VARCHAR(100) | YES  | デフォルト連絡                             |
| default_meeting_time  | TIME         | YES  | デフォルト集合時間                         |
| notes                 | TEXT         | YES  | 備考                                       |
| sort_order            | INTEGER      | YES  | 表示順                                     |
| is_active             | BOOLEAN      | NO   | 有効フラグ                                 |
| created_at            | TIMESTAMPTZ  | NO   | 作成日時                                   |
| updated_at            | TIMESTAMPTZ  | NO   | 更新日時                                   |

※ シフト区分（昼/夜）は日毎受注（daily_site_orders.shift_type）で確定管理。sitesテーブルのdefault_shift_typeはQuick Access等での入力時の初期値として使用
※ 現場監督候補は日毎受注（daily_site_orders）の過去入力実績から動的に取得するため、マスタでは保持しない
※ UNIQUE(group_company_id, company_id, category_id, name) — 同一会社・区分・契約先名・業務名の重複登録を防止

```sql
CREATE TABLE sites (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    company_id UUID NOT NULL REFERENCES companies(id),
    category_id UUID REFERENCES site_categories(id),
    code VARCHAR(20) NOT NULL,
    name VARCHAR(100),
    short_name VARCHAR(20),
    hierarchy_depth INTEGER NOT NULL DEFAULT 1 CHECK (hierarchy_depth BETWEEN 1 AND 3),
    default_shift_type VARCHAR(10) CHECK (default_shift_type IN ('day', 'night')),
    default_start_time TIME,
    default_end_time TIME,
    default_contact VARCHAR(100),
    default_meeting_time TIME,
    notes TEXT,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(company_id, code),
    UNIQUE(group_company_id, company_id, category_id, name)
);
```

---

### 3.2.2 site_sub_items（現場子項目）

現場の階層構造を管理するテーブル。PJNo.→工事名→作業内容のような階層を表現。

| カラム名   | 型           | NULL | 説明                                    |
| ---------- | ------------ | ---- | --------------------------------------- |
| id         | UUID         | NO   | 主キー                                  |
| site_id    | UUID         | NO   | 現場ID（FK）                            |
| parent_id  | UUID         | YES  | 親項目ID（FK、NULLなら第1階層子）       |
| label      | VARCHAR(50)  | NO   | 項目ラベル（例: 工事名、作業内容）       |
| name       | VARCHAR(100) | NO   | 項目名（値）                            |
| level      | INTEGER      | NO   | 階層レベル（1=子、2=孫）                |
| sort_order | INTEGER      | YES  | 表示順                                  |
| is_active  | BOOLEAN      | NO   | 有効フラグ                              |
| created_at | TIMESTAMPTZ  | NO   | 作成日時                                |
| updated_at | TIMESTAMPTZ  | NO   | 更新日時                                |

※ `label`: 現場ごとにカスタマイズ可能な項目種別名。高速系は「工事名」「作業内容」、施設系は「業務内容」など、区分や現場に応じて自由に設定できる。

```sql
CREATE TABLE site_sub_items (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    site_id UUID NOT NULL REFERENCES sites(id) ON DELETE CASCADE,
    parent_id UUID REFERENCES site_sub_items(id) ON DELETE CASCADE,
    label VARCHAR(50) NOT NULL DEFAULT '項目',
    name VARCHAR(100) NOT NULL,
    level INTEGER NOT NULL CHECK (level BETWEEN 1 AND 2),
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_site_sub_items_site ON site_sub_items(site_id);
CREATE INDEX idx_site_sub_items_parent ON site_sub_items(parent_id);
```

**使用例**:

西日本高速道路エンジニアリング四国㈱の場合:
```
sites: name = "24-1234" (hierarchy_depth = 3)
  └─ site_sub_items: label = "工事名", name = "〇〇橋補修工事" (level = 1)
       └─ site_sub_items: label = "作業内容", name = "点検作業" (level = 2, parent_id = 上の項目)

→ 計画書業務名: "24-1234 > 〇〇橋補修工事 > 点検作業"
```

Nikkei施設系の場合:
```
sites: name = "〇〇ビル" (hierarchy_depth = 2)
  └─ site_sub_items: label = "業務内容", name = "巡回警備" (level = 1)

→ 計画書業務名: "〇〇ビル > 巡回警備"
```

個別業務の場合:
```
sites: name = NULL (hierarchy_depth = 2)
  └─ site_sub_items: label = "作業内容", name = "交通誘導" (level = 1)

→ 計画書業務名: "交通誘導"（親業務名なし）
```

---

### 3.2.3 org_level_types（組織階層種別定義）

グループ会社ごとに組織階層の種別名を定義するマスタ。
各グループ会社が独自の階層構造（営業所→課、事業部→部→課 等）を持てるようにする。

| カラム名         | 型           | NULL | 説明                                       |
| ---------------- | ------------ | ---- | ------------------------------------------ |
| id               | UUID         | NO   | 主キー                                     |
| group_company_id | UUID         | NO   | グループ会社ID（FK）                       |
| depth            | INTEGER      | NO   | 階層の深さ（1〜5）                         |
| name             | VARCHAR(50)  | NO   | 階層種別名（営業所、部、課 等）            |
| created_at       | TIMESTAMPTZ  | NO   | 作成日時                                   |
| updated_at       | TIMESTAMPTZ  | NO   | 更新日時                                   |

※ 同一グループ会社内で depth はユニーク（各深さに1つの種別名）
※ depth は 1〜5 の範囲で制限（一般的な企業階層＋予備1階層）
※ 階層種別が未定義の深さには組織ノードを作成不可

```sql
CREATE TABLE org_level_types (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    depth INTEGER NOT NULL,
    name VARCHAR(50) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(group_company_id, depth),
    CHECK(depth BETWEEN 1 AND 5)
);

-- 初期データ
INSERT INTO org_level_types (group_company_id, depth, name)
SELECT gc.id, 1, '課'
FROM group_companies gc
WHERE gc.code = 'TOUO';

INSERT INTO org_level_types (group_company_id, depth, name)
SELECT gc.id, 1, '課'
FROM group_companies gc
WHERE gc.code = 'NIKKEI';

INSERT INTO org_level_types (group_company_id, depth, name)
SELECT gc.id, d.depth, d.name
FROM group_companies gc
CROSS JOIN (VALUES
    (1, '拠点'),
    (2, '課')
) AS d(depth, name)
WHERE gc.code = 'ZENNIHON';
```

---

### 3.2.4 organization_units（組織ノード）

グループ会社配下の組織階層を自己参照ツリーで管理する。
営業所・部・課など任意の深さのネスト構造を表現できる。
業務管理計画書の社員リストで組織階層タブの表示に使用する。

| カラム名         | 型            | NULL | 説明                                     |
| ---------------- | ------------- | ---- | ---------------------------------------- |
| id               | UUID          | NO   | 主キー                                   |
| group_company_id | UUID          | NO   | グループ会社ID（FK）                     |
| parent_id        | UUID          | YES  | 親組織ノードID（FK → 自己参照、NULLはルート） |
| level_type_id    | UUID          | NO   | 階層種別ID（FK → org_level_types）       |
| code             | VARCHAR(20)   | NO   | 組織コード                               |
| name             | VARCHAR(100)  | NO   | 組織名（東京本社、施設課 等）            |
| sort_order       | INTEGER       | YES  | 表示順                                   |
| is_active        | BOOLEAN       | NO   | 有効フラグ                               |
| created_at       | TIMESTAMPTZ   | NO   | 作成日時                                 |
| updated_at       | TIMESTAMPTZ   | NO   | 更新日時                                 |

※ 同一親ノードの下で組織コードはユニーク
※ parent_id = NULL はグループ会社直下のルートノード
※ level_type_id の depth と実際のツリー深さの整合性はアプリ側で検証
※ 社員（employees.org_unit_id）はツリーの任意ノードに所属可能
※ 配下の全社員取得には WITH RECURSIVE を使用（後述のビュー参照）

```sql
CREATE TABLE organization_units (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    parent_id UUID REFERENCES organization_units(id),
    level_type_id UUID NOT NULL REFERENCES org_level_types(id),
    code VARCHAR(20) NOT NULL,
    name VARCHAR(100) NOT NULL,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(group_company_id, parent_id, code)
);

CREATE INDEX idx_org_units_parent ON organization_units(parent_id);
CREATE INDEX idx_org_units_group_company ON organization_units(group_company_id);

-- 初期データ（東央警備: 課のみ、ルート直下）
WITH touo AS (SELECT id FROM group_companies WHERE code = 'TOUO'),
     lt_section AS (SELECT id FROM org_level_types WHERE group_company_id = (SELECT id FROM touo) AND depth = 1)
INSERT INTO organization_units (group_company_id, parent_id, level_type_id, code, name, sort_order)
VALUES
    ((SELECT id FROM touo), NULL, (SELECT id FROM lt_section), 'FACILITY', '施設課', 1),
    ((SELECT id FROM touo), NULL, (SELECT id FROM lt_section), 'TRAFFIC', '交通課', 2);

-- NikkeiHD: 課のみ（ルート直下）
WITH nikkei AS (SELECT id FROM group_companies WHERE code = 'NIKKEI'),
     lt_section AS (SELECT id FROM org_level_types WHERE group_company_id = (SELECT id FROM nikkei) AND depth = 1)
INSERT INTO organization_units (group_company_id, parent_id, level_type_id, code, name, sort_order)
VALUES
    ((SELECT id FROM nikkei), NULL, (SELECT id FROM lt_section), 'FACILITY', '施設課', 1),
    ((SELECT id FROM nikkei), NULL, (SELECT id FROM lt_section), 'TRAFFIC', '交通課', 2);

-- 全日本エンタープライズ: 拠点→課の2階層
WITH zen AS (SELECT id FROM group_companies WHERE code = 'ZENNIHON'),
     lt_base AS (SELECT id FROM org_level_types WHERE group_company_id = (SELECT id FROM zen) AND depth = 1)
INSERT INTO organization_units (group_company_id, parent_id, level_type_id, code, name, sort_order)
VALUES
    ((SELECT id FROM zen), NULL, (SELECT id FROM lt_base), 'HONSHA', '本社', 1),
    ((SELECT id FROM zen), NULL, (SELECT id FROM lt_base), 'EHIME', '愛媛営業所', 2);

-- 本社の下に課を追加
WITH zen AS (SELECT id FROM group_companies WHERE code = 'ZENNIHON'),
     honsha AS (SELECT id FROM organization_units WHERE code = 'HONSHA' AND group_company_id = (SELECT id FROM zen)),
     lt_section AS (SELECT id FROM org_level_types WHERE group_company_id = (SELECT id FROM zen) AND depth = 2)
INSERT INTO organization_units (group_company_id, parent_id, level_type_id, code, name, sort_order)
VALUES
    ((SELECT id FROM zen), (SELECT id FROM honsha), (SELECT id FROM lt_section), 'TRAFFIC1', '交通一課', 1),
    ((SELECT id FROM zen), (SELECT id FROM honsha), (SELECT id FROM lt_section), 'TRAFFIC2', '交通二課', 2),
    ((SELECT id FROM zen), (SELECT id FROM honsha), (SELECT id FROM lt_section), 'TRAFFIC3', '交通三課', 3);

-- 配下ノード取得用の再帰ビュー
CREATE OR REPLACE VIEW org_unit_tree AS
WITH RECURSIVE tree AS (
    SELECT id, group_company_id, parent_id, level_type_id, code, name, sort_order, is_active,
           id AS root_id, 1 AS tree_depth, ARRAY[id] AS path
    FROM organization_units
    WHERE parent_id IS NULL
    UNION ALL
    SELECT ou.id, ou.group_company_id, ou.parent_id, ou.level_type_id, ou.code, ou.name, ou.sort_order, ou.is_active,
           tree.root_id, tree.tree_depth + 1, tree.path || ou.id
    FROM organization_units ou
    JOIN tree ON ou.parent_id = tree.id
)
SELECT t.*, olt.name AS level_type_name, olt.depth AS level_depth
FROM tree t
JOIN org_level_types olt ON t.level_type_id = olt.id;
```

---

### 3.3 employees（社員マスタ）

| カラム名         | 型          | NULL | 説明                         |
| ---------------- | ----------- | ---- | ---------------------------- |
| id               | UUID        | NO   | 主キー                       |
| employee_code    | VARCHAR(20) | NO   | 社員コード（ユニーク）       |
| group_company_id | UUID        | NO   | グループ会社ID（FK）         |
| org_unit_id      | UUID        | YES  | 組織ノードID（FK）           |
| name             | VARCHAR(50) | NO   | 氏名                         |
| short_name       | VARCHAR(10) | YES  | 略称（ドロップダウン表示用） |
| sort_order       | INTEGER     | YES  | 表示順                       |
| is_active        | BOOLEAN     | NO   | 有効フラグ                   |
| created_at       | TIMESTAMPTZ | NO   | 作成日時                     |
| updated_at       | TIMESTAMPTZ | NO   | 更新日時                     |

※ `group_company_id`: 社員が所属するグループ会社（東央警備/Nikkeiホールディングス/全日本エンタープライズ）。休日申請管理のグループ会社フィルタ等に使用
※ `org_unit_id`: 所属組織ノード。organization_units テーブルを参照。NULL の場合は組織未設定。ツリーの任意階層に所属可能（営業所直属も課所属も可）

```sql
CREATE TABLE employees (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_code VARCHAR(20) NOT NULL UNIQUE,
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    org_unit_id UUID REFERENCES organization_units(id),
    name VARCHAR(50) NOT NULL,
    short_name VARCHAR(10),
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_employees_org_unit ON employees(org_unit_id);
```

---

### 3.3.1 employee_conflicts（社員配置制約）

人間関係（相性）により、同じ現場に配置すべきでない社員同士を管理。
配置時に注意喚起アイコンを表示する。

| カラム名      | 型          | NULL | 説明       |
| ------------- | ----------- | ---- | ---------- |
| id            | UUID        | NO   | 主キー     |
| employee_id_1 | UUID        | NO   | 社員1（FK）|
| employee_id_2 | UUID        | NO   | 社員2（FK）|
| is_active     | BOOLEAN     | NO   | 有効フラグ |
| created_at    | TIMESTAMPTZ | NO   | 作成日時   |
| updated_at    | TIMESTAMPTZ | NO   | 更新日時   |

```sql
CREATE TABLE employee_conflicts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id_1 UUID NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
    employee_id_2 UUID NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    -- 重複防止: employee_id_1 < employee_id_2 の制約
    CONSTRAINT chk_employee_order CHECK (employee_id_1 < employee_id_2),
    -- 同じ組み合わせは1つのみ
    UNIQUE(employee_id_1, employee_id_2)
);

CREATE INDEX idx_employee_conflicts_1 ON employee_conflicts(employee_id_1);
CREATE INDEX idx_employee_conflicts_2 ON employee_conflicts(employee_id_2);
```

**UI動作**:
- 同じ現場・同じ日・同じシフトに制約関係の社員が配置された場合
- 該当社員の横に警告アイコンを表示
- ホバーで「〇〇さんと配置制約があります」等のツールチップ

**制約チェックSQL例**:
```sql
-- 特定の配置で制約関係の社員がいるかチェック
SELECT ec.*, e1.name as employee1_name, e2.name as employee2_name
FROM employee_conflicts ec
JOIN employees e1 ON ec.employee_id_1 = e1.id
JOIN employees e2 ON ec.employee_id_2 = e2.id
WHERE ec.is_active = true
  AND (ec.employee_id_1 IN (:assigned_employee_ids)
       OR ec.employee_id_2 IN (:assigned_employee_ids))
  AND ec.employee_id_1 IN (:assigned_employee_ids)
  AND ec.employee_id_2 IN (:assigned_employee_ids);
```

---

### 3.4 license_types（資格種別マスタ）

| カラム名         | 型           | NULL | 説明                                    |
| ---------------- | ------------ | ---- | --------------------------------------- |
| id               | UUID         | NO   | 主キー                                  |
| code             | VARCHAR(20)  | NO   | 資格コード                              |
| name             | VARCHAR(100) | NO   | 資格名                                  |
| category         | VARCHAR(20)  | NO   | カテゴリ（certification/license/skill） |
| requires_renewal | BOOLEAN      | NO   | 更新が必要か                            |
| sort_order       | INTEGER      | YES  | 表示順                                  |
| is_active        | BOOLEAN      | NO   | 有効フラグ                              |
| created_at       | TIMESTAMPTZ  | NO   | 作成日時                                |
| updated_at       | TIMESTAMPTZ  | NO   | 更新日時                                |

#### カテゴリ（category）

| コード        | 説明                 |
| ------------- | -------------------- |
| certification | 検定（警備業務検定） |
| license       | 運転免許             |
| skill         | スキル（社内認定）   |

```sql
CREATE TABLE license_types (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    category VARCHAR(20) NOT NULL CHECK (category IN ('certification', 'license', 'skill')),
    requires_renewal BOOLEAN NOT NULL DEFAULT false,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 初期データ: 検定（警備業務検定6種×1/2級 = 12種）
-- ※ 注文書の `purchase_order_certifications` からもこのマスタを参照
INSERT INTO license_types (code, name, category, requires_renewal, sort_order) VALUES
    ('TRAFFIC_1', '交通誘導警備 1級', 'certification', false, 1),
    ('TRAFFIC_2', '交通誘導警備 2級', 'certification', false, 2),
    ('FACILITY_1', '施設警備 1級', 'certification', false, 3),
    ('FACILITY_2', '施設警備 2級', 'certification', false, 4),
    ('CROWD_1', '雑踏警備 1級', 'certification', false, 5),
    ('CROWD_2', '雑踏警備 2級', 'certification', false, 6),
    ('AIRPORT_1', '空港保安警備 1級', 'certification', false, 7),
    ('AIRPORT_2', '空港保安警備 2級', 'certification', false, 8),
    ('NUCLEAR_1', '核燃料物質等危険物運搬警備 1級', 'certification', false, 9),
    ('NUCLEAR_2', '核燃料物質等危険物運搬警備 2級', 'certification', false, 10),
    ('VALUABLE_1', '貴重品運搬警備 1級', 'certification', false, 11),
    ('VALUABLE_2', '貴重品運搬警備 2級', 'certification', false, 12);

-- 初期データ: 運転免許
INSERT INTO license_types (code, name, category, requires_renewal, sort_order) VALUES
    ('LICENSE_NORMAL_MT', '普通免許（MT）', 'license', true, 10),
    ('LICENSE_NORMAL_AT', '普通免許（AT限定）', 'license', true, 11),
    ('LICENSE_SEMI_MEDIUM_MT', '準中型免許（MT）', 'license', true, 12),
    ('LICENSE_SEMI_MEDIUM_AT', '準中型免許（AT限定）', 'license', true, 13),
    ('LICENSE_MEDIUM_MT', '中型免許（MT）', 'license', true, 14),
    ('LICENSE_MEDIUM_AT', '中型免許（AT限定）', 'license', true, 15);

-- 初期データ: スキル
INSERT INTO license_types (code, name, category, requires_renewal, sort_order) VALUES
    ('SKILL_SIGN_TRUCK', '標識車', 'skill', false, 20),
    ('SKILL_SIGN_TRUCK_EXT', '標識車（外販）', 'skill', false, 21),
    ('SKILL_2TON_TRUCK', '2トン車', 'skill', false, 22);
```

---

### 3.5 employee_licenses（社員資格）

| カラム名        | 型          | NULL | 説明             |
| --------------- | ----------- | ---- | ---------------- |
| id              | UUID        | NO   | 主キー           |
| employee_id     | UUID        | NO   | 社員ID（FK）     |
| license_type_id | UUID        | NO   | 資格種別ID（FK） |
| license_number  | VARCHAR(50) | YES  | 資格番号         |
| acquired_date   | DATE        | YES  | 取得日           |
| expiry_date     | DATE        | YES  | 有効期限         |
| notes           | TEXT        | YES  | 備考             |
| created_at      | TIMESTAMPTZ | NO   | 作成日時         |
| updated_at      | TIMESTAMPTZ | NO   | 更新日時         |

```sql
CREATE TABLE employee_licenses (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id UUID NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
    license_type_id UUID NOT NULL REFERENCES license_types(id),
    license_number VARCHAR(50),
    acquired_date DATE,
    expiry_date DATE,
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(employee_id, license_type_id)
);
```

---

### 3.6 daily_site_orders（日毎受注）

営業担当者が契約先から受けた受注情報を管理するテーブル。
「どの現場に、いつ、何名必要か」という受注の基本情報を保持する。

**業務背景**: 営業担当者が契約先から連絡を受け、システムに入力する。営業担当者が直接入力できない場合は、
配置担当者に連絡して代理入力してもらう。複数の営業担当者が同じ受注情報を変更する可能性があるため、
変更履歴の記録と楽観的ロックによる競合検知が必要。

| カラム名        | 型           | NULL | 説明                                                        |
| --------------- | ------------ | ---- | ----------------------------------------------------------- |
| id              | UUID         | NO   | 主キー                                                      |
| order_date      | DATE         | NO   | 受注対象日                                                  |
| site_id         | UUID         | NO   | 現場ID（FK）                                                |
| shift_type      | VARCHAR(10)  | NO   | シフト区分（day/night）                                     |
| placement_seq   | INTEGER      | NO   | 配置先連番（デフォルト: 0、同一行の複数配置先を区別）       |
| required_count  | INTEGER      | NO   | 必要人数                                                    |
| start_time      | TIME         | YES  | 開始時間                                                    |
| end_time        | TIME         | YES  | 終了時間                                                    |
| contact         | VARCHAR(100) | YES  | 連絡                                                        |
| supervisor_name | VARCHAR(100) | YES  | 現場監督名                                                  |
| supervisor_tel  | VARCHAR(20)  | YES  | 現場監督連絡先                                              |
| meeting_place   | VARCHAR(100) | YES  | 集合場所                                                    |
| meeting_time    | TIME         | YES  | 集合時間                                                    |
| daily_task_name | VARCHAR(200) | YES  | 計画書業務名（自動生成: 親業務名 > 子 > 孫）                |
| contact_info    | TEXT         | YES  | 連絡事項                                                    |
| maps            | JSONB        | YES  | 現場地図（複数対応）`[{"label":"..","url":".."}]`           |
| notes           | TEXT         | YES  | 備考                                                        |
| badge_child_ids | UUID[]       | YES  | 選択済み子バッジID（category_badges.id の配列）             |
| badge_gc_map    | JSONB        | YES  | 選択済み孫バッジ `{"子バッジID": ["孫バッジID", ...]}`      |
| confidence      | VARCHAR(20)  | NO   | 信頼度（confirmed/tentative_high/tentative_low）デフォルト: tentative_high |
| status          | VARCHAR(20)  | NO   | ステータス（active/cancelled）デフォルト: active            |
| version         | INTEGER      | NO   | 楽観的ロック用バージョン番号（デフォルト: 1）               |
| created_by      | UUID         | YES  | 作成者（FK → employees）                                    |
| updated_by      | UUID         | YES  | 最終更新者（FK → employees）                                |
| created_at      | TIMESTAMPTZ  | NO   | 作成日時                                                    |
| updated_at      | TIMESTAMPTZ  | NO   | 更新日時                                                    |

※ `badge_child_ids`: 選択された子バッジIDの配列。親バッジ（区分）は `sites.category_id` → `site_categories` で決定されるため保持不要。子バッジのみ配列で保存。`daily_assignments.vehicle_ids` と同じUUID[]パターン
※ `badge_gc_map`: 選択された孫バッジのマップ。キーは子バッジID、値は孫バッジID配列。例: `{"hw-lane": ["hw-lane-sign", "hw-lane-mat"]}`
※ `confidence`: 信頼度。confirmed=確定、tentative_high=予定（信頼:高）、tentative_low=予定（信頼:低）。新規入力時のデフォルトはtentative_high
※ `confidence` 自動確定ルール: (1) 受注対象日（order_date）が昨日以前 → 自動的にconfirmedに変更。(2) 業務管理計画書（daily_assignments）で社員が配置された場合 → 自動的にconfirmedに変更
※ `status`: active=有効、cancelled=その日のキャンセル
※ `version`: 楽観的ロック用。保存時にバージョンを照合し、他者が先に変更していた場合は警告を表示
※ `placement_seq`: 同一日・同一現場・同一シフトで複数の配置先に分けるための連番。通常は0。複数配置先の場合は0, 1, 2...と増加
※ `created_by` / `updated_by`: 誰が入力・変更したかを記録

```sql
CREATE TABLE daily_site_orders (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    order_date DATE NOT NULL,
    site_id UUID NOT NULL REFERENCES sites(id),
    shift_type VARCHAR(10) NOT NULL CHECK (shift_type IN ('day', 'night')),
    placement_seq INTEGER NOT NULL DEFAULT 0,
    required_count INTEGER NOT NULL DEFAULT 0,
    start_time TIME,
    end_time TIME,
    contact VARCHAR(100),
    supervisor_name VARCHAR(100),
    supervisor_tel VARCHAR(20),
    meeting_place VARCHAR(100),
    meeting_time TIME,
    daily_task_name VARCHAR(200),
    contact_info TEXT,
    maps JSONB,
    notes TEXT,
    badge_child_ids UUID[],
    badge_gc_map JSONB,
    confidence VARCHAR(20) NOT NULL DEFAULT 'tentative_high' CHECK (confidence IN ('confirmed', 'tentative_high', 'tentative_low')),
    status VARCHAR(20) NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'cancelled')),
    version INTEGER NOT NULL DEFAULT 1,
    created_by UUID REFERENCES employees(id),
    updated_by UUID REFERENCES employees(id),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(order_date, site_id, shift_type, placement_seq)
);

CREATE INDEX idx_daily_site_orders_date ON daily_site_orders(order_date);
CREATE INDEX idx_daily_site_orders_site ON daily_site_orders(site_id);
CREATE INDEX idx_daily_site_orders_status ON daily_site_orders(status);
CREATE INDEX idx_daily_site_orders_badges ON daily_site_orders USING GIN(badge_child_ids);
```

---

### 3.6.1 daily_order_sub_items（日毎受注業務詳細）

日毎受注（daily_site_orders）に紐づく業務詳細項目。
モーダルの「業務詳細」セクションで動的に追加・削除される子項目を保持する。
ラベル（工事名、作業内容など）と値のペアで管理し、複数エントリを許容する。

| カラム名             | 型           | NULL | 説明                                                        |
| -------------------- | ------------ | ---- | ----------------------------------------------------------- |
| id                   | UUID         | NO   | 主キー                                                      |
| daily_site_order_id  | UUID         | NO   | 日毎受注ID（FK）                                            |
| sort_order           | INTEGER      | NO   | 表示順（0始まり）                                           |
| label                | VARCHAR(50)  | NO   | 項目ラベル（例: 工事名、作業内容、業務内容）                |
| value                | VARCHAR(200) | NO   | 項目値                                                      |
| created_at           | TIMESTAMPTZ  | NO   | 作成日時                                                    |
| updated_at           | TIMESTAMPTZ  | NO   | 更新日時                                                    |

※ `label`: マスタ（site_sub_items）のlabelをデフォルト値として使用するが、日毎受注ごとにカスタマイズ可能
※ `sort_order`: 計画書業務名の連結順序を決定（0 → 1 → 2 の順に `>` で連結）
※ `daily_task_name`（親テーブル）は本テーブルの値から自動生成: `親業務名 > value[0] > value[1]`

```sql
CREATE TABLE daily_order_sub_items (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    daily_site_order_id UUID NOT NULL REFERENCES daily_site_orders(id) ON DELETE CASCADE,
    sort_order INTEGER NOT NULL DEFAULT 0,
    label VARCHAR(50) NOT NULL DEFAULT '項目',
    value VARCHAR(200) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_daily_order_sub_items_order ON daily_order_sub_items(daily_site_order_id);
```

**使用例**:

高速道路の日毎受注の場合:
```
daily_site_orders: site_id → sites(name = "24-1234"), daily_task_name = "24-1234 > 〇〇橋補修工事 > 点検作業"
  └─ daily_order_sub_items: sort_order=0, label="工事名", value="〇〇橋補修工事"
  └─ daily_order_sub_items: sort_order=1, label="作業内容", value="点検作業"
```

個別業務（sites.name = NULL）の場合:
```
daily_site_orders: site_id → sites(name = NULL), daily_task_name = "交通誘導"
  └─ daily_order_sub_items: sort_order=0, label="作業内容", value="交通誘導"
```

---

### 3.6.2 daily_assignments（日毎配置）

配置担当者が管理する配置計画情報。日毎受注に紐づき、車両配置など配置業務に必要な情報を保持する。

| カラム名            | 型           | NULL | 説明                                              |
| ------------------- | ------------ | ---- | ------------------------------------------------- |
| id                  | UUID         | NO   | 主キー                                            |
| daily_site_order_id | UUID         | NO   | 日毎受注ID（FK）                                  |
| vehicle_ids         | UUID[]       | YES  | 配置車両ID（複数可）                              |
| etc_card_ids        | UUID[]       | YES  | 配置ETCカードID（複数可）                         |
| display_items       | JSONB        | YES  | 備考列表示用アイテム（送迎・集合場所・備考等のラベル+色） |
| created_at          | TIMESTAMPTZ  | NO   | 作成日時                                          |
| updated_at          | TIMESTAMPTZ  | NO   | 更新日時                                          |

※ `display_items`: 業務管理計画書グリッドの「備考」列に表示するアイテム。送迎・集合場所・備考等をラベル+値+色のタグとして表示する。車両・ETC表示は独立した「車両」列（`vehicle_ids` / `etc_card_ids`）で管理するため、display_itemsには含めない
※ 車両列は上部=車両タグ、下部=ETCタグの2段構成。同一シフト帯に同じ車両・ETCを複数現場に配置不可

`display_items` の形式:
```json
[
  {"label": "送迎", "value": "高橋→田中", "base": "#975A16", "bg": "rgba(151,90,22,0.12)", "color": "#6B4513"},
  {"label": "集合場所", "value": "新宿駅南口", "base": "#2563EB", "bg": "rgba(37,99,235,0.12)", "color": "#1E3A5F"},
  {"label": "備考", "value": "雨天中止", "base": "#6B7280", "bg": "rgba(107,114,128,0.12)", "color": "#374151"}
]
```

- `label`: アイテムラベル（プリセット: 送迎/集合場所/備考、カスタム追加可能）
- `value`: アイテムの値（自由入力）
- `base`: ベースカラー（HEX）。`bg`と`color`の自動生成元
- `bg`: 背景色（baseからrgba alpha=0.12で自動生成）
- `color`: テキスト色（baseからHSL暗色で自動生成）

```sql
CREATE TABLE daily_assignments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    daily_site_order_id UUID NOT NULL REFERENCES daily_site_orders(id) ON DELETE CASCADE,
    vehicle_ids UUID[],
    etc_card_ids UUID[],
    display_items JSONB DEFAULT '[]'::jsonb,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(daily_site_order_id)
);

CREATE INDEX idx_daily_assignments_vehicles ON daily_assignments USING GIN(vehicle_ids);
CREATE INDEX idx_daily_assignments_etc_cards ON daily_assignments USING GIN(etc_card_ids);
```

---

### 3.7 assignment_details（配置詳細）

| カラム名            | 型          | NULL | 説明                                            |
| ------------------- | ----------- | ---- | ----------------------------------------------- |
| id                  | UUID        | NO   | 主キー                                          |
| daily_assignment_id | UUID        | NO   | 日毎配置ID（FK）                                |
| employee_id         | UUID        | NO   | 社員ID（FK）                                    |
| contact_type        | VARCHAR(20) | YES  | 連絡タイプ（company/direct/line/pickup/op/other）|
| position_order      | INTEGER     | YES  | 配置順                                          |
| created_at          | TIMESTAMPTZ | NO   | 作成日時                                        |
| updated_at          | TIMESTAMPTZ | NO   | 更新日時                                        |

※ 車両配置は日毎配置（daily_assignments.vehicle_ids）で管理
※ `contact_type`: 配置社員への連絡方法。company=会社経由、direct=直接、line=LINE、pickup=迎え、op=OP（オペレーター）、other=その他。UIでカスタム追加も可能

```sql
CREATE TABLE assignment_details (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    daily_assignment_id UUID NOT NULL REFERENCES daily_assignments(id) ON DELETE CASCADE,
    employee_id UUID NOT NULL REFERENCES employees(id),
    contact_type VARCHAR(20),
    position_order INTEGER DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(daily_assignment_id, employee_id)
);
```

---

### 3.7.2 order_change_history（受注変更履歴）

日毎受注（daily_site_orders）の全変更を記録するテーブル。変更されたカラム名・変更前の値・変更後の値を記録し、
任意の過去バージョンへのロールバックを可能にする。

**業務背景**: 複数の営業担当者が受注情報を入力・変更し、配置担当者が代理入力やキャンセル処理を行う運用において、
聞き間違いによる誤キャンセル、誤った人数変更、他の営業担当者による誤情報の上書きが発生する。
変更履歴により「誰がいつ何を変更したか」を追跡し、誤操作時のロールバックを可能にする。

| カラム名       | 型           | NULL | 説明                                                |
| -------------- | ------------ | ---- | --------------------------------------------------- |
| id             | UUID         | NO   | 主キー                                              |
| order_id       | UUID         | NO   | 日毎受注ID（FK）                                    |
| version        | INTEGER      | NO   | この変更後のバージョン番号                          |
| operation_type | VARCHAR(20)  | NO   | 操作種別（create/update/cancel/restore/rollback/merge） |
| changed_fields | JSONB        | NO   | 変更内容 [{field, old_value, new_value}, ...]       |
| change_reason  | TEXT         | YES  | 変更理由（任意入力）                                |
| changed_by     | UUID         | NO   | 変更者（FK → employees）                            |
| changed_at     | TIMESTAMPTZ  | NO   | 変更日時                                            |

**operation_type の値**:

| 値       | 説明                                               |
| -------- | -------------------------------------------------- |
| create   | 新規受注の入力                                     |
| update   | 通常の更新（人数変更、時間変更等）                 |
| cancel   | その日のキャンセル処理（status → cancelled）       |
| restore  | キャンセルからの復元（status → active）            |
| rollback | 過去バージョンへのロールバック                     |
| merge    | 競合解決時のスマートマージ（非重複項目の統合）     |

**changed_fields の形式**:

```json
[
  { "field": "required_count", "old_value": "3", "new_value": "2" },
  { "field": "start_time", "old_value": "08:00", "new_value": "09:00" },
  { "field": "status", "old_value": "active", "new_value": "cancelled" }
]
```

```sql
CREATE TABLE order_change_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    order_id UUID NOT NULL REFERENCES daily_site_orders(id) ON DELETE CASCADE,
    version INTEGER NOT NULL,
    operation_type VARCHAR(20) NOT NULL CHECK (operation_type IN ('create', 'update', 'cancel', 'restore', 'rollback', 'merge')),
    changed_fields JSONB NOT NULL DEFAULT '[]'::jsonb,
    change_reason TEXT,
    changed_by UUID NOT NULL REFERENCES employees(id),
    changed_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(order_id, version)
);

CREATE INDEX idx_order_change_history_order ON order_change_history(order_id);
CREATE INDEX idx_order_change_history_changed_by ON order_change_history(changed_by);
CREATE INDEX idx_order_change_history_changed_at ON order_change_history(changed_at);
```

**リアルタイム同期との関係**:

受注簿と業務管理計画書は同一DBを共有し、Supabase Realtimeによりデータ変更は全画面にリアルタイム反映される。自分・他ユーザーの変更ともに変更通知モーダルで確認でき、「元に戻す」「やっぱり反映」で操作可能（全ユーザー使用可）。復元には通知レコードに保存された変更前後のスナップショット（`snapshot_before` / `snapshot_after`）を使用する。

**楽観的ロック（競合検知）の動作**:

1. 画面表示時に受注の `version` を取得
2. 保存時にDBの `version` と照合
3. 一致すれば保存成功 → `version` をインクリメント、`order_change_history` に記録
4. 不一致なら他者が先に変更済み → **スマートマージ判定**を実行（下記参照）

**スマートマージ（競合解決）の動作**:

バージョン不一致を検知した場合、自分の変更項目と他者の変更項目を比較し、競合の種類に応じた解決方法を提示する。

**ケースA: 変更項目が重複しない場合（自動マージ候補）**
- 例: 営業Aが `start_time` を変更、営業Bが `required_count` を変更
- ダイアログで両者の変更内容を表示し、以下の選択肢を提示:
  - **両方の変更をマージ**: 営業Aの変更と営業Bの変更を統合して保存（`operation_type = 'merge'`）
  - **自分の変更のみ上書き**: 他者の変更を破棄し、自分の変更で上書き
  - **キャンセル**: 保存を中止し、最新データを再取得

**ケースB: 変更項目が重複する場合（手動選択必須）**
- 例: 営業Aも営業Bも `required_count` を変更
- 競合ダイアログで重複項目ごとに以下を表示:
  - 変更前の値（ベースバージョン）
  - 他者の変更後の値（現在のDB値）
  - 自分の変更後の値
- 各項目について、他者の値と自分の値のどちらを採用するか選択
- 非重複項目がある場合は、それらも含めてマージ
- 最終的な変更内容を確認後に保存（`operation_type = 'merge'`）

**マージ後の履歴記録**:
- マージ操作は `operation_type = 'merge'` として `order_change_history` に記録
- `changed_fields` には最終的に適用された変更の差分を記録
- `change_reason` に「バージョンX（営業Aの変更）とマージ」等の自動メモを付与

**ロールバックの動作**:

1. 変更履歴一覧からロールバック先のバージョンを選択
2. 選択バージョンの `changed_fields` から、そのバージョン時点の値を復元
3. ロールバック自体も `operation_type = 'rollback'` として履歴に記録
4. 営業担当者・配置担当者・管理者がロールバック可能

---

### 3.8 transport_relations（送迎関係）

| カラム名              | 型           | NULL | 説明                   |
| --------------------- | ------------ | ---- | ---------------------- |
| id                    | UUID         | NO   | 主キー                 |
| assignment_date       | DATE         | NO   | 配置日                 |
| driver_employee_id    | UUID         | NO   | 迎えに行く社員ID（FK） |
| passenger_employee_id | UUID         | NO   | 迎えられる社員ID（FK） |
| pickup_location       | VARCHAR(100) | YES  | ピックアップ場所       |
| notes                 | TEXT         | YES  | 備考                   |
| created_at            | TIMESTAMPTZ  | NO   | 作成日時               |
| updated_at            | TIMESTAMPTZ  | NO   | 更新日時               |

```sql
CREATE TABLE transport_relations (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    assignment_date DATE NOT NULL,
    driver_employee_id UUID NOT NULL REFERENCES employees(id),
    passenger_employee_id UUID NOT NULL REFERENCES employees(id),
    pickup_location VARCHAR(100),
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CHECK (driver_employee_id != passenger_employee_id)
);
```

---

### 3.9 連続勤務（自動検知）

連続勤務はテーブルで管理せず、**配置データから自動検知**する機能として実装。

#### 検知ロジック

**検知範囲**: 同日内 + 日をまたぐパターンの両方

| パターン   | 説明                     | 検知条件                               |
| ---------- | ------------------------ | -------------------------------------- |
| 昼→夜     | 同日の昼勤務後に夜勤務   | 同一社員が同日に昼・夜両方に配置       |
| 夜→昼     | 前日夜勤務後に翌日昼勤務 | 同一社員が前日夜に配置＆当日昼に配置   |
| 昼→夜→昼 | 昼から夜を経て翌日昼     | 同一社員が昼→夜→翌日昼に連続配置     |
| 夜→昼→夜 | 夜から昼を経て同日夜     | 同一社員が前日夜→当日昼→当日夜に配置 |

#### 表示方法

- **アイコン・バッジ**: 連続勤務パターンを示すアイコンを社員名の横に表示
- **背景色**: 連続勤務中の社員を背景色で視覚的に強調

#### 検知SQL例

```sql
-- 特定日の社員の連続勤務パターンを検知
WITH employee_shifts AS (
    SELECT
        ad.employee_id,
        da.assignment_date,
        da.shift_type,
        da.id as assignment_id
    FROM assignment_details ad
    JOIN daily_assignments da ON ad.daily_assignment_id = da.id
    WHERE da.assignment_date BETWEEN :target_date - 1 AND :target_date
),
patterns AS (
    SELECT
        e1.employee_id,
        -- 同日の昼→夜
        CASE WHEN EXISTS (
            SELECT 1 FROM employee_shifts e2
            WHERE e2.employee_id = e1.employee_id
            AND e2.assignment_date = e1.assignment_date
            AND e1.shift_type = 'day' AND e2.shift_type = 'night'
        ) THEN true ELSE false END as day_to_night,
        -- 前日夜→当日昼
        CASE WHEN EXISTS (
            SELECT 1 FROM employee_shifts e2
            WHERE e2.employee_id = e1.employee_id
            AND e2.assignment_date = e1.assignment_date - 1
            AND e2.shift_type = 'night' AND e1.shift_type = 'day'
        ) THEN true ELSE false END as night_to_day
    FROM employee_shifts e1
    WHERE e1.assignment_date = :target_date
)
SELECT
    employee_id,
    CASE
        WHEN day_to_night AND night_to_day THEN 'night_day_night'
        WHEN day_to_night THEN 'day_to_night'
        WHEN night_to_day THEN 'night_to_day'
        ELSE null
    END as continuous_pattern
FROM patterns
WHERE day_to_night OR night_to_day;
```

---

### 3.10 vehicles（車両マスタ）

| カラム名            | 型           | NULL | 説明                          |
| ------------------- | ------------ | ---- | ----------------------------- |
| id                  | UUID         | NO   | 主キー                        |
| vehicle_code        | VARCHAR(20)  | NO   | 車両コード（ユニーク）        |
| group_company_id    | UUID         | NO   | 所属グループ会社ID（FK）      |
| license_plate       | VARCHAR(20)  | NO   | ナンバープレート              |
| short_name          | VARCHAR(20)  | YES  | 略称（ドロップダウン表示用）  |
| model               | VARCHAR(100) | YES  | 車種名                        |
| oil_change_distance | INTEGER      | YES  | オイル交換距離（km）          |
| inspection_date     | DATE         | YES  | 車検満了日                    |
| notes               | TEXT         | YES  | 備考                          |
| sort_order          | INTEGER      | YES  | 表示順                        |
| is_active           | BOOLEAN      | NO   | 有効フラグ                    |
| created_at          | TIMESTAMPTZ  | NO   | 作成日時                      |
| updated_at          | TIMESTAMPTZ  | NO   | 更新日時                      |

```sql
CREATE TABLE vehicles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    vehicle_code VARCHAR(20) NOT NULL UNIQUE,
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    license_plate VARCHAR(20) NOT NULL,
    short_name VARCHAR(20),
    model VARCHAR(100),
    oil_change_distance INTEGER,
    inspection_date DATE,
    notes TEXT,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

### 3.10.1 etc_cards（ETCカードマスタ）

グループ会社ごとに管理するETCカード。車両列の下部にドラッグ&ドロップで配置する。
東央警備はアルファベットラベル方式（ETC-A 〜 ETC-F）、NikkeiHDは車番紐付け方式（ETC + ナンバー下4桁）で管理。

| カラム名            | 型           | NULL | 説明                              |
| ------------------- | ------------ | ---- | --------------------------------- |
| id                  | UUID         | NO   | 主キー                            |
| card_label          | VARCHAR(30)  | NO   | カードラベル（例: ETC-A, ETC く7521） |
| group_company_id    | UUID         | NO   | 所属グループ会社ID（FK）          |
| vehicle_id          | UUID         | YES  | 紐付け車両ID（FK、Nikkei方式用）  |
| notes               | TEXT         | YES  | 備考                              |
| sort_order          | INTEGER      | YES  | 表示順                            |
| is_active           | BOOLEAN      | NO   | 有効フラグ                        |
| created_at          | TIMESTAMPTZ  | NO   | 作成日時                          |
| updated_at          | TIMESTAMPTZ  | NO   | 更新日時                          |

```sql
CREATE TABLE etc_cards (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    card_label VARCHAR(30) NOT NULL,
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    vehicle_id UUID REFERENCES vehicles(id),
    notes TEXT,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(card_label, group_company_id)
);
```

---

### 3.11 maintenance_schedules（メンテナンス予定）

週間予定表の「修理/点検行」で参照される。`scheduled_date` が表示週に含まれ、`status = 'scheduled'` のレコードを車両バッジとして表示する。配置済み車両（`daily_assignments.vehicle_ids` に含まれる）は配置先現場名を併記する。

| カラム名                 | 型           | NULL | 説明                                                        |
| ------------------------ | ------------ | ---- | ----------------------------------------------------------- |
| id                       | UUID         | NO   | 主キー                                                      |
| vehicle_id               | UUID         | NO   | 車両ID（FK）                                                |
| maintenance_type         | VARCHAR(50)  | NO   | メンテナンス種別（inspection/oil_change/tire/repair/other） |
| scheduled_date           | DATE         | NO   | 予定日                                                      |
| shop_name                | VARCHAR(100) | YES  | 整備店名                                                    |
| google_calendar_event_id | VARCHAR(100) | YES  | Googleカレンダー連携用ID                                    |
| notes                    | TEXT         | YES  | 備考                                                        |
| status                   | VARCHAR(20)  | NO   | ステータス（scheduled/completed/cancelled）                 |
| created_at               | TIMESTAMPTZ  | NO   | 作成日時                                                    |
| updated_at               | TIMESTAMPTZ  | NO   | 更新日時                                                    |

```sql
CREATE TABLE maintenance_schedules (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    vehicle_id UUID NOT NULL REFERENCES vehicles(id),
    maintenance_type VARCHAR(50) NOT NULL CHECK (maintenance_type IN ('inspection', 'oil_change', 'tire', 'repair', 'other')),
    scheduled_date DATE NOT NULL,
    shop_name VARCHAR(100),
    google_calendar_event_id VARCHAR(100),
    notes TEXT,
    status VARCHAR(20) NOT NULL DEFAULT 'scheduled' CHECK (status IN ('scheduled', 'completed', 'cancelled')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

### 3.12 penalty_codes（ペナルティコードマスタ）

| カラム名    | 型           | NULL | 説明               |
| ----------- | ------------ | ---- | ------------------ |
| id          | UUID         | NO   | 主キー             |
| code        | VARCHAR(20)  | NO   | コード（ユニーク） |
| name        | VARCHAR(100) | NO   | コード名           |
| description | TEXT         | YES  | 説明               |
| sort_order  | INTEGER      | YES  | 表示順             |
| is_active   | BOOLEAN      | NO   | 有効フラグ         |
| created_at  | TIMESTAMPTZ  | NO   | 作成日時           |
| updated_at  | TIMESTAMPTZ  | NO   | 更新日時           |

```sql
CREATE TABLE penalty_codes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(20) NOT NULL UNIQUE,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 初期データ
INSERT INTO penalty_codes (code, name, sort_order) VALUES
    ('SICK', '病欠・体調不良', 1),
    ('LATE', '寝坊・遅刻', 2),
    ('ABSENT_NOTICE', '事前連絡あり欠勤', 3),
    ('OTHER', 'その他', 4);
```

---

### 3.13 penalty_records（ペナルティ記録）

| カラム名            | 型          | NULL | 説明                     |
| ------------------- | ----------- | ---- | ------------------------ |
| id                  | UUID        | NO   | 主キー                   |
| employee_id         | UUID        | NO   | 社員ID（FK）             |
| penalty_code_id     | UUID        | NO   | ペナルティコードID（FK） |
| incident_date       | DATE        | NO   | 発生日                   |
| daily_assignment_id | UUID        | YES  | 関連する配置ID（FK）     |
| description         | TEXT        | YES  | 詳細説明                 |
| notes               | TEXT        | YES  | 備考                     |
| created_at          | TIMESTAMPTZ | NO   | 作成日時                 |
| updated_at          | TIMESTAMPTZ | NO   | 更新日時                 |

```sql
CREATE TABLE penalty_records (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id UUID NOT NULL REFERENCES employees(id),
    penalty_code_id UUID NOT NULL REFERENCES penalty_codes(id),
    incident_date DATE NOT NULL,
    daily_assignment_id UUID REFERENCES daily_assignments(id),
    description TEXT,
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

### 3.14 change_history（変更履歴）

| カラム名   | 型          | NULL | 説明                               |
| ---------- | ----------- | ---- | ---------------------------------- |
| id         | UUID        | NO   | 主キー                             |
| table_name | VARCHAR(50) | NO   | テーブル名                         |
| record_id  | UUID        | NO   | レコードID                         |
| action     | VARCHAR(20) | NO   | アクション（insert/update/delete） |
| changed_by | UUID        | YES  | 変更者（ユーザーID）               |
| old_values | JSONB       | YES  | 変更前の値                         |
| new_values | JSONB       | YES  | 変更後の値                         |
| created_at | TIMESTAMPTZ | NO   | 作成日時                           |

```sql
CREATE TABLE change_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    table_name VARCHAR(50) NOT NULL,
    record_id UUID NOT NULL,
    action VARCHAR(20) NOT NULL CHECK (action IN ('insert', 'update', 'delete')),
    changed_by UUID,
    old_values JSONB,
    new_values JSONB,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_change_history_table_record ON change_history(table_name, record_id);
CREATE INDEX idx_change_history_created_at ON change_history(created_at);
```

---

### 3.15 notification_logs（通知ログ）

| カラム名          | 型           | NULL | 説明                                                                         |
| ----------------- | ------------ | ---- | ---------------------------------------------------------------------------- |
| id                | UUID         | NO   | 主キー                                                                       |
| notification_type | VARCHAR(20)  | NO   | 通知タイプ（line/email/screen）                                              |
| trigger_type      | VARCHAR(50)  | NO   | トリガー（count_change/new_order/self_change/other_change/realtime_change等） |
| recipient         | VARCHAR(200) | YES  | 送信先                                                                       |
| message           | TEXT         | NO   | メッセージ内容                                                               |
| related_table     | VARCHAR(50)  | YES  | 関連テーブル                                                                 |
| related_id        | UUID         | YES  | 関連レコードID                                                               |
| status            | VARCHAR(20)  | NO   | ステータス（pending/sent/failed）                                            |
| sent_at           | TIMESTAMPTZ  | YES  | 送信日時                                                                     |
| error_message     | TEXT         | YES  | エラーメッセージ                                                             |
| snapshot_before   | JSONB        | YES  | 変更前スナップショット（「元に戻す」復元用）                                 |
| snapshot_after    | JSONB        | YES  | 変更後スナップショット（「やっぱり反映」再適用用）                           |
| revert_status     | VARCHAR(20)  | YES  | 復元ステータス（null/reverted/reapplied）                                    |
| created_at        | TIMESTAMPTZ  | NO   | 作成日時                                                                     |

```sql
CREATE TABLE notification_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    notification_type VARCHAR(20) NOT NULL CHECK (notification_type IN ('line', 'email', 'screen')),
    trigger_type VARCHAR(50) NOT NULL,
    recipient VARCHAR(200),
    message TEXT NOT NULL,
    related_table VARCHAR(50),
    related_id UUID,
    status VARCHAR(20) NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'sent', 'failed')),
    sent_at TIMESTAMPTZ,
    error_message TEXT,
    snapshot_before JSONB,
    snapshot_after JSONB,
    revert_status VARCHAR(20) CHECK (revert_status IN ('reverted', 'reapplied')),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 3.16 qa_user_hidden_companies（QA非表示契約先）

Quick Accessでユーザーが非表示にした契約先を管理するテーブル。
Quick Access専用の表示制御であり、受注簿・業務管理計画書の表示には影響しない。
`companies.is_active`（論理削除）とは別概念。

| カラム名   | 型          | NULL | 説明                            |
| ---------- | ----------- | ---- | ------------------------------- |
| id         | UUID        | NO   | 主キー                          |
| user_id    | UUID        | NO   | ユーザーID（FK → auth.users）   |
| company_id | UUID        | NO   | 契約先ID（FK → companies）      |
| created_at | TIMESTAMPTZ | NO   | 非表示にした日時                |

```sql
CREATE TABLE qa_user_hidden_companies (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, company_id)
);
```

---

### 3.17 qa_user_hidden_sites（QA非表示現場）

Quick Accessでユーザーが非表示にした現場を管理するテーブル。
受注実績がある現場は削除不可のため、非表示で対応する。

| カラム名   | 型          | NULL | 説明                          |
| ---------- | ----------- | ---- | ----------------------------- |
| id         | UUID        | NO   | 主キー                        |
| user_id    | UUID        | NO   | ユーザーID（FK → auth.users） |
| site_id    | UUID        | NO   | 現場ID（FK → sites）          |
| created_at | TIMESTAMPTZ | NO   | 非表示にした日時              |

```sql
CREATE TABLE qa_user_hidden_sites (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    site_id UUID NOT NULL REFERENCES sites(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, site_id)
);
```

---

### 3.18 holidays（祝日マスタ）

受注簿グリッドの祝日ハイライト表示に使用する祝日マスタ。
年ごとに祝日を登録し、グリッドの日付列の背景色を変更する。

| カラム名   | 型           | NULL | 説明                   |
| ---------- | ------------ | ---- | ---------------------- |
| id         | UUID         | NO   | 主キー                 |
| date       | DATE         | NO   | 祝日の日付（ユニーク） |
| name       | VARCHAR(50)  | NO   | 祝日名                 |
| is_active  | BOOLEAN      | NO   | 有効フラグ             |
| created_at | TIMESTAMPTZ  | NO   | 作成日時               |
| updated_at | TIMESTAMPTZ  | NO   | 更新日時               |

```sql
CREATE TABLE holidays (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    date DATE NOT NULL UNIQUE,
    name VARCHAR(50) NOT NULL,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_holidays_date ON holidays(date);

-- 初期データ（2026年）
INSERT INTO holidays (date, name) VALUES
    ('2026-01-01', '元日'),
    ('2026-01-12', '成人の日'),
    ('2026-02-11', '建国記念の日'),
    ('2026-02-23', '天皇誕生日'),
    ('2026-03-20', '春分の日'),
    ('2026-04-29', '昭和の日'),
    ('2026-05-03', '憲法記念日'),
    ('2026-05-04', 'みどりの日'),
    ('2026-05-05', 'こどもの日'),
    ('2026-05-06', '振替休日'),
    ('2026-07-20', '海の日'),
    ('2026-08-11', '山の日'),
    ('2026-09-21', '敬老の日'),
    ('2026-09-22', '国民の休日'),
    ('2026-09-23', '秋分の日'),
    ('2026-10-12', 'スポーツの日'),
    ('2026-11-03', '文化の日'),
    ('2026-11-23', '勤労感謝の日');
```

---

### 3.19 user_color_presets（ユーザーカラープリセット）

ユーザーごとのカラー設定プリセットを管理。
グループ会社背景色、区分カラー、シフト色などの画面カラーを個人設定として保存可能。
1ユーザーにつき最大5パターン。

| カラム名   | 型           | NULL | 説明                                       |
| ---------- | ------------ | ---- | ------------------------------------------ |
| id         | UUID         | NO   | 主キー                                     |
| user_id    | UUID         | NO   | ユーザーID（auth.users）                   |
| name       | VARCHAR(50)  | NO   | プリセット名                               |
| colors     | JSONB        | NO   | カラー設定（CSS変数名→値のマップ）         |
| sort_order | INTEGER      | YES  | 表示順                                     |
| created_at | TIMESTAMPTZ  | NO   | 作成日時                                   |
| updated_at | TIMESTAMPTZ  | NO   | 更新日時                                   |

```sql
CREATE TABLE user_color_presets (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    name VARCHAR(50) NOT NULL,
    colors JSONB NOT NULL DEFAULT '{}',
    sort_order INTEGER DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT max_presets_per_user CHECK (sort_order BETWEEN 0 AND 4)
);

CREATE UNIQUE INDEX idx_user_color_presets_user_order ON user_color_presets(user_id, sort_order);
CREATE INDEX idx_user_color_presets_user ON user_color_presets(user_id);
```

JSONB `colors` カラムの例:
```json
{
    "--gc-bg-touo": "#FFFFFF",
    "--gc-bg-nikkei": "#F3F9F6",
    "--gc-bg-zennihon": "#F2F4F8",
    "--cat-bg-facility": "#c6f6d5",
    "--cat-text-facility": "#276749",
    "--shift-bg-day": "#fefcbf",
    "--shift-text-day": "#744210"
}
```

### 3.20 user_company_assignments（ユーザー契約先紐付）

営業担当者のアカウントに担当契約先を紐付けるテーブル。
Quick Access機能で自分の担当契約先を素早く選択するために使用する。
1ユーザーが複数の契約先を担当可能。

| カラム名     | 型          | NULL | 説明                           |
| ------------ | ----------- | ---- | ------------------------------ |
| id           | UUID        | NO   | 主キー                         |
| user_id      | UUID        | NO   | ユーザーID（auth.users）       |
| company_id   | UUID        | NO   | 契約先ID（companies.id FK）    |
| sort_order   | INTEGER     | YES  | 表示順                         |
| created_at   | TIMESTAMPTZ | NO   | 作成日時                       |

```sql
CREATE TABLE user_company_assignments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES auth.users(id),
    company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
    sort_order INTEGER DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, company_id)
);

CREATE INDEX idx_user_company_assignments_user ON user_company_assignments(user_id);
```

---

### 3.21 intergroup_billings（グループ間請求）

配置完了後に、受注元のグループ会社と実際に社員を派遣したグループ会社が異なる場合に発生するグループ間請求を管理するテーブル。
配置データ（assignment_details）から自動検知し、経理が確認・処理する。

| カラム名              | 型          | NULL | 説明                                          |
| --------------------- | ----------- | ---- | --------------------------------------------- |
| id                    | UUID        | NO   | 主キー                                        |
| billing_date          | DATE        | NO   | 請求対象日                                    |
| order_id              | UUID        | NO   | 日毎受注ID（daily_site_orders.id FK）         |
| receiving_company_id  | UUID        | NO   | 受注元グループ会社（group_companies.id FK）    |
| dispatching_company_id| UUID        | NO   | 派遣元グループ会社（group_companies.id FK）    |
| dispatched_count      | INTEGER     | NO   | 派遣人数                                      |
| status                | VARCHAR(20) | NO   | ステータス（unconfirmed/confirmed/billed）     |
| confirmed_by          | UUID        | YES  | 確認者（auth.users FK）                       |
| confirmed_at          | TIMESTAMPTZ | YES  | 確認日時                                      |
| notes                 | TEXT        | YES  | 備考                                          |
| created_at            | TIMESTAMPTZ | NO   | 作成日時                                      |
| updated_at            | TIMESTAMPTZ | NO   | 更新日時                                      |

```sql
CREATE TABLE intergroup_billings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    billing_date DATE NOT NULL,
    order_id UUID NOT NULL REFERENCES daily_site_orders(id) ON DELETE CASCADE,
    receiving_company_id UUID NOT NULL REFERENCES group_companies(id),
    dispatching_company_id UUID NOT NULL REFERENCES group_companies(id),
    dispatched_count INTEGER NOT NULL DEFAULT 0,
    status VARCHAR(20) NOT NULL DEFAULT 'unconfirmed'
        CHECK (status IN ('unconfirmed', 'confirmed', 'billed')),
    confirmed_by UUID REFERENCES auth.users(id),
    confirmed_at TIMESTAMPTZ,
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_intergroup_billings_date ON intergroup_billings(billing_date);
CREATE INDEX idx_intergroup_billings_status ON intergroup_billings(status);
CREATE INDEX idx_intergroup_billings_receiving ON intergroup_billings(receiving_company_id);
CREATE INDEX idx_intergroup_billings_dispatching ON intergroup_billings(dispatching_company_id);
```

---

### 3.22 employee_day_off_requests（休日申請）

社員の休日申請を管理するテーブル。
配置担当者が社員から受け取った紙の申請用紙をもとに入力する。
業務管理計画書では、休日申請済みの日は当該社員を配置不可として扱う。
週間予定表では「休み行」として表示期間内の休日申請済み社員をバッジ表示する。社員軸ビューでは該当セルに「休」マークを表示する。

| カラム名      | 型          | NULL | 説明                      |
| ------------- | ----------- | ---- | ------------------------- |
| id            | UUID        | NO   | 主キー                    |
| employee_id   | UUID        | NO   | 社員ID（employees.id FK） |
| day_off_date  | DATE        | NO   | 休日希望日                |
| registered_by | UUID        | YES  | 登録者（auth.users FK）   |
| notes         | TEXT        | YES  | 備考                      |
| created_at    | TIMESTAMPTZ | NO   | 作成日時                  |
| updated_at    | TIMESTAMPTZ | NO   | 更新日時                  |

※ 同一社員の同一日に重複登録不可（UNIQUE制約）
※ 遠い未来の日付（半年先以上）も登録可能
※ 業務管理計画書の配置時に参照し、配置不可の判定に使用

```sql
CREATE TABLE employee_day_off_requests (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    employee_id UUID NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
    day_off_date DATE NOT NULL,
    registered_by UUID REFERENCES auth.users(id),
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(employee_id, day_off_date)
);

CREATE INDEX idx_employee_day_off_requests_employee ON employee_day_off_requests(employee_id);
CREATE INDEX idx_employee_day_off_requests_date ON employee_day_off_requests(day_off_date);
```

---

### 3.23 support_partners（協力業者マスタ）

応援を依頼する社外の警備会社（協力業者）を管理するマスタ。
週間予定表の応援予約行からD&Dで現場セルへ配置して運用する。

**後付け紐付けをサポート**するため、`formal_name`/`address`/`representative_*`/`phone` は NULL 許容。週間予定表で略称のみ登録 → 後でマスタ画面から完全情報を入力 → 注文書発行可、という段階的運用が可能。

| カラム名             | 型           | NULL | 説明                                                        |
| -------------------- | ------------ | ---- | ----------------------------------------------------------- |
| id                   | UUID         | NO   | 主キー                                                      |
| group_company_id     | UUID         | NO   | 依頼元グループ会社ID（group_companies FK）                 |
| short_name           | VARCHAR(50)  | NO   | 略称（バッジ表示用、例: `A社①`）                            |
| formal_name          | VARCHAR(200) | YES  | 正式名称（注文書用、例: `株式会社志'ｓCorporation`）        |
| postal_code          | VARCHAR(8)   | YES  | 郵便番号                                                    |
| address              | TEXT         | YES  | 住所                                                        |
| representative_title | VARCHAR(50)  | YES  | 代表者役職（例: 代表取締役）                                |
| representative_name  | VARCHAR(50)  | YES  | 代表者名                                                    |
| phone                | VARCHAR(20)  | YES  | 電話番号                                                    |
| email                | VARCHAR(100) | YES  | メールアドレス                                              |
| notes                | TEXT         | YES  | 備考                                                        |
| is_master_complete   | BOOLEAN      | NO   | マスタ情報完備フラグ（注文書発行の可否判定に使用）          |
| is_active            | BOOLEAN      | NO   | 有効フラグ                                                  |
| created_at           | TIMESTAMPTZ  | NO   | 作成日時                                                    |
| updated_at           | TIMESTAMPTZ  | NO   | 更新日時                                                    |

※ `is_master_complete` は `formal_name`, `address`, `representative_title`, `representative_name` が揃った時点で true にする（トリガー or アプリ層で管理）
※ 略称は同一GC内でユニーク制約

```sql
CREATE TABLE support_partners (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    short_name VARCHAR(50) NOT NULL,
    formal_name VARCHAR(200),
    postal_code VARCHAR(8),
    address TEXT,
    representative_title VARCHAR(50),
    representative_name VARCHAR(50),
    phone VARCHAR(20),
    email VARCHAR(100),
    notes TEXT,
    is_master_complete BOOLEAN NOT NULL DEFAULT false,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(group_company_id, short_name)
);

CREATE INDEX idx_support_partners_gc ON support_partners(group_company_id);
CREATE INDEX idx_support_partners_active ON support_partners(is_active);
-- 略称オートコンプリート用
CREATE INDEX idx_support_partners_short_name_trgm ON support_partners USING GIN (short_name gin_trgm_ops);
```

---

### 3.24 support_reservations（協力業者予約）

応援予約として事前交渉した協力業者の日別・シフト別人数を保持する。
週間予定表の応援予約行に「残3」のように表示され、現場セルへの配置で残数が消費される（`support_assignments` と組み合わせて残数を算出）。

| カラム名        | 型          | NULL | 説明                                            |
| --------------- | ----------- | ---- | ----------------------------------------------- |
| id              | UUID        | NO   | 主キー                                          |
| partner_id      | UUID        | NO   | 協力業者ID（support_partners FK）               |
| reserved_date   | DATE        | NO   | 予約対象日                                      |
| shift_type      | VARCHAR(10) | NO   | シフト種別（day/night/flex）                    |
| reserved_count  | INTEGER     | NO   | 予約人数（下限0、上限なし）                     |
| notes           | TEXT        | YES  | 備考                                            |
| created_at      | TIMESTAMPTZ | NO   | 作成日時                                        |
| updated_at      | TIMESTAMPTZ | NO   | 更新日時                                        |

※ `shift_type`: `day`=昼専用、`night`=夜専用、`flex`=昼夜どちらでも使える予約
※ 同一（partner_id, reserved_date, shift_type）に対して UNIQUE
※ 残数 = `reserved_count` − 当該 partner/date/shift の `support_assignments.assigned_count` 合計

```sql
CREATE TABLE support_reservations (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    partner_id UUID NOT NULL REFERENCES support_partners(id) ON DELETE CASCADE,
    reserved_date DATE NOT NULL,
    shift_type VARCHAR(10) NOT NULL CHECK (shift_type IN ('day', 'night', 'flex')),
    reserved_count INTEGER NOT NULL DEFAULT 0 CHECK (reserved_count >= 0),
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(partner_id, reserved_date, shift_type)
);

CREATE INDEX idx_support_reservations_date ON support_reservations(reserved_date);
CREATE INDEX idx_support_reservations_partner ON support_reservations(partner_id);
```

---

### 3.25 support_assignments（協力業者配置）

応援予約行から現場セルへD&D配置した結果を記録する。
`support_reservations` の予約残から消費される。同一（partner_id, 日付, 現場, シフト）に対して複数人配置可（`assigned_count` で表現）。

| カラム名          | 型          | NULL | 説明                                                          |
| ----------------- | ----------- | ---- | ------------------------------------------------------------- |
| id                | UUID        | NO   | 主キー                                                        |
| partner_id        | UUID        | NO   | 協力業者ID（support_partners FK）                             |
| assigned_date     | DATE        | NO   | 配置日                                                        |
| shift_type        | VARCHAR(10) | NO   | 配置シフト（day/night）※flex予約を消費した場合も配置側は確定  |
| source_shift_type | VARCHAR(10) | NO   | 消費元予約シフト（day/night/flex）                            |
| site_id           | UUID        | NO   | 配置先現場ID（sites FK）                                      |
| assigned_count    | INTEGER     | NO   | 配置人数（同セル複数配置対応。デフォルト1）                   |
| created_at        | TIMESTAMPTZ | NO   | 作成日時                                                      |
| updated_at        | TIMESTAMPTZ | NO   | 更新日時                                                      |

※ `shift_type` は配置セル側のシフト。flex予約から昼セルへ配置した場合は `shift_type='day'`, `source_shift_type='flex'`
※ 同日内は別行・別シフトへD&D自由、別日移動は同シフト維持（源泉シフト維持）という運用ルールに従う

```sql
CREATE TABLE support_assignments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    partner_id UUID NOT NULL REFERENCES support_partners(id) ON DELETE CASCADE,
    assigned_date DATE NOT NULL,
    shift_type VARCHAR(10) NOT NULL CHECK (shift_type IN ('day', 'night')),
    source_shift_type VARCHAR(10) NOT NULL CHECK (source_shift_type IN ('day', 'night', 'flex')),
    site_id UUID NOT NULL REFERENCES sites(id),
    assigned_count INTEGER NOT NULL DEFAULT 1 CHECK (assigned_count > 0),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_support_assignments_date ON support_assignments(assigned_date);
CREATE INDEX idx_support_assignments_partner ON support_assignments(partner_id);
CREATE INDEX idx_support_assignments_site ON support_assignments(site_id);
```

---

### 3.26 price_note_templates（料金特記定型文マスタ）

注文書の「警備料金」欄に付記する定型文のマスタ。チェックボックス複数選択＋自由追記欄と組み合わせて使用する。

| カラム名               | 型          | NULL | 説明                                              |
| ---------------------- | ----------- | ---- | ------------------------------------------------- |
| id                     | UUID        | NO   | 主キー                                            |
| body                   | TEXT        | NO   | 定型文本文                                        |
| is_default_selected    | BOOLEAN     | NO   | 新規注文書作成時に既定でON                        |
| sort_order             | INTEGER     | YES  | 表示順                                            |
| is_active              | BOOLEAN     | NO   | 有効フラグ                                        |
| created_at             | TIMESTAMPTZ | NO   | 作成日時                                          |
| updated_at             | TIMESTAMPTZ | NO   | 更新日時                                          |

```sql
CREATE TABLE price_note_templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    body TEXT NOT NULL,
    is_default_selected BOOLEAN NOT NULL DEFAULT false,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 初期データ
INSERT INTO price_note_templates (body, is_default_selected, sort_order) VALUES
    ('上記金額は税抜きとなります。', true, 1),
    ('上記金額は交通費・ETC代・ガソリン代・宿泊費等の係る諸経費を全て含めた金額になります。', true, 2);
```

---

### 3.27 special_note_templates（その他特記定型文マスタ）

注文書の「その他」欄に付記する定型文のマスタ。警備業法遵守・管理監督関係・資機材条件などの定型条項を管理する。

| カラム名               | 型          | NULL | 説明                                      |
| ---------------------- | ----------- | ---- | ----------------------------------------- |
| id                     | UUID        | NO   | 主キー                                    |
| body                   | TEXT        | NO   | 定型文本文                                |
| is_default_selected    | BOOLEAN     | NO   | 新規注文書作成時に既定でON                |
| sort_order             | INTEGER     | YES  | 表示順                                    |
| is_active              | BOOLEAN     | NO   | 有効フラグ                                |
| created_at             | TIMESTAMPTZ | NO   | 作成日時                                  |
| updated_at             | TIMESTAMPTZ | NO   | 更新日時                                  |

```sql
CREATE TABLE special_note_templates (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    body TEXT NOT NULL,
    is_default_selected BOOLEAN NOT NULL DEFAULT false,
    sort_order INTEGER DEFAULT 0,
    is_active BOOLEAN NOT NULL DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 初期データ
INSERT INTO special_note_templates (body, is_default_selected, sort_order) VALUES
    ('警備業法を遵守し、乙の警備員は乙の管理監督の下業務を行う事', true, 1),
    ('警備員が使用するその他の資機材は自社の物を使用する事', true, 2);
```

---

### 3.28 purchase_orders（注文書）

協力業者向けの警備業務注文書を管理するテーブル。
**発行時点のマスタ情報をスナップショット保存**し、後日のマスタ変更による過去書類の改変を防ぐ。
同一注文書から「注文書」「注文請書」の2書類を出力する（請書は `purchase_order_acceptances` でステータス管理）。

| カラム名                         | 型           | NULL | 説明                                                              |
| -------------------------------- | ------------ | ---- | ----------------------------------------------------------------- |
| id                               | UUID         | NO   | 主キー                                                            |
| order_number                     | VARCHAR(30)  | NO   | 注文書番号（ユニーク、例: `PO-2026-0001`）                        |
| issue_date                       | DATE         | NO   | 発行日                                                            |
| **三者情報**                     |              |      |                                                                   |
| group_company_id                 | UUID         | NO   | 甲：委託者GC（group_companies FK）                                |
| partner_id                       | UUID         | NO   | 乙：受託者・協力業者（support_partners FK）                       |
| client_id                        | UUID         | NO   | 丙：発注者・契約先（companies FK）                                |
| **対象**                         |              |      |                                                                   |
| site_id                          | UUID         | NO   | 対象現場ID（sites FK）                                            |
| target_work_name_snapshot        | VARCHAR(200) | NO   | 対象工事名（現場名スナップショット）                              |
| security_location                | TEXT         | NO   | 警備実施場所                                                      |
| **配置時間**                     |              |      |                                                                   |
| start_time                       | TIME         | NO   | 配置開始時刻                                                      |
| end_time                         | TIME         | NO   | 配置終了時刻                                                      |
| **配置人員**                     |              |      |                                                                   |
| guard_count                      | INTEGER      | NO   | 配置人数（合計）                                                  |
| certification_note               | TEXT         | YES  | 検定記載文（例: 「内1名以上は交通誘導警備業務2級検定保持者」）    |
| **警備内容・期間**               |              |      |                                                                   |
| security_content                 | TEXT         | NO   | 警備内容（フリー記入）                                            |
| period_start                     | DATE         | NO   | 実施期間開始                                                      |
| period_end                       | DATE         | NO   | 実施期間終了                                                      |
| period_note                      | TEXT         | YES  | 実施期間注記（例: 「変更の可能性有り」）                          |
| **料金**                         |              |      |                                                                   |
| daily_unit_price                 | INTEGER      | NO   | 日額単価（税抜、円）                                              |
| price_note_template_ids          | UUID[]       | YES  | 適用する料金定型文IDの配列（price_note_templates.id）             |
| price_note_free                  | TEXT         | YES  | 料金欄の自由追記                                                  |
| **支払条件**                     |              |      |                                                                   |
| payment_terms                    | TEXT         | NO   | 支払条件（例: 「月末締め 翌月末支払い」）                         |
| payment_method                   | VARCHAR(50)  | YES  | 支払方法（例: 「現金振込」）                                      |
| **連絡先（甲スナップショット）** |              |      |                                                                   |
| issuer_company_name_snapshot     | VARCHAR(200) | NO   | 発行元会社名スナップショット                                      |
| issuer_address_snapshot          | TEXT         | NO   | 発行元住所スナップショット                                        |
| issuer_representative_snapshot   | VARCHAR(100) | NO   | 発行元代表者（役職＋氏名）スナップショット                        |
| issuer_phone_snapshot            | VARCHAR(20)  | NO   | 発行元電話スナップショット                                        |
| **受託者（乙スナップショット）** |              |      |                                                                   |
| partner_company_name_snapshot    | VARCHAR(200) | NO   | 受託者会社名スナップショット                                      |
| partner_address_snapshot         | TEXT         | NO   | 受託者住所スナップショット                                        |
| partner_representative_snapshot  | VARCHAR(100) | NO   | 受託者代表者（役職＋氏名）スナップショット                        |
| **発注者（丙スナップショット）** |              |      |                                                                   |
| client_formal_name_snapshot      | VARCHAR(200) | NO   | 発注者正式名称スナップショット                                    |
| **その他**                       |              |      |                                                                   |
| special_note_template_ids        | UUID[]       | YES  | 適用するその他特記定型文IDの配列（special_note_templates.id）     |
| special_note_free                | TEXT         | YES  | その他特記の自由追記                                              |
| **ステータス・集計**             |              |      |                                                                   |
| status                           | VARCHAR(20)  | NO   | draft/issued/accepted/closed                                      |
| subtotal                         | INTEGER      | YES  | 小計（日額単価×人数×日数、税抜）                                  |
| tax                              | INTEGER      | YES  | 消費税額                                                          |
| total                            | INTEGER      | YES  | 合計（税込）                                                      |
| created_at                       | TIMESTAMPTZ  | NO   | 作成日時                                                          |
| updated_at                       | TIMESTAMPTZ  | NO   | 更新日時                                                          |

※ スナップショット方式により、マスタ（group_companies/support_partners/companies）を後日変更しても過去の注文書内容は不変
※ `status`: `draft`=下書き、`issued`=発行済、`accepted`=請書受領済、`closed`=完了

```sql
CREATE TABLE purchase_orders (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    order_number VARCHAR(30) NOT NULL UNIQUE,
    issue_date DATE NOT NULL,
    group_company_id UUID NOT NULL REFERENCES group_companies(id),
    partner_id UUID NOT NULL REFERENCES support_partners(id),
    client_id UUID NOT NULL REFERENCES companies(id),
    site_id UUID NOT NULL REFERENCES sites(id),
    target_work_name_snapshot VARCHAR(200) NOT NULL,
    security_location TEXT NOT NULL,
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    guard_count INTEGER NOT NULL CHECK (guard_count > 0),
    certification_note TEXT,
    security_content TEXT NOT NULL,
    period_start DATE NOT NULL,
    period_end DATE NOT NULL,
    period_note TEXT,
    daily_unit_price INTEGER NOT NULL CHECK (daily_unit_price >= 0),
    price_note_template_ids UUID[],
    price_note_free TEXT,
    payment_terms TEXT NOT NULL,
    payment_method VARCHAR(50),
    issuer_company_name_snapshot VARCHAR(200) NOT NULL,
    issuer_address_snapshot TEXT NOT NULL,
    issuer_representative_snapshot VARCHAR(100) NOT NULL,
    issuer_phone_snapshot VARCHAR(20) NOT NULL,
    partner_company_name_snapshot VARCHAR(200) NOT NULL,
    partner_address_snapshot TEXT NOT NULL,
    partner_representative_snapshot VARCHAR(100) NOT NULL,
    client_formal_name_snapshot VARCHAR(200) NOT NULL,
    special_note_template_ids UUID[],
    special_note_free TEXT,
    status VARCHAR(20) NOT NULL DEFAULT 'draft'
        CHECK (status IN ('draft', 'issued', 'accepted', 'closed')),
    subtotal INTEGER,
    tax INTEGER,
    total INTEGER,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_purchase_orders_issue_date ON purchase_orders(issue_date);
CREATE INDEX idx_purchase_orders_partner ON purchase_orders(partner_id);
CREATE INDEX idx_purchase_orders_site ON purchase_orders(site_id);
CREATE INDEX idx_purchase_orders_status ON purchase_orders(status);
```

---

### 3.29 purchase_order_certifications（注文書必要検定）

注文書における「検定資格者配置」の要否と必要資格・人数を記録する。
1つの注文書に複数種別の検定要件を指定可（例: 交通誘導2級×1名かつ雑踏2級×1名）。
参照先は `license_types` の category='certification' のレコード。

| カラム名          | 型          | NULL | 説明                                          |
| ----------------- | ----------- | ---- | --------------------------------------------- |
| id                | UUID        | NO   | 主キー                                        |
| purchase_order_id | UUID        | NO   | 注文書ID（purchase_orders FK）                |
| license_type_id   | UUID        | NO   | 検定種別ID（license_types FK）                |
| required_count    | INTEGER     | NO   | 必要人数                                      |
| created_at        | TIMESTAMPTZ | NO   | 作成日時                                      |

```sql
CREATE TABLE purchase_order_certifications (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    purchase_order_id UUID NOT NULL REFERENCES purchase_orders(id) ON DELETE CASCADE,
    license_type_id UUID NOT NULL REFERENCES license_types(id),
    required_count INTEGER NOT NULL CHECK (required_count > 0),
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(purchase_order_id, license_type_id)
);
```

---

### 3.30 purchase_order_acceptances（注文請書）

発行済み注文書に対する受託者からの「注文請書」の受領を管理する。
レイアウトは注文書と同一内容で甲乙位置反転、印紙貼付欄あり。

| カラム名          | 型          | NULL | 説明                                          |
| ----------------- | ----------- | ---- | --------------------------------------------- |
| id                | UUID        | NO   | 主キー                                        |
| purchase_order_id | UUID        | NO   | 元注文書ID（purchase_orders FK、1:1）         |
| accepted_date     | DATE        | YES  | 請書受領日                                    |
| stamp_placed      | BOOLEAN     | NO   | 印紙貼付済フラグ                              |
| document_url      | TEXT        | YES  | 請書PDF/画像URL（Supabase Storage等）         |
| notes             | TEXT        | YES  | 備考                                          |
| created_at        | TIMESTAMPTZ | NO   | 作成日時                                      |
| updated_at        | TIMESTAMPTZ | NO   | 更新日時                                      |

```sql
CREATE TABLE purchase_order_acceptances (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    purchase_order_id UUID NOT NULL REFERENCES purchase_orders(id) ON DELETE CASCADE UNIQUE,
    accepted_date DATE,
    stamp_placed BOOLEAN NOT NULL DEFAULT false,
    document_url TEXT,
    notes TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## 4. インデックス設計

```sql
-- 日毎受注の検索用（テーブル定義側で作成済み）
-- CREATE INDEX idx_daily_site_orders_date ON daily_site_orders(order_date);
-- CREATE INDEX idx_daily_site_orders_site ON daily_site_orders(site_id);
-- CREATE INDEX idx_daily_site_orders_status ON daily_site_orders(status);

-- 受注変更履歴の検索用（テーブル定義側で作成済み）
-- CREATE INDEX idx_order_change_history_order ON order_change_history(order_id);
-- CREATE INDEX idx_order_change_history_changed_by ON order_change_history(changed_by);
-- CREATE INDEX idx_order_change_history_changed_at ON order_change_history(changed_at);

-- 配置詳細の検索用
CREATE INDEX idx_assignment_details_employee ON assignment_details(employee_id);

-- 送迎関係の検索用
CREATE INDEX idx_transport_relations_date ON transport_relations(assignment_date);

-- メンテナンス予定の検索用
CREATE INDEX idx_maintenance_schedules_date ON maintenance_schedules(scheduled_date);
CREATE INDEX idx_maintenance_schedules_vehicle ON maintenance_schedules(vehicle_id);

-- ペナルティ記録の検索用
CREATE INDEX idx_penalty_records_employee ON penalty_records(employee_id);
CREATE INDEX idx_penalty_records_date ON penalty_records(incident_date);
```

---

## 5. RLS（Row Level Security）ポリシー

Supabaseで認証されたユーザーのみアクセス可能にする設定。

```sql
-- 全テーブルにRLSを有効化
ALTER TABLE companies ENABLE ROW LEVEL SECURITY;
ALTER TABLE sites ENABLE ROW LEVEL SECURITY;
ALTER TABLE employees ENABLE ROW LEVEL SECURITY;
-- ... 他のテーブルも同様

-- 認証済みユーザーは全データにアクセス可能（社内システムのため）
CREATE POLICY "Authenticated users can read all data" ON companies
    FOR SELECT USING (auth.role() = 'authenticated');

CREATE POLICY "Authenticated users can insert data" ON companies
    FOR INSERT WITH CHECK (auth.role() = 'authenticated');

CREATE POLICY "Authenticated users can update data" ON companies
    FOR UPDATE USING (auth.role() = 'authenticated');

-- ... 他のテーブルも同様
```

---

## 6. 既存Excelデータとのマッピング

### 6.1 受注簿との対応

| Excel項目             | DBテーブル                     | DBカラム                                                          |
| --------------------- | ------------------------------ | ----------------------------------------------------------------- |
| 本社/支店             | employees → organization_units | org_unit_id (FK) → organization_units.name                        |
| 契約先カテゴリ        | sites → site_categories        | category_id (FK)                                                  |
| 昼/夜                 | daily_site_orders              | shift_type                                                        |
| 契約先名              | companies                      | name                                                              |
| 業務名                | sites                          | name（NULLなら個別業務）                                          |
| 業務詳細              | daily_order_sub_items          | label, value（動的ペア）                                          |
| 計画書業務名          | daily_site_orders              | daily_task_name（自動生成）                                       |
| 配置先連番            | daily_site_orders              | placement_seq（0始まり、複数配置先時に使用）                      |
| 人数                  | daily_site_orders              | required_count                                                    |
| バッジ（区分子バッジ）| daily_site_orders              | badge_child_ids（子バッジID配列）、badge_gc_map（孫バッジマップ） |
| 信頼度                | daily_site_orders              | confidence（confirmed/tentative_high/tentative_low）              |
| 時間（コメント）      | daily_site_orders              | start_time, end_time                                              |
| 集合場所（コメント）  | daily_site_orders              | meeting_place                                                     |
| 集合時間（コメント）  | daily_site_orders              | meeting_time                                                      |
| 配置（コメント）      | assignment_details             | employee_id                                                       |
| 連絡タイプ            | assignment_details             | contact_type（company/direct/line等）                             |
| 車両（配置）          | daily_assignments              | vehicle_ids（UUID配列）                                           |
| ETCカード（配置）     | daily_assignments              | etc_card_ids（UUID配列）                                          |
| 備考（表示アイテム）  | daily_assignments              | display_items（JSONB: ラベル+値+色）                              |
| 現場監督              | daily_site_orders              | supervisor_name                                                   |
| 現場監督連絡先        | daily_site_orders              | supervisor_tel                                                    |
| 連絡（コメント）      | daily_site_orders              | contact_info                                                      |
| 備考（コメント）      | daily_site_orders              | notes                                                             |

---

## 更新履歴

| 日付       | 内容                                                                                         |
| ---------- | -------------------------------------------------------------------------------------------- |
| 2026-02-03 | 初版作成                                                                                     |
| 2026-02-04 | companies: address, phone, email, contact_person削除                                        |
| 2026-02-04 | sites: address削除、hierarchy_depth追加、shift_typeをday/nightのみに変更                     |
| 2026-02-04 | employees: has_car, has_motorcycle, has_bicycle, can_night_shift, can_distant, work_restrictions, email削除 |
| 2026-02-04 | site_sub_items: 新規追加（現場の階層構造）                                                   |
| 2026-02-04 | employee_conflicts: 新規追加（社員配置制約）                                                 |
| 2026-02-04 | sites: shift_type削除（日毎配置で管理するため不要）                                          |
| 2026-02-04 | site_maps: テーブル削除（日毎配置で直接管理）                                                |
| 2026-02-04 | sites: default_map_url削除                                                                   |
| 2026-02-04 | companies: short_name削除                                                                    |
| 2026-02-04 | employees: name_kana, phone, notes削除                                                       |
| 2026-02-04 | daily_assignments: map_url追加（現場地図URL）                                                |
| 2026-02-04 | site_categories: description削除                                                             |
| 2026-02-04 | license_types: category必須化、初期データ追加（検定/運転免許/スキル）                        |
| 2026-02-04 | transport_method: train削除                                                                  |
| 2026-02-04 | transport_relations: pickup_time削除                                                         |
| 2026-02-04 | vehicles: color, capacity, ownership削除、owner追加                                          |
| 2026-02-04 | vehicle_assignments: テーブル削除（日毎配置で管理）                                          |
| 2026-02-04 | daily_assignments: vehicle_ids追加（配置車両ID配列）                                         |
| 2026-02-04 | assignment_details: vehicle_id削除（車両は日毎配置レベルで管理）                             |
| 2026-02-04 | vehicles: vehicle_type削除、short_name・oil_change_distance追加                              |
| 2026-02-04 | maintenance_schedules: completed_date・cost削除                                              |
| 2026-02-04 | penalty_codes: severity削除                                                                  |
| 2026-02-05 | assignment_details: transport_method, equipment, notes削除                                   |
| 2026-02-05 | sites: default_meeting_place → default_contact（連絡）に変更                                 |
| 2026-02-05 | daily_assignments: meeting_place → contact（連絡）に変更                                     |
| 2026-02-21 | 設計方針にフロントエンド非依存（React先行・Excel VBA後続）の記載を追加                       |
| 2026-02-24 | group_companies: 新規追加（グループ会社マスタ、badge_color含む）                               |
| 2026-02-24 | sites: group_company_id追加（現場単位でグループ会社に紐付け、同一元請けが複数グループ可）    |
| 2026-02-24 | user_color_presets: 新規追加（ユーザーごとのカラー設定プリセット、JSONB、最大5パターン）     |
| 2026-02-27 | group_companies: short_name削除（ボーダー方式で不要に）                                      |
| 2026-02-27 | daily_assignments → daily_site_orders（受注）+ daily_assignments（配置）に分離               |
| 2026-02-27 | daily_site_orders: 新規追加（営業入力の受注情報、status/version/created_by/updated_by含む）  |
| 2026-02-27 | order_change_history: 新規追加（受注変更履歴、変更前後記録、ロールバック対応）                |
| 2026-03-03 | site_sub_items: label列追加（項目ラベルのカスタマイズ対応）                                  |
| 2026-03-03 | sites: name列をNULL許可に変更（個別業務で親業務名なしを許容）                                |
| 2026-03-03 | daily_site_orders: meeting_place追加（集合場所）                                             |
| 2026-03-03 | daily_site_orders: daily_task_name追加（計画書業務名の自動生成値を保持）                     |
| 2026-03-03 | daily_order_sub_items: 新規追加（日毎受注の業務詳細、動的ラベル+値ペア）                     |
| 2026-03-03 | Excelマッピング更新（業務詳細・計画書業務名・集合場所を追加、現場名を削除）                  |
| 2026-03-04 | daily_site_orders: confidence追加（信頼度: confirmed/tentative_high/tentative_low）           |
| 2026-03-04 | confidence自動確定ルール追記（過去日付ルール・配置確定ルール）                                |
| 2026-03-04 | Excelマッピング更新（信頼度を追加）                                                          |
| 2026-03-04 | category_badges: 新規追加（区分バッジマスタ、区分に紐づく子バッジ定義）                      |
| 2026-03-04 | daily_site_orders: badge_child_ids追加（選択済み子バッジIDのUUID配列）                        |
| 2026-03-04 | Excelマッピング更新（バッジを追加）                                                          |
| 2026-03-04 | assignment_details: contact_type追加（連絡タイプ: company/direct/line/pickup/op/other）       |
| 2026-03-04 | holidays: 新規追加（祝日マスタ、2026年初期データ18件）                                        |
| 2026-03-04 | daily_assignments: display_items追加（車両・送迎等のグリッド表示用JSONB）                     |
| 2026-03-04 | Excelマッピング更新（連絡タイプ・車両送迎表示を追加）                                        |
| 2026-03-05 | daily_site_orders: supervisor_name, supervisor_tel追加（現場監督名・連絡先）                 |
| 2026-03-05 | sites: supervisor_candidates追加（現場監督候補リスト、JSONB）                                |
| 2026-03-05 | Excelマッピング更新（現場監督・現場監督連絡先を追加）                                        |
| 2026-03-05 | category_badges: parent_badge_id, level追加（自己参照FKで孫バッジ階層対応）                  |
| 2026-03-06 | employees: group_company_id追加（社員のグループ会社所属、休日申請管理のフィルタ等に使用）     |
| 2026-03-06 | employee_day_off_requests: 新規追加（休日申請テーブル、社員+日付のUNIQUE制約）               |
| 2026-03-15 | GCフィルタ: 複数選択＋プリセット方式。デフォルトはgroup_company_idから自動判定               |
| 2026-03-18 | daily_site_orders: placement_seq, placement_name追加（複数配置先対応）                        |
| 2026-03-18 | daily_site_orders: UNIQUE制約を(order_date, site_id, shift_type, placement_seq)に変更         |
| 2026-03-18 | Excelマッピング更新（配置先連番・配置場所名を追加）                                          |
| 2026-03-19 | daily_site_orders: placement_name削除（配置場所名を廃止）                                    |
| 2026-03-19 | sites: supervisor_candidates削除（現場監督候補は過去入力実績から動的取得に変更）              |
| 2026-03-19 | Excelマッピング更新（配置場所名を削除）                                                      |
| 2026-03-19 | map_urlをdaily_assignmentsからdaily_site_ordersに移動（受注段階で地図URL入力可能に）         |
| 2026-03-26 | departments: 新規追加（部署マスタ、グループ会社ごとに部署を定義、施設課・交通課等）           |
| 2026-03-26 | group_companies: short_name再追加（表示用略称、タブ・テーブル列等の狭いUI向け）               |
| 2026-03-26 | employees: department VARCHAR(50) → department_id UUID FK に変更（部署マスタ参照化）          |
| 2026-03-26 | Excelマッピング更新（本社/支店の参照先をdepartmentsテーブルに変更）                           |
| 2026-03-26 | display_items: 用途を「車両列」→「備考列」に変更、base（ベースカラー）プロパティ追加、車両はvehicle_idsで独立管理 |
| 2026-03-26 | etc_cards: 新規追加（ETCカードマスタ、グループ会社別管理、車両紐付けオプション）                                |
| 2026-03-26 | daily_assignments: etc_card_ids追加（配置ETCカードID配列）、GINインデックス追加                                 |
| 2026-03-26 | Excelマッピング更新（車両・ETCカード・備考の配置管理を明確化）                                                  |
| 2026-04-06 | daily_site_orders: map_url(TEXT)→maps(JSONB)に変更（複数地図エントリ対応）                                      |
| 2026-04-06 | companies: pg_trgm拡張によるGINインデックス追加（曖昧検索バリデーション用）                                     |
| 2026-04-06 | qa_user_hidden_companies: 新規追加（QA専用、ユーザー別の契約先非表示管理）                                      |
| 2026-04-06 | qa_user_hidden_sites: 新規追加（QA専用、ユーザー別の現場非表示管理）                                            |
| 2026-04-06 | セクション番号整理（3.16-3.22）                                                                                |
| 2026-04-07 | daily_site_orders: badge_gc_map(JSONB)追加（孫バッジマップ、3階層バッジ対応）                        |
| 2026-04-09 | departments削除 → org_level_types + organization_unitsに置換（組織階層の自由定義対応）       |
| 2026-04-09 | org_level_types: 新規追加（グループ会社ごとの階層種別定義、最大5階層）                       |
| 2026-04-09 | organization_units: 新規追加（自己参照ツリーによる組織ノード、隣接リスト方式）               |
| 2026-04-09 | org_unit_tree: 再帰ビュー追加（WITH RECURSIVEによる階層展開）                                |
| 2026-04-09 | employees: department_id → org_unit_idに変更（organization_units参照、任意階層に所属可能）   |
| 2026-04-09 | Excelマッピング更新（本社/支店の参照先をorganization_unitsに変更）                           |
| 2026-04-14 | maintenance_schedules: 週間予定表「修理/点検行」との連携記述を追加                            |
| 2026-04-14 | employee_day_off_requests: 週間予定表「休み行」との連携記述を追加                             |
| 2026-04-14 | vehicles: owner(VARCHAR)廃止→group_company_id(UUID FK)に置換（GC別フィルタ対応）             |
| 2026-04-14 | org_level_types初期データ: 東央を課のみに簡略化、全日本の拠点→課を追加                       |
| 2026-04-18 | group_companies: postal_code/address/representative_title/representative_name/phone追加（注文書の甲情報源） |
| 2026-04-18 | companies: formal_name必須追加（注文書の丙正式名称用、括弧株展開等に使用）                      |
| 2026-04-18 | license_types: 初期データに空港保安1/2級・核燃料物質等危険物運搬1/2級・貴重品運搬1/2級を追加（検定計12種） |
| 2026-04-18 | support_partners: 新規追加（3.23）。協力業者マスタ、略称のみで先行登録可能（formal_name等NULL許容）、is_master_complete フラグ、GC内略称UNIQUE、trigram GINインデックス |
| 2026-04-18 | support_reservations: 新規追加（3.24）。協力業者予約、日別・シフト別（day/night/flex）の人数保持、(partner_id,date,shift)UNIQUE |
| 2026-04-18 | support_assignments: 新規追加（3.25）。協力業者配置、shift_type（配置先）とsource_shift_type（消費元予約）を分離、assigned_count で同セル複数配置対応 |
| 2026-04-18 | price_note_templates: 新規追加（3.26）。注文書料金特記の定型文マスタ、初期データ2件 |
| 2026-04-18 | special_note_templates: 新規追加（3.27）。注文書その他特記の定型文マスタ、初期データ2件（警備業法遵守・自社資機材使用） |
| 2026-04-18 | purchase_orders: 新規追加（3.28）。協力業者向け警備業務注文書、三者（甲/乙/丙）スナップショット方式、配置時間・検定記載・料金特記・支払条件・status（draft/issued/accepted/closed） |
| 2026-04-18 | purchase_order_certifications: 新規追加（3.29）。注文書の検定要件複数指定（license_types参照） |
| 2026-04-18 | purchase_order_acceptances: 新規追加（3.30）。注文請書の受領管理、印紙貼付フラグ、PDF/画像URL |
| 2026-04-14 | organization_units初期データ: 東央を施設課/交通課に変更、全日本の本社→課＋愛媛を追加         |
