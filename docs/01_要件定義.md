# 受注管理システム 要件定義書

## 1. プロジェクト概要

### 1.1 システム名
受注管理システム (Order Management System)

### 1.2 目的
現在Excel（VBA）で運用している受注簿・業務管理計画書をWebシステム化し、
データの一元管理、検索性向上、複数契約先の統合管理を実現する。

### 1.3 開発方針

- **Phase 1（先行開発）**: React + Node.js によるWebアプリケーションとして業務管理計画書UIを構築
- **Phase 2（後続開発）**: Excel VBA によるUIを構築（既存の受注簿・計画書との連携）
- Webアプリケーション（React）を正（プライマリ）とし、Excel UIはサブシステムとして位置づける

### 1.4 技術スタック

| 項目 | 技術 | 備考 |
| ---- | ---- | ---- |
| フロントエンド | Next.js (React) | **先行開発（プライマリUI）** |
| バックエンド | Supabase (PostgreSQL + Auth + Storage) | |
| 認証 | Supabase Auth (メール・パスワード) | |
| 外部連携 | Googleカレンダー API | |
| Excel連携 | VBA + Supabase REST API | **後続開発（セカンダリUI）** |
| ホスティング | 未定 (Vercel推奨) | |

---

## 2. システム要件

### 2.1 利用者

- **対象**: 社内管理者（営業担当者・配置担当者・経理）
- **同時利用者数**: 少人数（1〜数名）

#### 2.1.1 ユーザーロール

| ロール | 主な業務 | 主な利用端末 | 主な画面 |
|--------|----------|-------------|---------|
| 営業担当者 | 契約先からの受注を受け、受注簿に入力 | モバイル（主）、PC | Quick Access、受注簿 |
| 配置担当者 | 受注に対して社員を配置し、日々の業務計画を作成。営業担当の代理入力も行う | PC（主） | 業務管理計画書、受注簿 |
| 経理 | 電票との付き合わせ、グループ間請求の確認 | PC | 経理画面 |

#### 2.1.2 業務フロー概要

```text
契約先 → [電話] → 営業担当者 → [受注簿に入力（Quick Access）]
    ↓
配置担当者 → [社員を配置（業務管理計画書）] → [社員へ通知]
    ↓
社員 → [現場で業務] → [電票にサイン取得]
    ↓
経理 → [電票と受注簿の照合] → [グループ間請求の確認・処理]
```

### 2.2 動作環境
- **PC**: Chrome, Edge, Firefox（最新版）
- **モバイル**: レスポンシブ対応（iOS Safari, Android Chrome）

### 2.3 既存システムとの関係

- **React Webアプリ（プライマリ）**: 業務管理計画書の主要UIをReact + Node.jsで先行開発
- **並行運用**: React Webアプリ完成後も、当面はExcelシステムと並行して運用
- **データ連携**: 受注簿と業務管理計画書は同一DBを共有し、リアルタイム自動同期
- **Excel UI（後続開発）**: React版の完成後に、Excel VBA版のUIを構築しDBと連携

---

## 3. 機能要件

### 3.1 受注簿管理

#### 3.1.1 データ項目
| カテゴリ | 項目例 |
|----------|--------|
| 契約先情報 | 契約先名、現場名、住所、連絡先 |
| 人員配置 | 配置人数、勤務時間、シフトパターン |
| 金額情報 | 単価、請求額、支払条件 |
| ステータス | 受注状況、変更履歴 |

#### 3.1.2 信頼度（受注確度）

受注データの確度を3段階で管理し、合計表示に反映する。

| 信頼度 | 説明 | グリッド表示 |
|--------|------|-------------|
| 確定 | 確定した受注 | 通常表示（不透明度100%） |
| 予定（信頼:高） | 高確率の予定 | 文字色を薄く表示（不透明度50%） |
| 予定（信頼:低） | 低確率の予定 | 文字色をさらに薄く表示（不透明度25%） |

**自動確定ルール**:

| ルール | 条件 | 動作 |
|--------|------|------|
| 過去日付ルール | 受注対象日が昨日以前 | 自動的に「確定」に変更 |
| 配置確定ルール | 業務管理計画書で社員が配置された | 自動的に「確定」に変更 |

**合計表示（最小～最大）**:

行合計・日別合計・総合計を「最小値～最大値」形式で表示する。

| 値 | 算出方法 |
|----|----------|
| 最小値 | 信頼度が「確定」のデータのみ合計 |
| 最大値 | 全データ（予定含む）の合計 |

※ 最小値と最大値が同じ場合は単一の数値で表示

#### 3.1.3 行情報編集

受注簿グリッドの固定列（会社・区分・昼夜・契約先名・業務名）をクリックすると行情報編集モーダルが開く。

| 項目 | 操作方法 |
|------|----------|
| 会社 | チップ選択（単一選択） |
| 区分 | チップ選択（単一選択）＋ 新規区分追加ボタン |
| 昼夜 | チップ選択（単一選択） |
| 契約先名 | テキスト入力 |
| 業務名 | テキスト入力（空欄で個別業務） |

※ 区分の新規追加時は、バッジ定義にも自動的に同期される

#### 3.1.4 バッジ（区分子バッジ）

セル編集モーダルで、区分に紐づく子バッジをチップ選択できる。
区分名が親バッジ（読み取り専用）、その下に子バッジ候補が表示される。

**構造**:

| 要素 | 説明 |
|------|------|
| 親バッジ | 区分名をそのまま表示（読み取り専用） |
| 子バッジ | 区分ごとに定義されたバッジ候補（複数選択可） |

**初期データ（子バッジ）**:

| 区分 | 子バッジ |
|------|----------|
| 高速 | 車線規制、路肩規制、ブース規制、保安員 |
| 交通 | 片側交互、通行止め |
| 施設 | （なし、追加可能） |
| イベント | （なし、追加可能） |
| 応援交通 | （なし、追加可能） |

**操作**:

| 操作 | 説明 |
|------|------|
| 子バッジ選択 | チップをクリックしてON/OFF切替（複数選択可） |
| 子バッジ追加 | 「+ バッジ追加」ボタンからpromptで新規バッジ名を入力 |
| グリッド表示 | セル内に選択済みバッジ名を「・」区切りで表示 |

※ 親バッジ（区分）は行情報編集モーダルの区分変更に連動して自動決定される
※ 区分変更時、子バッジ選択はリセットされる
※ DB上では `daily_site_orders.badge_child_ids UUID[]` に選択済み子バッジIDを保存

#### 3.1.5 機能一覧

- [ ] 受注データ一覧表示
- [ ] 受注データ検索・フィルタリング
  - 契約先での絞り込み
  - 現場・契約先での絞り込み
  - 期間での絞り込み
- [ ] 受注データ登録・編集・削除
- [ ] 受注データソート（複数条件）
- [ ] 信頼度の選択（確定/予定（高）/予定（低））
- [ ] 信頼度に応じたグリッド文字色の変更
- [ ] 信頼度の自動確定（過去日付ルール・配置確定ルール）
- [ ] 合計の最小～最大表示（確定のみ～全データ）
- [ ] 行情報編集モーダル（チップ選択UI）
- [ ] 区分の動的追加とバッジ定義への自動同期
- [ ] セル編集モーダルでの子バッジチップ選択（複数選択可）
- [ ] 子バッジの動的追加（promptで新規バッジ名入力）
- [ ] グリッドセル内での選択済みバッジ名表示（・区切り）
- [ ] 祝日ハイライト表示（トグル切替）
- [ ] 夜勤行の視覚的区別
- [ ] バッジ表示トグル（グリッド・カレンダーモーダル両方でON/OFF切替）
- [ ] 列表示/非表示トグル（固定列の表示制御）
- [ ] カレンダー入力モーダル（行単位の月間カレンダー編集、詳細は3.1.10）

#### 3.1.6 祝日ハイライト

受注簿グリッドの日付列で土日・祝日に背景色を適用する。ツールバーのトグルボタンでON/OFF可能。

| 種別 | 背景色 |
|------|--------|
| 土曜日 | 薄い青系 |
| 日曜日 | 薄い赤系 |
| 祝日 | 薄い金色系 |

※ 祝日データは `holidays` テーブルで管理
※ ヘッダー行はデータ行より濃い色で表示

#### 3.1.7 夜勤行の表示

受注簿・業務管理計画書で夜勤データを視覚的に区別する。

| 画面 | 表示方法 |
|------|----------|
| 受注簿（月間グリッド） | セル内文字色を赤系（#B5524D）に変更 |
| 業務管理計画書（日次） | 行背景は白のまま、契約先名・現場名・集合時間・業務時間・人数・配置社員名の文字色を赤色（#D05050）に変更。監督バッジ・集合列の連絡バッジも赤系統に変更 |

※ 2画面で異なるスタイルを使用する理由: 受注簿はセル単位で密度が高いため文字色で区別、業務管理計画書は行単位で主要項目の文字色を赤にして区別

#### 3.1.8 区分デフォルトラベル

セル編集モーダルの「業務詳細」で、区分に応じたデフォルトのラベル名を自動設定する。

| 区分 | デフォルトラベル |
|------|----------------|
| 高速 | 工事名、作業内容 |
| 交通 | 作業内容 |
| 施設 | 業務内容 |

※ ラベルはデフォルト値として使用。ユーザーが自由に変更可能
※ DB上は `site_sub_items.label` で管理（マスタレベル）、`daily_order_sub_items.label` で日毎にカスタマイズ可能

#### 3.1.9 複数配置先（マルチサイト）

同一行（同一契約先・同一業務）の同日セル内で、複数の配置場所に人員を分けて受注できる機能。

**ユースケース**: 同一契約先から同日に合計5名の受注があり、現場Aに3名・現場Bに2名のように、集合場所や時間が異なる複数の物理的な現場に分かれるケース。

| 項目 | 説明 |
|------|------|
| 配置先の独立性 | 各配置先は全項目が独立（人数・時間・監督・集合場所・バッジ・備考等） |
| グリッド表示 | セル内に各配置先の人数を縦並びで表示。それぞれ個別にクリック可能 |
| 配置先の追加 | セル編集モーダル内の「+ 配置先を追加」ボタンで新規配置先を作成 |
| 配置先の切替 | モーダル内の◀▶ナビゲーションで配置先を切り替え |
| 配置先の削除 | 「この配置先を削除」ボタンで個別に削除可能 |
| 合計計算 | 行合計・日別合計は全配置先の人数を合算 |
| ツールチップ | 複数配置先の場合、各配置先の情報を区別して表示 |

※ DB上は `daily_site_orders` の `placement_seq`（配置先連番）で同一日・同一現場・同一シフト内の複数配置先を区別

#### 3.1.10 カレンダー入力モーダル

受注簿グリッドの各行にあるカレンダーアイコンから、その行の月間カレンダービューを開いて編集する機能。月単位でセルデータを俯瞰しながら個別セルを編集できる。

**画面構成**:

| エリア | 内容 |
|--------|------|
| ヘッダー | メタ情報（会社名・契約先/業務名・区分/昼夜）、月ナビゲーション（◀▶）、Undo/Redoボタン、バッジ表示トグル、閉じるボタン |
| カレンダーグリッド | 月間カレンダー。各セルに人数バッジを表示。バッジ表示ON時は子バッジ名も表示 |
| 編集アコーディオン | セルクリックで展開。セル編集モーダルと同じフォーム（人数・信頼度・業務詳細・時間・監督・集合・地図・備考）を表示 |
| フッター | 削除・カレンダー表示・保存ボタン |

**操作**:

| 操作 | 動作 |
|------|------|
| セルクリック | 編集アコーディオンを展開し、該当セルのデータを読み込み。カレンダーは選択した週のみ表示に切替 |
| 別セルクリック | 現在の編集内容を自動保存し、新しいセルの編集に切替 |
| 空セルクリック | 新規エントリを作成して編集開始 |
| アコーディオンヘッダークリック | 編集パネルの展開/折りたたみを切替 |
| モーダル閉じ | 未保存の編集内容を自動保存してからモーダルを閉じる |

**Undo/Redo**:

| 項目 | 説明 |
|------|------|
| カレンダー専用スタック | カレンダーモーダル内の操作用。保存・削除ごとにスナップショットを記録 |
| アコーディオン状態維持 | Undo/Redo実行時、編集アコーディオンが開いていればデータを再読み込みして開いたまま維持。セルが存在しなくなった場合のみ閉じる |
| グローバルUndo連携 | モーダルを閉じる際、セッション中に変更があればカレンダー操作全体を1エントリとしてツールバーのグローバルUndoスタックに追加 |

**機能一覧**:

- [ ] 行単位の月間カレンダー表示（人数バッジ付き）
- [ ] カレンダーセルクリックによるアコーディオン編集
- [ ] セル切替時の自動保存
- [ ] カレンダー専用Undo/Redo（アコーディオン状態維持）
- [ ] グローバルUndoとの連携（セッション単位）
- [ ] 月ナビゲーション（前月/次月切替、セルデータキャッシュ）
- [ ] カレンダー内バッジ表示トグル
- [ ] 単体エントリ削除時の自動保存（アコーディオン折りたたみ＋データ反映）

### 3.2 業務管理計画書

#### 3.2.1 データ項目
| カテゴリ | 項目例 |
|----------|--------|
| 日毎配置 | 日付、現場、配置人員、勤務時間 |
| 月間スケジュール | 月単位の配置計画 |

#### 3.2.2 機能一覧
- [ ] カレンダー形式での表示（Excelライクなグリッド）
- [ ] 日毎の人員配置登録・編集
- [ ] 月間スケジュール一覧
- [ ] 受注簿とのリアルタイム自動同期（同一DB共有）

#### 3.2.3 ドラッグ&ドロップ配置（UI/UX）
| 操作 | 説明 |
|------|------|
| リスト→現場 | 社員リストから現場枠へドラッグ&ドロップで配置 |
| 現場→現場 | 現場Aから現場Bへ社員を移動 |

- [ ] 社員リストからのドラッグ&ドロップ配置
- [ ] 現場間での社員移動
- [ ] 直感的な操作による配置作業の効率化

### 3.3 配置者への追加情報

#### 3.3.1 連絡タイプ管理

配置された社員ごとの連絡方法を管理する。業務管理計画書グリッドで社員名の横にバッジとして表示。

| タイプ | 説明 |
|--------|------|
| 会社 | 会社経由で連絡 |
| 直 | 社員に直接連絡 |
| LINE | LINEで連絡 |
| 迎え | 送迎あり（他社員が迎えに行く） |
| OP | オペレーター経由 |
| その他 | カスタム連絡タイプ（ユーザーが追加可能） |

- [ ] 社員配置時の連絡タイプ選択
- [ ] カスタム連絡タイプの追加
- [ ] グリッド上でのバッジ表示（色分け）

#### 3.3.2 移動手段管理
| 項目 | 説明 |
|------|------|
| 移動手段種別 | 社有車、マイカー、バイク、自転車、送迎、その他 |
| 送迎関係 | 誰が誰を迎えに行くかの関係を記録（迎え場所を指定） |

※ 電車は選択肢から除外

#### 3.3.3 連続勤務管理
| 項目 | 説明 |
|------|------|
| パターン | 夜→昼、昼→夜、夜→昼→夜、その他カスタム |
| 表示方法 | アイコン・バッジで視覚的に表示 |

#### 3.3.4 その他情報
- [ ] 備品・装備（持参する備品の記録）
- [ ] 資格・免許（必要資格の確認）

### 3.4 契約先管理

#### 3.4.1 対象契約先

- 東央警備
- Nikkeiホールディングス
- 全日本エンタープライズ
- その他（拡張可能）

#### 3.4.2 機能一覧
- [ ] 契約先マスタ管理
- [ ] 契約先ごとのデータフィルタリング
- [ ] 全契約先の統合ビュー

### 3.5 現場管理

#### データ項目
| カテゴリ | 項目例 |
|----------|--------|
| 基本情報 | 現場名、略称（出勤簿表示用）、区分 |
| デフォルト設定 | 開始時間、終了時間、集合場所、集合時間 |
| 階層構造 | 親（現場）→ 子（工事名等）→ 孫（作業内容等）最大3階層 |

#### 3.5.1 現場の階層構造
| 項目 | 説明 |
|------|------|
| 最大階層 | 3階層（親→子→孫） |
| 連結表示 | スペース区切りで連結（例：「24-1234 〇〇橋補修工事 点検作業」） |
| 配置対象 | 親（現場）に対してのみ配置 |

#### 3.5.2 現場地図機能
| 項目 | 説明 |
|------|------|
| 用途 | 日々変更される現場の地図を管理 |
| データソース | GoogleマップURL |
| 管理方法 | 日毎受注（daily_site_orders）に直接URLを保持 |

- [ ] 現場マスタ管理（略称・階層含む）
- [ ] 現場子項目の登録・管理
- [ ] 日毎配置での地図URL設定
- [ ] GoogleマップURLからの地図表示

### 3.6 社員マスタ管理

#### 3.6.1 データ項目
| カテゴリ | 項目例 |
|----------|--------|
| 基本情報 | 社員コード、氏名、**略称（ドロップダウン表示用）**、所属グループ会社、所属部署 |
| 資格・スキル | 検定（交通誘導等）、運転免許、社内スキル |

#### 3.6.1.1 部署マスタ

社員の所属部署をマスタで管理する。グループ会社ごとに部署を定義し、業務管理計画書の社員リスト部署別タブ等に使用する。

| 項目 | 説明 |
|------|------|
| 所属単位 | グループ会社ごとに部署を定義（例: 東央警備の施設課、NikkeiHDの交通課） |
| DB管理 | departments テーブルで管理。employees.department_id で参照 |

**初期データ**:

| グループ会社 | 部署 |
|-------------|------|
| 東央警備 | 施設課、交通課 |
| Nikkeiホールディングス | 施設課、交通課 |

#### 3.6.2 資格・免許・スキル
| カテゴリ | 説明 | 例 |
|----------|------|-----|
| 検定 | 公的な警備業務検定 | 交通誘導警備1級/2級、施設警備、雑踏警備 |
| 運転免許 | 自動車運転免許 | 普通(MT/AT)、準中型(MT/AT)、中型(MT/AT) |
| スキル | 社内認定スキル | 標識車、標識車（外販）、2トン車 |

#### 3.6.3 機能一覧
- [ ] 社員情報の登録・編集・削除（略称含む）
- [ ] 部署マスタの管理（グループ会社ごとの部署定義）
- [ ] 社員への部署割り当て
- [ ] 資格・免許・スキルの付与（資格番号、取得日、有効期限）
- [ ] ドロップダウンでの略称表示

#### 3.6.4 社員配置制約（相性管理）
| 項目 | 説明 |
|------|------|
| 用途 | 同じ現場に配置すべきでない社員の組み合わせを管理 |
| 警告表示 | 配置時に注意喚起アイコンを表示（配置はブロックしない） |

### 3.7 車両管理（新規）

#### 3.7.1 車両マスタ
| 項目 | 説明 |
|------|------|
| 基本情報 | 車両コード、ナンバー、略称、車種名、所有者 |
| メンテナンス | オイル交換距離（km）、車検満了日 |
| 稼働状況 | 使用可能・使用不可のステータス |

#### 3.7.2 配車管理
| 項目 | 説明 |
|------|------|
| 配置レベル | 日毎配置（daily_assignments）単位で車両・ETCカードを配置 |
| 複数配置 | 1つの現場に複数の車両・ETCカードを配置可能 |
| シフト帯制約 | 同一車両・ETCカードは同じシフト帯（昼 or 夜）に1現場のみ配置可能。異なるシフト帯には配置可能 |
| 行間移動 | 配置済みの車両・ETCタグを別の行にドラッグ&ドロップで移動可能（社員と同じ操作感） |

- [ ] 日毎配置での車両指定（複数可）
- [ ] 日毎配置でのETCカード指定（複数可）
- [ ] シフト帯重複チェック（車両・ETC共通）
- [ ] 稼働状況の表示
- [ ] Googleカレンダー連携（メンテナンス予定）

#### 3.7.2.1 ETCカード管理（新規）

ETCカードを車両とは独立して管理し、業務管理計画書の車両列に配置する。

| 項目 | 説明 |
|------|------|
| 管理単位 | グループ会社ごとにETCカードを管理 |
| 東央警備 | アルファベットラベル方式（ETC-A 〜 ETC-F） |
| Nikkei HD | 車番紐付け方式（ETC + ナンバー下4桁、例: ETC く7521） |
| 配置方法 | 車両タブ内のETCリストからドラッグ&ドロップで配置 |
| 配置位置 | 車両列の下部（上部: 車両、下部: ETC）|

- [ ] ETCカードマスタ管理
- [ ] 車両タブ内のETCリスト表示（グループ会社別）
- [ ] ETCカードのドラッグ&ドロップ配置

#### 3.7.4 車両・備考グリッド表示

業務管理計画書のグリッドで、車両と備考情報をそれぞれ独立した列で表示する。

**車両列**（上部: 車両ゾーン、下部: ETCゾーンの2段構成）:

| 項目 | 説明 |
|------|------|
| 表示形式 | 上部に車両ナンバータグ、下部にETCカードタグを表示 |
| 配置方法 | 車両リスト・ETCリストからドラッグ&ドロップ。列内のどこにドロップしても自動振り分け |
| 行間移動 | 配置済みタグを別行にドラッグ&ドロップで移動可能 |
| シフト帯制約 | 同一車両・ETCは同じシフト帯に1現場のみ |
| 複数配置 | 1現場に複数の車両・ETCを配置可能 |
| 未設定時 | 車両ゾーン:「車両」、ETCゾーン:「ETC」のプレースホルダー表示 |

**備考列（送迎・備考アイテム）**:

| 項目 | 説明 |
|------|------|
| 表示形式 | ラベル＋値のカラータグを縦並びで表示 |
| ベース色 | アイテムごとにベース色1色を指定し、背景色（rgba 0.12）と文字色（暗色）を自動生成 |
| 文字色オーバーライド | 自動生成された文字色を手動で変更可能 |
| プリセット | 送迎（茶系）、集合場所（ティール系）、備考（グレー系）の3種 |
| カスタムアイテム | 任意のラベル・値・色のアイテムを自由追加可能 |
| 並び順 | ドラッグ＆ドロップで変更可能 |

**車両選択時の動作:**
- 車両マスタからセレクトボックスで選択
- マスタにない車両は自由入力で追加可能（マスタにも登録される）

- [ ] 車両・送迎タグのグリッド表示
- [ ] タグごとの背景色・文字色カスタマイズ
- [ ] ドラッグ＆ドロップによる並び替え
- [ ] カスタムアイテムの自由追加

#### 3.7.3 メンテナンス管理
| 種別 | 説明 |
|------|------|
| inspection | 車検 |
| oil_change | オイル交換 |
| tire | タイヤ |
| repair | 修理 |
| other | その他 |

- [ ] メンテナンス予定の登録（予定日、整備店名）
- [ ] ステータス管理（予定/完了/キャンセル）

### 3.8 ペナルティ・勤怠トラブル管理（新規）

#### 3.8.1 ペナルティコード（初期データ）
| コード | 名称 |
|--------|------|
| SICK | 病欠・体調不良 |
| LATE | 寝坊・遅刻 |
| ABSENT_NOTICE | 事前連絡あり欠勤 |
| OTHER | その他 |

※ カスタムコードの追加可能

#### 3.8.2 機能一覧
- [ ] ペナルティコードのマスタ管理
- [ ] ペナルティの記録（社員・日付・配置との紐付け）
- [ ] 社員ごとの集計・レポート

### 3.9 インポート・エクスポート

#### 3.9.1 インポート
- [ ] Excelファイル（.xlsx）からのデータ取込
- [ ] CSVファイルからのデータ取込

#### 3.9.2 エクスポート
- [ ] Excel形式（.xlsx）での出力
- [ ] CSV形式での出力

#### 3.9.3 受注簿リアルタイム同期

受注簿と業務管理計画書は同一データベースを共有し、データ変更はリアルタイムで自動同期される。手動での「データ取得」「転記」操作は不要。

| 項目 | 内容 |
|------|------|
| 同期方式 | 同一DB共有によるオンタイム自動同期 |
| 同期方向 | 双方向（受注簿での変更 ↔ 業務管理計画書での変更） |
| 反映タイミング | データ変更時に即座に全画面へ反映 |
| 技術 | Supabase Realtime（PostgreSQL LISTEN/NOTIFY） |

- [ ] 同一DBによる受注簿・業務管理計画書のリアルタイム同期
- [ ] Supabase Realtimeによる変更の即時反映
- [ ] 他ユーザーの変更を自動受信・画面更新

#### 3.9.4 入力時あいまい一致バリデーション

新規現場追加時に、既存データとの表記揺れを検知し、重複登録や誤入力を未然に防止する。

**正規化処理:**
- 全角 → 半角変換
- 半角カナ → 全角カナ変換
- スペース正規化（連続スペース除去、全角/半角統一）
- 法人格の除去（「株式会社」「(株)」等）
- 括弧の統一

**類似度判定:**

| 類似度 | 判定 | アクション |
|--------|------|------------|
| 100%（完全一致） | 重複 | 登録をブロック、既存データを提示 |
| 80%以上 | 高類似 | 警告表示、既存候補を提示して確認を求める |
| 50%以上 | 中類似 | 候補リストを表示（参考情報） |
| 50%未満 | 新規 | そのまま登録可能 |

- [ ] 契約先名・現場名の正規化処理
- [ ] レーベンシュタイン距離ベースの類似度判定
- [ ] 新規入力時の重複候補表示UI
- [ ] 重複検知時の警告・確認ダイアログ

### 3.10 Excel連携（VBA経由）【後続開発】

※ React Webアプリの完成後に開発する。

#### 3.10.1 機能

- [ ] DBからExcelへのデータ読み込み
- [ ] ExcelからDBへのデータ書き込み
- [ ] Supabase REST API経由での通信

### 3.11 認証・セキュリティ

- [ ] メール・パスワード認証
- [ ] セッション管理
- [ ] パスワードリセット機能
- [ ] ロールベースのアクセス制御（営業担当者・配置担当者・経理）

### 3.12 Quick Access（受注クイック入力）

営業担当者が電話で受注を受けた際に、素早く受注簿へ入力するための機能。

#### 3.12.1 概要

- 営業担当のアカウントに担当契約先を紐付け
- 電話受注時に素早く契約先を選択し、業務名・人数・内容を入力
- モバイルファーストのUI設計（PC版も対応）

#### 3.12.2 画面構成

| 要素 | 内容 |
| ---- | ---- |
| トップ | 担当する全契約先一覧を更新順（新着順）で表示 |
| タブ | 「すべて」「イベント」「高速」「交通」「施設」等のカテゴリタブ |
| タブ紐付け | タブは受注簿の区分（badgeDefinitions）と連動 |
| 入力フォーム | 契約先選択後に業務名・人数・日付・時間・備考を入力 |

#### 3.12.3 機能一覧

- [ ] アカウント別の担当契約先紐付け
- [ ] 契約先クイック選択（更新順表示）
- [ ] カテゴリタブによるフィルタリング（区分と連動）
- [ ] 受注クイック入力フォーム（業務名・人数・日付・時間等）
- [ ] モバイル最適化レイアウト
- [ ] PC版レイアウト（配置担当者の代理入力にも対応）

### 3.13 グループ間請求管理

グループ会社間での社員派遣に伴う請求を管理する機能。

#### 3.13.1 概要

配置完了後、受注したグループ会社とは異なるグループ会社の社員が派遣された場合に、グループ間請求が発生する。

```text
例: 全日本エンタープライズが5名の現場を受注
  → うち2名を東央警備の社員が担当
  → 東央警備は2名分を全日本エンタープライズに請求
```

#### 3.13.2 タイミング

- 受注簿への入力時点ではグループ間請求は発生しない
- 配置担当者が社員を割り振った後に、派遣元と受注元が異なる場合に発生
- 経理が確認・処理を行う

#### 3.13.3 機能一覧

- [ ] 配置データからグループ間請求の自動検知（受注元 ≠ 派遣元）
- [ ] グループ間請求一覧画面（経理向け）
- [ ] 電票との付き合わせ確認
- [ ] 請求ステータス管理（未確認/確認済み/請求済み）
- [ ] 期間別の請求集計

### 3.14 アラート・通知

#### 3.14.1 画面通知
- [ ] 画面上での通知表示
- [ ] 未配置警告
- [ ] 車検切れアラート

#### 3.14.2 LINE通知（LINEグループ連携）
| 項目 | 内容 |
|------|------|
| 通知先 | LINEグループ |
| トリガー | 人数変更時、新規受注時 |
| 技術 | LINE Notify または LINE Messaging API |

- [ ] LINEグループへの通知送信
- [ ] 人数変更時の自動通知
- [ ] 新規受注時の自動通知

#### 3.14.3 リアルタイム変更通知

他ユーザーによるデータ変更をリアルタイムで受信し、画面上で即座に通知・反映する。

**トースト通知:**
- 変更発生時にトースト（ポップアップ）で即座に通知
- タイプ別表示: 追加（緑）、変更（黄）、削除（赤）
- 変更者名・現場名を表示、一定時間後に自動消去

**変更通知モーダル:**

| 項目 | 内容 |
| ---- | ---- |
| タブ構成 | 「最新の変更」タブ + 「変更履歴」タブ |
| カード表示 | タイプ別バッジ、変更者、時刻、現場名、差分詳細（旧値→新値） |
| ステータス | 承認済バッジ / 取消済バッジ |
| アクション | 「元に戻す」ボタン / 「やっぱり反映」ボタン |
| 権限 | 全ユーザーが元に戻す・やっぱり反映を使用可能 |
| コンフリクト | 自分が編集中のデータが他者に変更された場合、警告バナーを表示 |

**ワークフロー:**
1. 他ユーザーがデータ変更 → 自画面にリアルタイム反映（オンタイム同期）
2. 変更通知モーダルで差分を確認可能
3. 「元に戻す」で変更を取消 → テーブルが変更前の状態に復元
4. 「やっぱり反映」で取消を撤回 → テーブルに再度変更を適用

- [ ] Supabase Realtimeによる変更のリアルタイム受信
- [ ] トースト通知（タイプ別: 追加/変更/削除）
- [ ] 変更通知モーダル（最新の変更タブ + 変更履歴タブ）
- [ ] カード形式の差分表示（旧値→新値）
- [ ] 承認済/取消済ステータスバッジ
- [ ] 「元に戻す」機能（変更の取消・データ復元）
- [ ] 「やっぱり反映」機能（取消の撤回・再適用）
- [ ] コンフリクト検知バナー表示

#### 3.14.4 将来拡張
- [ ] メール通知

### 3.15 履歴管理

#### 3.15.1 受注変更履歴・ロールバック

**業務背景**: 営業担当者が契約先から受注情報（何日にどの現場で何名必要か）を受け、システムに入力する。
営業担当者が端末を使えない場合は配置担当者に連絡して代理入力する。
別の営業担当者が同じ契約先から誤った伝言を受けて上書きしたり、聞き間違いによる誤キャンセルが発生する。
これらの誤操作から復旧するため、変更履歴の記録とロールバック機能が必要。

| 項目 | 内容 |
|------|------|
| 追跡対象 | 日毎受注（daily_site_orders）の全カラム変更 |
| 記録内容 | 変更カラム名、変更前の値、変更後の値 |
| 変更理由 | 任意入力（例：「契約先からの連絡で人数変更」「キャンセル連絡あり」） |
| ロールバック | 任意の過去バージョンに戻せる |
| ロールバック権限 | 営業担当者・配置担当者・管理者 |
| 競合対策 | 楽観的ロック（バージョン番号照合） + スマートマージ |

**受注ステータス管理**:

| ステータス | 説明                 |
|------------|----------------------|
| active     | 有効（通常状態）     |
| cancelled  | その日のキャンセル   |

**競合解決（スマートマージ）**:

同時編集でバージョン不一致を検知した場合、変更項目の重複有無に応じて解決方法を提示する。

| ケース       | 条件                         | 解決方法                                       |
|--------------|------------------------------|------------------------------------------------|
| A: 非重複    | 自分と他者の変更項目が異なる | 両方の変更をマージして保存（自動マージ候補）   |
| B: 重複あり  | 同じ項目を双方が変更         | 項目ごとにどちらの値を採用するか選択ダイアログ |

- [ ] 受注情報変更時の自動履歴記録（変更カラム・変更前後の値・変更者・日時）
- [ ] 変更理由の入力フォーム（任意）
- [ ] 変更履歴一覧の表示（受注ごと）
- [ ] 任意の過去バージョンへのロールバック機能
- [ ] ロールバック時の確認ダイアログ（差分プレビュー表示）
- [ ] 楽観的ロックによる同時編集の競合検知
- [ ] 競合時のスマートマージ（非重複項目の自動統合提案）
- [ ] 競合時の手動選択ダイアログ（重複項目の値選択）
- [ ] 受注ステータス管理（active/cancelled）

#### 3.15.2 汎用変更履歴

- [ ] 変更履歴の記録（全テーブル対象の汎用ログ）
- [ ] 履歴の参照・検索

### 3.16 データベース構造（テーブル相関関係）

本システムのデータベースは、以下の主要なデータグループで構成される。

#### 3.16.1 契約先・現場系

**契約先（companies）** は複数の **現場（sites）** を保有する。各現場は **区分（site_categories）** によって「施設」「イベント」「交通」「高速」などに分類される。

現場は最大3階層の階層構造を持つことができ、**現場子項目（site_sub_items）** で「工事名」「作業内容」などを管理する。例えば「24-1234（PJ番号）→ 〇〇橋補修工事 → 点検作業」のように連結表示される。

#### 3.16.2 社員・資格系

**社員（employees）** は複数の **資格・スキル（employee_licenses）** を保有できる。資格は **資格種別マスタ（license_types）** で「検定」「運転免許」「スキル」の3カテゴリに分類される。

また、人間関係（相性）により同じ現場に配置すべきでない社員の組み合わせを **社員配置制約（employee_conflicts）** で管理する。この制約は配置をブロックするものではなく、警告アイコンで注意喚起を行う。

#### 3.16.3 受注・配置系（中核）

**日毎受注（daily_site_orders）** は営業担当者が契約先から受けた受注情報を管理する。「どの現場に」「いつ」「何名必要か」「時間帯」を記録する。1つの現場に対して昼・夜の2シフトが設定可能。楽観的ロック（version）による同時編集の競合検知に対応。

**日毎配置（daily_assignments）** は配置担当者が管理する配置計画。日毎受注に1:1で紐づき、配置車両（vehicle_ids）を保持する。

**配置詳細（assignment_details）** は日毎配置に紐づき、「誰を」「どの移動手段で」配置するかの詳細を管理する。1つの日毎配置に対して複数の社員を配置できる。

#### 3.16.4 送迎関係

**送迎関係（transport_relations）** は「誰が誰を迎えに行くか」を記録する。運転者と同乗者の関係を日付ごとに管理し、迎え場所も指定できる。

#### 3.16.5 車両・メンテナンス系

**車両（vehicles）** は社有車の情報を管理する。車両は日毎配置（daily_assignments.vehicle_ids）に紐付けられ、1つの現場に複数の車両を配置することも可能。

**メンテナンス予定（maintenance_schedules）** は車検・オイル交換・タイヤ交換などの予定を管理する。Googleカレンダーとの連携も可能。

#### 3.16.6 ペナルティ系

**ペナルティコード（penalty_codes）** は「病欠」「遅刻」などの種別を定義する。**ペナルティ記録（penalty_records）** は社員ごとの発生履歴を記録し、関連する配置との紐付けも可能。

#### 3.16.7 受注変更履歴系

**受注変更履歴（order_change_history）** は日毎受注（daily_site_orders）の全変更を記録する。変更されたカラム名・変更前の値・変更後の値を保持し、任意の過去バージョンへのロールバックを可能にする。楽観的ロック（daily_site_orders.version）により同時編集の競合を検知する。

#### 3.16.8 ログ系

**変更履歴（change_history）** は全テーブルのデータ変更（登録・更新・削除）を自動記録する。変更前後の値をJSON形式で保持。

**通知ログ（notification_logs）** はLINE通知などの送信履歴を記録する。

#### 3.16.9 連続勤務（自動検知）

#### 3.16.10 グループ間請求系

**ユーザー契約先紐付（user_company_assignments）** は営業担当者のアカウントと担当契約先の紐付けを管理する。Quick Access機能で自分の担当契約先を素早く選択するために使用する。

**グループ間請求（intergroup_billings）** は配置完了後に発生するグループ会社間の請求を管理する。受注元のグループ会社と、実際に社員を派遣したグループ会社が異なる場合に自動検知し、経理が確認・処理する。

連続勤務はテーブルで管理せず、配置データから自動的に検知する。「昼→夜」「夜→昼」「昼→夜→昼」「夜→昼→夜」のパターンを検知し、アイコン・バッジと背景色で視覚的に表示する。

#### 3.16.11 休日申請系

**休日申請（employee_day_off_requests）** は社員の休日希望を管理する。社員が紙の申請用紙に記入した希望日を、配置担当者がシステムに入力する。業務管理計画書では、休日申請済みの日は当該社員を配置不可として扱う。

### 3.17 休日申請管理

#### 3.17.1 概要

社員の休日申請を一元管理する機能。社員が紙の休日申請用紙に希望日を記入し、配置担当者がシステムに入力する。東央警備・Nikkeiホールディングス・全日本エンタープライズの全グループ会社の社員を一括管理する。

#### 3.17.2 業務フロー

```text
社員 → [紙の休日申請用紙に希望日を記入] → 配置担当者
    ↓
配置担当者 → [システムに入力（カレンダーUIで社員をドラッグ&ドロップ）]
    ↓
業務管理計画書 → [休日申請済みの日は配置不可として表示]
```

※ 社員はシステムにアクセスしない。システム操作は配置担当者のみが行う。

#### 3.17.3 画面構成

| エリア | 内容 |
|--------|------|
| ヘッダー | 月選択、グループ会社フィルタ |
| メインエリア | カレンダーグリッド（日付=列、行=登録済み社員の休日データ） |
| 右側パネル | 社員一覧（グループ会社別、ドラッグ元） |

**カレンダーグリッド**: 業務管理計画書と同様のカレンダー形式。日付が列ヘッダー、各行は登録された社員の休日データを表示。

**社員パネル**: 右側に全社員の一覧を表示。グループ会社別にフィルタ可能。社員名をドラッグして日付セルにドロップすることで休日を登録する。

#### 3.17.4 機能一覧

- [ ] カレンダーグリッド表示（月単位、日付=列）
- [ ] 右側社員パネルからのドラッグ&ドロップによる休日登録
- [ ] グループ会社フィルタ（東央警備/Nikkei/全日本エンタープライズ）
- [ ] 休日申請の削除
- [ ] 遠い未来の日付への対応（半年先以上の申請も登録可能）
- [ ] 業務管理計画書との連携（休日申請済み=配置不可として表示）

#### 3.17.5 配置連携

| 項目 | 説明 |
|------|------|
| 配置ブロック | 休日申請が登録された日は、当該社員を業務管理計画書で配置不可とする |
| 表示方法 | 業務管理計画書の社員リストで、休日申請済みの社員を視覚的に区別（グレーアウト等） |

#### 3.17.6 データ項目

| 項目 | 説明 |
|------|------|
| 社員 | 休日を希望する社員（employee_id） |
| 希望日 | 休日希望の日付（day_off_date） |
| 登録者 | 入力した配置担当者（registered_by） |

---

## 4. 非機能要件

### 4.1 パフォーマンス
- 一覧表示: 1000件以下で2秒以内
- 検索結果表示: 1秒以内

### 4.2 可用性
- 業務時間内の安定稼働
- データの定期バックアップ

### 4.3 セキュリティ
- HTTPS通信
- 認証必須
- SQLインジェクション対策（Supabaseで担保）

### 4.4 拡張性
- データベースの冗長性を確保
- 将来の機能追加に対応できる設計

---

## 5. 画面一覧（予定）

| 画面名 | 概要 |
|--------|------|
| ログイン | メール・パスワード認証 |
| ダッシュボード | 概要表示、アラート、クイックアクセス |
| **業務管理計画書** | 受注データの一覧・詳細・編集・配置を一体化したメイン画面（配置担当者向け） |
| **受注簿** | 月別の受注一覧グリッド、日付セルクリックで編集（配置担当者・営業担当者向け） |
| **Quick Access** | 営業担当者向けの受注クイック入力画面（モバイルファースト） |
| **経理画面** | 電票照合・グループ間請求の確認・処理（経理向け） |
| **休日申請管理** | 社員の休日申請をカレンダー形式で一括管理（配置担当者向け） |
| 社員管理 | 社員マスタの管理 |
| 車両管理 | 車両マスタ・配車の管理 |
| 契約先管理 | 契約先マスタの管理 |
| 現場管理 | 現場マスタ・区分の管理 |
| レポート | 各種集計・レポート |
| 設定 | ユーザー設定、システム設定 |

### 5.1 業務管理計画書（メイン画面）【React先行開発】

既存のExcel業務管理計画書と同様のレイアウトを採用し、受注一覧・受注詳細・編集・人員配置を1つの画面で操作可能にする。React + Next.js で先行開発し、後続でExcel VBA版UIも構築する。

#### 5.1.1 画面構成

詳細な画面レイアウトは以下のHTMLファイルを参照：
**[screen-layout.html](screen-layout.html)** - 業務管理計画書の画面モックアップ

ブラウザで開くと、以下の内容を確認できる：
- 実際の画面に近いビジュアルモックアップ
- 区分別の色分け表示
- 昼/夜シフトの背景色
- 人数不足のハイライト表示
- 連続勤務・配置制約のインジケーター
- 凡例・操作説明

#### 5.1.2 レイアウト詳細

| エリア | 内容 |
|--------|------|
| ヘッダー部 | 日付選択（前後ナビ・日付ピッカー）、グループ会社フィルタ（モーダルでチェックボックス複数選択＋プリセット） |
| ツールバー | アクションボタン群（行操作、ソート・カラー設定等） |
| メイングリッド | No. / 会社・現場名 / 集合 / 時間 / 人数 / 配置（右からD&D） / 作業内容 / 地図 / 車両 / 備考 |
| 右サイドパネル | 配置状況ミニマップ（アコーディオン）＋ 社員リスト（部署別タブ・検索・ドラッグ元） |

**右サイドパネル詳細**:

| コンポーネント | 説明 |
|---------------|------|
| 配置状況ミニマップ | サイドパネル上部にアコーディオン表示。全現場の配置状況（シフトバッジ＋現場名＋配置済み/必要人数）を一覧。クリックで該当行にスクロール |
| 社員リスト | 部署別の縦タブ構成（すべて / 東央警備 / NikkeiHD）。各グループ会社タブ内はサブタブ（施設課 / 交通課）で絞り込み。検索ボックスで社員名フィルタ。社員をドラッグして配置ゾーンにドロップ |
| 車両タブ | メインタブ切替（社員リスト / 車両）。車両タブではグループ会社別に車両リスト＋ETCカードリストを表示。配置済みはグレーアウト表示。検索ボックスでフィルタ可能 |

#### 5.1.3 表示項目

| 列 | 説明 | クリック時 |
|----|------|-----------|
| No. | 行番号（表示順） | — |
| 会社/現場名 | 昼夜バッジ＋区分バッジ、契約先名、現場名（階層連結表示）。グループ会社は行背景色で識別 | 現場詳細入力モーダルを開く |
| 時間 | 開始-終了時間を表示 | 時間モーダルを開く |
| 作業内容 | 区分に紐づくバッジ（親バッジ＋子バッジ）を表示。親バッジグループ間は点線区切り、親子間は改行で区切る | 作業内容モーダルを開く |
| 地図 | GoogleマップURLが設定済みの場合「MAP」ボタン表示、未設定の場合「＋」表示 | 地図URLモーダルを開く |
| 集合 | 集合時間＋連絡タイプバッジ（会社/直/LINE等）。現場監督バッジをグリッド表示（氏名＋電話番号） | 集合モーダルを開く |
| 人数 | 配置済み/必要人数（例: 1/2）。不足時は赤色＋「不足」バッジ、過多時はグレー＋「過多」バッジを表示 | インライン編集 |
| 配置（右からD&D） | 配置済み社員名＋連絡タイプバッジを表示。社員リストからドラッグ&ドロップで配置 | 社員名クリックで連絡タイプ変更 |
| 車両 | 上部に車両タグ、下部にETCタグの2段構成。車両タブからドラッグ&ドロップで配置。列内どこにドロップしても自動振り分け。配置済みタグは別行へドラッグ移動可能。同一シフト帯の重複配置は不可 | 車両タグクリックで編集 |
| 備考 | 送迎・集合場所・備考等をアイテム形式（ラベル＋値のカラータグ）で表示。プリセット3種（送迎/集合場所/備考）＋カスタムアイテム追加可能 | 送迎・備考モーダルを開く |

#### 5.1.4 シフト区分の表示

| シフト | 表示方法 |
|--------|----------|
| 昼 | 通常表示（白背景） |
| 夜 | 白背景、昼夜バッジに「夜」表示。契約先名・現場名・時間・人数・配置社員名・監督バッジ・集合連絡バッジの文字色を紫色（#b024cc）に変更 |

#### 5.1.5 アクションボタン

| ボタン | 機能 |
|--------|------|
| ＋新規追加 | 新規現場行を追加するモーダルを開く（ツールバー先頭に配置） |
| 契約先名横の＋ボタン | 同一行の会社・区分・契約先名を引き継いだ新規追加モーダルを開く |
| 業務名横の昼夜＋ボタン | 同一行の全情報を引き継ぎ、対になる昼夜を自動選択した新規追加モーダルを開く。昼夜ペアが揃っている場合は非表示 |
| 戻す / やり直す | Undo / Redo |
| 行削除 | 選択行を削除 |
| 消去 | 選択した配置をクリア |
| ソート設定 | ユーザー指定の並び替え条件を設定 |
| カラー設定 | グループ会社背景色（3社）、区分カラー（10種）、シフトカラー（昼/夜）のカスタマイズ。ベース色を選択すると背景色・文字色を自動生成。各カラーピッカー横にhexコード入力フィールド、パネル上部にプリセットカラーパレット（15色）。パネル外クリックで閉じる |

#### 5.1.5.1 自動ソート

行の追加・編集時に以下の優先順で自動ソートを実行する。手動での並べ替え（↑上へ/↓下へ）は廃止し、自動ソートに統一する。

| 優先順 | ソートキー | 順序 |
|--------|------------|------|
| 1 | グループ会社 | マスタ定義順 |
| 2 | 区分 | マスタ定義順 |
| 3 | 契約先名 | 五十音順 |
| 4 | 業務名 | 五十音順 |
| 5 | 昼夜 | 昼→夜の固定順 |

#### 5.1.5.2 新規追加時の重複防止

| 機能 | 説明 |
|------|------|
| リアルタイム重複検証 | 新規追加・行編集モーダルで、入力・選択のたびに5項目（会社・区分・契約先名・業務名・昼夜）の完全一致をチェック |
| 保存ボタン無効化 | 重複検出時に保存ボタンを無効化（薄色表示＋クリック不可） |
| 警告メッセージ | 「※ この組み合わせは既に存在します」をモーダル内に赤文字で表示 |
| 昼夜チップ無効化 | 既に存在するシフトのチップを無効化表示（全ての新規追加経路で適用） |
| 昼夜ペア完成時の＋ボタン非表示 | 業務名横の昼夜＋ボタンは、昼夜ペアが揃っている場合は表示しない |

#### 5.1.6 セル操作

| 操作 | 動作 |
|------|------|
| 会社/現場名セルクリック | 現場詳細入力モーダルを開く |
| 時間セルクリック | 時間モーダルを開く |
| 作業内容セルクリック | 作業内容モーダルを開く（バッジ選択） |
| 地図セルクリック | 地図URLモーダルを開く |
| 集合セルクリック | 集合モーダルを開く（集合時間・連絡タイプ・集合場所） |
| 人数セルクリック | 人数のインライン編集 |
| 車両セルクリック | 車両タグの編集（車両リストからD&D、またはクリックで選択） |
| 備考セルクリック | 送迎・備考モーダルを開く（アイテムベース編集） |
| 配置ゾーンへドラッグ | 社員リストから配置ゾーンへドラッグ&ドロップで配置 |
| 配置済み社員名クリック | 連絡タイプ変更ポップアップを開く |
| 配置済み社員の×ボタン | 配置を解除 |

#### 5.1.7 モーダル一覧

各セルクリックで専用のモーダルが開き、詳細情報を編集可能。

**現場詳細入力モーダル**（時間/会社/現場名セルクリック）

| 項目 | 説明 |
|------|------|
| 契約先（元請け） | コンボボックスで選択 |
| 業務管理計画書 業務名 | 読み取り専用（契約先→現場→業務詳細から自動生成） |
| 現場名 | コンボボックスで選択（契約先に紐づく現場一覧） |
| 業務詳細 | 動的追加可能なサブタスク（ラベル＋値ペア） |
| 区分 | チップ選択（単一選択、新規追加可能） |
| 昼夜 | チップ選択（単一選択） |
| 会社 | チップ選択（グループ会社、単一選択） |
| 現場監督 / 連絡先 | テキスト入力。同一業務名の過去入力実績から候補をチップ表示（選択・削除・並替可） |

**時間モーダル**（時間セルクリック）

| 項目 | 説明 |
|------|------|
| 開始時間 | タイムピッカー |
| 終了時間 | タイムピッカー |

**作業内容モーダル**（作業内容セルクリック）

| 項目 | 説明 |
|------|------|
| 親バッジ | 区分名（読み取り専用） |
| 子バッジ | チップ選択（複数選択可、追加可能） |
| 孫バッジ | 子バッジ配下の詳細バッジ（複数選択可、追加可能） |

**集合モーダル**（集合セルクリック）

| 項目 | 説明 |
|------|------|
| 集合時間 | タイムピッカー |
| 連絡 | チップ選択（会社/直/LINE等、追加可能） |

**送迎・備考モーダル**（備考セルクリック）

アイテムベースの入力形式。各アイテムはラベル＋値＋ベース色＋文字色で構成される。

| 項目 | 説明 |
|------|------|
| プリセットボタン | 「＋送迎」「＋集合場所」「＋備考」で定型アイテムを追加（プリセットごとにデフォルト色あり） |
| アイテム行 | ラベル入力＋値入力＋ベース色ピッカー＋文字色ピッカー。ベース色変更で文字色を自動生成 |
| 並べ替え | ドラッグ&ドロップでアイテムの表示順を変更 |
| カスタムアイテム | 「＋項目を追加」で任意のラベル・値のアイテムを追加可能 |
| グリッド表示 | 各アイテムをカラータグ（ラベルバッジ＋値テキスト）として備考列に表示 |

#### 5.1.7.1 モーダルデザインパターン（確定仕様）

全画面共通のモーダルデザインパターン。UIコンポーネント（`docs/ui-components/index-light.html#modal-design-pattern`）にビジュアル仕様あり。

**CSS命名規則:** `modal-*` プレフィックスに統一（`ob-modal-*` は廃止）

| 要素 | クラス名 | 説明 |
| ---- | -------- | ---- |
| オーバーレイ | `.modal-overlay` | 背景暗転レイヤー |
| コンテナ | `.modal` | モーダル本体 |
| ヘッダー | `.modal-header` | タイトル＋閉じるボタン |
| ボディ | `.modal-body` | スクロール可能な内容領域 |
| フッター | `.modal-footer` | アクションボタン領域 |
| 閉じるボタン | `.modal-close` | ✕ボタン（ヘッダー右上） |
| セクション区切り | `.modal-section-bar` | テキスト＋横線の区切り |
| カード | `.modal-body-card` | 白背景のグループ化カード |

**サイズバリアント（4段階）:**

| バリアント | クラス | 幅 | 用途例 |
| --------- | ------ | --- | ------ |
| sm | `.modal-sm` | 380px | 行編集、ソート、打合せ、作業時間、備考、地図 |
| md | `.modal`（デフォルト） | 480px | セル編集 |
| lg | `.modal-lg` | 520px | 現場詳細、カレンダー入力、変更通知 |
| xl | `.modal-xl` | 600px+ | 将来の大型モーダル用 |

**ヘッダー:** 全モーダル統一で `#5AB8C6` 背景・白文字。閉じるボタン右上配置。

**ボディ背景（2パターン）:**

| パターン | 背景 | 用途 |
| ------- | ---- | ---- |
| カード型 | `#E9F1F6` 背景 + 白カード(`.modal-body-card`) | 現場詳細、カレンダー編集パネル、打合せ、行編集、備考等 |
| フラット型 | `var(--bg-surface)` 背景のみ | ソート、変更通知等の特殊レイアウトモーダル |

**フッター配置パターン（4種）:**

| パターン | レイアウト | 用途 |
| ------- | -------- | ---- |
| A) 削除あり型 | `[Delete] ─── [Cancel] [Save]` | 現場詳細、カレンダー |
| B) 基本型 | `─────── [Cancel] [Save]` | 打合せ、作業時間、備考等 |
| C) 閉じるのみ型 | `─────── [Close]` | 変更通知（読み取り専用） |
| D) フッターなし | ヘッダーの✕ボタンのみ | ポップアップ、インライン編集 |

**オーバーレイ:** `rgba(0, 69, 84, 0.35)`（アクセント色ベース）、`position: fixed; inset: 0; z-index: 1000`

**開閉アニメーション（Fade + Scale）:**

- オーバーレイ: `opacity: 0 → 1`（0.2s ease）
- モーダル本体: `scale(0.95) → scale(1)` + `opacity: 0 → 1`（0.2s ease）

**共通仕様:**

- 角丸: `border-radius: 12px`
- 影: `var(--shadow-strong)`
- 枠線: `1px solid var(--divider)`
- 最大高さ: `85vh`
- スクロールバー: `scrollbar-width: thin; width: 5px`
- オーバーレイクリックで閉じる

**追加要素（モーダル関連パターン）:**

| 要素 | 説明 | 適用箇所 |
| ---- | ---- | -------- |
| iOSアラート | 確認ダイアログ（OK/Cancel、破壊的操作の確認） | 削除確認等 |
| iOSアクションシート | アクション選択メニュー（グループ + Cancel） | 操作選択 |
| 競合バナー | ヘッダー下の黄色警告バナー（他ユーザーが編集中） | セル編集、カレンダー |
| トースト通知 | 保存成功・エラー等の一時的フィードバック（自動消去） | 全画面共通 |
| ローディング状態 | モーダル内の処理中表示（スピナー or スケルトン） | データ読み込み、保存中 |

#### 5.1.8 視覚的インジケーター

| 表示 | 意味 |
|------|------|
| 連続勤務アイコン | 昼→夜、夜→昼などの連続勤務を検知時に表示 |
| 配置制約アイコン | 相性の悪い社員が同じ現場に配置された場合に警告表示 |
| 人数不足バッジ | 必要人数に対して配置人数が不足している場合、赤枠「不足」バッジを表示 |
| 人数過多バッジ | 必要人数に対して配置人数が超過している場合、グレー枠「過多」バッジを表示 |
| 資格不足アイコン | 必要資格を持たない社員が配置された場合（将来機能） |

#### 5.1.9 フィルタ・ソート

| 機能 | 説明 |
|------|------|
| グループ会社フィルタ | モーダルでチェックボックス複数選択。プリセット（すべて/東央+Nikkei/各社単独）あり。デフォルトはログインユーザーの所属グループ会社により自動設定（東央・Nikkei所属→東央+Nikkei、全日本所属→全日本のみ） |
| 契約先フィルタ | 特定の契約先のみ表示 |
| 区分フィルタ | 特定の区分のみ表示 |
| 自動ソート | 行の追加・編集保存時に、会社→区分→契約先名→業務名→昼夜の順で自動ソート（5.1.5.1参照） |
| 空行非表示 | 配置のない行を非表示にするトグル |

---

## 6. 開発フェーズ

### ■ React + Node.js Webアプリ（先行開発）

#### Phase 1: MVP（最優先）

1. プロジェクトセットアップ（Next.js + Supabase）
2. 認証機能（ログイン・ログアウト）
3. 基本的なマスタ管理（契約先、現場、社員）
4. 受注データ一覧・検索・フィルタ

#### Phase 2: コア機能（業務管理計画書UI）

1. 業務管理計画書のメイン画面（Reactコンポーネント）
2. 日毎配置の登録・編集（ドラッグ&ドロップ配置）
3. 配置者への追加情報（連絡方法、車両・送迎、連続勤務）
4. 受注データ登録・編集・削除

#### Phase 3: データ連携・リアルタイム同期

1. Supabase Realtimeによるリアルタイム同期
2. リアルタイム変更通知システム（トースト・モーダル・元に戻す/やっぱり反映）
3. 入力時あいまい一致バリデーション（表記揺れ検知）
4. Excelファイルのインポート・エクスポート機能

#### Phase 4: 拡張機能

1. 車両管理機能
2. Googleカレンダー連携
3. アラート・通知（画面表示）
4. ペナルティ管理

#### Phase 5: 高度な機能

1. 履歴管理
2. レポート・集計機能
3. 通知の拡張（メール、LINE等）
4. 詳細な権限管理

### ■ Excel VBA UI（後続開発）

#### Phase 6: Excel UI構築

1. VBA用API設計（Supabase REST API）
2. Excel業務管理計画書UIの構築
3. DB読み込み機能（DB→Excel）
4. DB書き込み機能（Excel→DB）
5. Excel側の差分処理UI（VBAフォーム）

---

## 7. 今後の検討事項

- [x] 詳細なデータ項目の確定
- [x] 既存Excelのデータ構造分析
- [x] データベース設計（ER図）
- [ ] API設計
- [ ] UI/UXデザイン

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-02-03 | 初版作成 |
| 2026-02-03 | 追加要件反映（移動手段、連続勤務、車両管理、ペナルティ管理、Excel連携） |
| 2026-02-05 | データベース設計に合わせて更新（移動手段から電車削除、現場階層構造追加、社員配置制約追加、車両配置を日毎配置レベルに変更、資格・スキル設計詳細化） |
| 2026-02-05 | データベース構造（テーブル相関関係）セクション追加 |
| 2026-02-05 | 画面一覧を更新：「受注一覧」「受注詳細/編集」を統合した「業務管理計画書」画面の詳細設計を追加 |
| 2026-02-05 | 画面レイアウトHTML（screen-layout.html）を作成、要件定義からリンク |
| 2026-02-21 | 開発方針変更：React + Node.js を先行開発、Excel VBA UIを後続開発に位置づけ。開発フェーズを再構成 |
| 2026-02-27 | 受注変更履歴・ロールバック要件追加（3.13.1）。daily_assignmentsをdaily_site_orders（受注）とdaily_assignments（配置）に分離。楽観的ロック・競合検知を追加。テーブル相関に受注変更履歴系（3.13.7）を追加 |
| 2026-03-04 | 信頼度（受注確度）要件追加（3.1.2）: 確定/予定（高）/予定（低）の3段階、グリッド文字色の不透明度変更、自動確定ルール（過去日付・配置確定）、合計の最小～最大表示 |
| 2026-03-04 | 行情報編集モーダル要件追加（3.1.3）: チップ選択UI（会社・区分・昼夜）、区分の動的追加とバッジ定義同期 |
| 2026-03-04 | バッジ（区分子バッジ）要件追加（3.1.4）: 親バッジ=区分名（読み取り専用）、子バッジ=複数選択チップ、動的追加、グリッド表示 |
| 2026-03-04 | モックアップ整合性監査: 祝日ハイライト（3.1.6）、夜勤行表示（3.1.7）、区分デフォルトラベル（3.1.8）を追加 |
| 2026-03-04 | 連絡タイプ管理（3.3.1）追加: 会社/直/LINE/迎え/OP/他の6種。配置詳細単位で管理 |
| 2026-03-04 | 差分確認・反映（3.9.3）追加 → 2026-03-18にリアルタイム同期（3.9.3）+あいまい一致バリデーション（3.9.4）に変更 |
| 2026-03-04 | 車両・送迎グリッド表示（3.7.4）追加: display_itemsによるタグ表示、色カスタマイズ、ドラッグ並替、自由追加 |
| 2026-03-06 | ユーザーロール定義追加（2.1.1）: 営業担当者・配置担当者・経理の3ロール、業務フロー概要（2.1.2） |
| 2026-03-06 | Quick Access機能追加（3.12）: 営業担当向け受注クイック入力、モバイルファースト、カテゴリタブ |
| 2026-03-06 | グループ間請求管理追加（3.13）: 配置後のグループ会社間請求の自動検知・経理確認、テーブル相関（3.16.10） |
| 2026-03-06 | 画面一覧に受注簿・Quick Access・経理画面を追加 |
| 2026-03-06 | 対象契約先に全日本エンタープライズを追加 |
| 2026-03-06 | 休日申請管理追加（3.17）: 社員の休日申請一元管理、カレンダーUI、ドラッグ&ドロップ入力、配置連携（配置不可） |
| 2026-03-06 | 画面一覧に休日申請管理を追加 |
| 2026-03-09 | 業務管理計画書モックアップ反映: レイアウト詳細（5.1.2）、表示項目（5.1.3）、セル操作（5.1.6）、モーダル体系（5.1.7）を現行モックアップに合わせて更新。夜勤行の時間赤色表示（3.1.7）を追加 |
| 2026-03-06 | テーブル相関に休日申請系（3.16.11）を追加 |
| 2026-03-19 | 受注簿モックアップ反映: カレンダー入力モーダル（3.1.10）追加、バッジ表示トグル・列表示/非表示トグルを3.1.5に追加 |
| 2026-03-19 | 配置場所名（placement_name）を廃止: 3.1.9から自動命名・配置場所名の記述を削除 |
| 2026-03-19 | 現場監督候補を動的取得方式に変更: 5.1.7の記述を更新、sites.supervisor_candidates廃止 |
| 2026-03-19 | 現場地図URL（map_url）をdaily_assignmentsからdaily_site_ordersに移動: 3.5.2・3.16.3の記述を更新 |
| 2026-03-26 | 部署マスタ追加（3.6.1.1）: グループ会社ごとの部署定義、社員マスタからFK参照、社員リスト部署タブで使用 |
| 2026-03-26 | モックアップ反映: 車両列独立（5.1.2/5.1.3）、備考をアイテム形式に変更（5.1.3/5.1.7）、カラー設定パネル強化（5.1.5）、右サイドパネル詳細（ミニマップ+部署タブ）追加、3.7.4を車両・備考グリッド表示に更新 |
| 2026-03-26 | ETCカード管理追加（3.7.2.1）: グループ会社別のETCカード管理、車両列への配置 |
| 2026-03-26 | 配車制約追加（3.7.2）: 車両・ETCのシフト帯重複制約、行間ドラッグ移動 |
| 2026-03-26 | 車両列を2段構成に変更（3.7.4/5.1.3）: 上部=車両、下部=ETC、自動振り分けドロップ |
| 2026-03-26 | 右サイドパネルに車両タブ追加（5.1.2）: 社員リスト/車両のメインタブ切替 |
| 2026-03-30 | モーダルデザインパターン確定仕様追加（5.1.7.1）: CSS命名統一（modal-*）、サイズ4段階、ヘッダー/ボディ/フッター/オーバーレイ/アニメーション仕様 |
